# The ONLY secret in this project (EN-041, ING-021).
# Obtain per SPEC-01 §2 (ING-020): register at https://transparency.entsoe.eu,
# request REST API access, generate the token under account settings.
# Copy this file to .env (gitignored). Never put the token in YAML, code, or logs (A-7).
ENTSOE_API_TOKEN=your-token-here
