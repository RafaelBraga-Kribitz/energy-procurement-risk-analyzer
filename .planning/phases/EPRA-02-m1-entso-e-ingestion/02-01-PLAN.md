---
phase: EPRA-02-m1-entso-e-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ADR/ADR-003_entsoe-raw-client-sg01.md
  - docs/ADR/ADR-004_pyarrow-parquet-engine.md
  - docs/ADR/ADR-005_latest-complete-month-sg02.md
  - pyproject.toml
  - src/epra/ingest/exceptions.py
  - tests/conftest.py
autonomous: true
requirements:
  - REQ-ING-01
  - ING-022
  - ING-042
user_setup: []
must_haves:
  truths:
    - SG-01 and SG-02 are binding via merged ADRs before parser or fetch code starts
    - pyarrow is pinned and importable so pandas parquet I/O works
    - Ingest exception hierarchy exists for transport, auth, contract, and gate failures
  artifacts:
    - docs/ADR/ADR-003_entsoe-raw-client-sg01.md
    - docs/ADR/ADR-004_pyarrow-parquet-engine.md
    - docs/ADR/ADR-005_latest-complete-month-sg02.md
    - src/epra/ingest/exceptions.py
    - tests/conftest.py
  key_links:
    - ADR-003 references ING-009/022/050/060 output contracts preserved per A-1
    - pyproject.toml pyarrow pin matches ADR-004
---

<objective>
Record Wave 0 architecture decisions (SG-01, pyarrow, SG-02) and lay ingest error-handling plus test infrastructure so downstream plans can implement without re-litigating client choice or parquet engine.

Purpose: Unblocks _fetch and _io per RESEARCH Wave 0 gate; satisfies A-1 spec supremacy for ING-022 deviation.
Output: Three ADRs, pyarrow dependency, exceptions.py, conftest.py with tmp data paths.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/EPRA-02-m1-entso-e-ingestion/02-RESEARCH.md
@.planning/phases/EPRA-02-m1-entso-e-ingestion/02-PATTERNS.md
@docs/SPEC-01_data_ingestion.md
@docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md
@docs/ADR/ADR-001_light-governance-no-external-kit.md
@src/epra/common/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADRs for SG-01, pyarrow, and SG-02</name>
  <files>docs/ADR/ADR-003_entsoe-raw-client-sg01.md, docs/ADR/ADR-004_pyarrow-parquet-engine.md, docs/ADR/ADR-005_latest-complete-month-sg02.md</files>
  <action>
Create ADR-003 adopting SG-01: use entsoe-py EntsoeRawClient as transport only (still satisfies ING-022 entsoe-py mandate); own Appendix-A XML parsers in entsoe.py for §7 contracts; EntsoePandasClient forbidden for persistence; raw XML cached per ING-009; output contracts unchanged.

Create ADR-004: add pyarrow>=18,<26 to runtime dependencies (SPEC-07 pin list omission); pandas to_parquet/read_parquet as canonical engine for _io and ING-070; cite RESEARCH Package Legitimacy Audit pyarrow [ASSUMED] OK.

Create ADR-005 adopting SG-02: latest_complete_month returns first day of min(AT prices complete month, DE-LU prices complete month); document completeness rule (all calendar days in month have price rows after hourly aggregation); load/gen completeness checked by gates but not in this function.

Follow ADR-001 format (Context, Decision, Consequences, Spec deviations). Set Status: accepted. Cross-reference SG-01/SG-02 rows in 14_SPEC_GAPS.md as adopted.
  </action>
  <verify>
    <automated>test -f docs/ADR/ADR-003_entsoe-raw-client-sg01.md &amp;&amp; test -f docs/ADR/ADR-004_pyarrow-parquet-engine.md &amp;&amp; test -f docs/ADR/ADR-005_latest-complete-month-sg02.md &amp;&amp; grep -c "EntsoeRawClient" docs/ADR/ADR-003_entsoe-raw-client-sg01.md | grep -qv "^0$"</automated>
  </verify>
  <done>Three accepted ADRs exist; SG-01 and SG-02 marked adopted; ING-022 deviation explicitly documented with preserved output contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Pin pyarrow and refresh install</name>
  <files>pyproject.toml</files>
  <action>
Add pyarrow>=18,<26 to project.dependencies in pyproject.toml per ADR-004. Run uv pip install -e ".[dev]" so venv picks up pyarrow. Verify import: pandas can resolve a parquet engine (pyarrow). Do not add other new dependencies in this task.
  </action>
  <verify>
    <automated>uv run python -c "import pyarrow; import pandas as pd; pd.io.parquet.get_engine('pyarrow'); print('ok')"</automated>
  </verify>
  <done>pyarrow importable; pandas parquet engine available; pyproject.toml lists pyarrow pin.</done>
</task>

<task type="auto">
  <name>Task 3: Ingest exceptions and pytest conftest</name>
  <files>src/epra/ingest/exceptions.py, tests/conftest.py</files>
  <action>
Create src/epra/ingest/exceptions.py per PATTERNS and 03_MODULES: IngestError base; IngestAuthError, IngestTransportError, ContractError, GateFailure, NoDataError subclasses with actionable messages (gate id, HTTP status, column names). Docstring cites Implements: ING-006 context.

Create tests/conftest.py with pytest fixtures: tmp_settings redirecting paths.data_raw and paths cache/reports to tmp_path; helper to load_settings with overridden paths (mirror test_logging_and_db pattern). Fixture entsoe_fixtures_dir pointing at tests/fixtures/entsoe (mkdir placeholder README if empty).

No production ingest logic yet — infrastructure only.
  </action>
  <verify>
    <automated>uv run pytest tests/conftest.py --collect-only 2>&1 | grep -q conftest || uv run python -c "from epra.ingest.exceptions import GateFailure, IngestAuthError"</automated>
  </verify>
  <done>Exception hierarchy importable; conftest provides tmp_path-backed Settings for ingest tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ADR → implementation | ADR text becomes binding; incorrect ADR misroutes all M1 code |
| pyproject → supply chain | New runtime dependency pyarrow must match pinned registry package |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | pyproject.toml pyarrow pin | medium | mitigate | ADR-004 documents pin; install only from PyPI via uv; RESEARCH legitimacy audit |
| T-02-02 | Information disclosure | ADR content | low | accept | ADRs contain no secrets; token handling deferred to _fetch plan |
| T-02-SC | Tampering | pip/uv install | high | mitigate | pyarrow pre-pinned in SPEC-07 family; slopcheck via pinned version range ADR-004 |
</threat_model>

<verification>
ADR-003/004/005 on disk; uv run python -c "import pyarrow"; exceptions importable.
</verification>

<success_criteria>
Wave 0 decisions frozen; pyarrow works; test harness ready for _io/_fetch plans.
</success_criteria>

<output>
Create `.planning/phases/EPRA-02-m1-entso-e-ingestion/02-01-SUMMARY.md` when done
</output>
