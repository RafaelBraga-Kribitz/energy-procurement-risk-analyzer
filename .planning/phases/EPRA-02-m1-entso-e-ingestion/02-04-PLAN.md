---
phase: EPRA-02-m1-entso-e-ingestion
plan: 04
type: tdd
wave: 4
depends_on:
  - 02-01
  - 02-03
files_modified:
  - src/epra/ingest/entsoe.py
  - tests/fixtures/entsoe/README.md
  - tests/fixtures/entsoe/prices_pt60m_at.xml
  - tests/fixtures/entsoe/prices_pt15m_at.xml
  - tests/fixtures/entsoe/prices_a03_forward_fill.xml
  - tests/fixtures/entsoe/load_at.xml
  - tests/fixtures/entsoe/gen_at.xml
  - tests/fixtures/entsoe/acknowledgement.xml
  - tests/fixtures/entsoe/dst_spring.xml
  - tests/fixtures/entsoe/dst_fall.xml
  - tests/unit/test_entsoe_parse.py
  - tests/unit/test_aggregate_hourly.py
autonomous: true
requirements:
  - REQ-ING-01
  - ING-031
  - ING-032
  - ING-050
  - ING-051
  - ING-060
  - ING-061
  - ING-062
  - ING-063
must_haves:
  truths:
    - parse_publication_xml returns §7 price columns with UTC ts_utc and resolution
    - parse_gl_xml returns load or generation long-format frames per ING-032
    - A03 forward-fill applied within period only; fill count in frame attrs
    - hourly_mean uses arithmetic mean not sum for PT15M quarters (T-2)
  artifacts:
    - src/epra/ingest/entsoe.py (parser functions)
    - tests/fixtures/entsoe/*
    - tests/unit/test_entsoe_parse.py
    - tests/unit/test_aggregate_hourly.py
  key_links:
    - Parsers use epra.common.timeutil.to_utc at boundary (ING-031, T-4)
    - Non-EUR/MWH XML raises ContractError (ING-050)
---

<objective>
Implement pure ENTSO-E XML parsers and hourly aggregation helper with fixture-driven TDD — no orchestration yet.

Purpose: Highest-risk M1 logic (resolution, A03, DST, 15-min mean) isolated and tested before backfill wiring.
Output: Parser API pinned in 03_MODULES; committed XML fixtures; ING-062/063 tests green.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/SPEC-01_data_ingestion.md
@docs/EXECUTION_BLUEPRINT/03_MODULES.md
@docs/ADR/ADR-003_entsoe-raw-client-sg01.md
@src/epra/common/timeutil.py
@src/epra/ingest/exceptions.py
</context>

<feature>
  <name>ENTSO-E XML parsers and hourly aggregation</name>
  <files>src/epra/ingest/entsoe.py, tests/unit/test_entsoe_parse.py, tests/unit/test_aggregate_hourly.py</files>
  <behavior>
    parse_publication_xml(xml) -> DataFrame columns ts_utc, price_eur_mwh, resolution, zone; EUR and MWH asserted from XML paths Appendix A; Acknowledgement -> NoDataError; A03 fills tracked in attrs a03_fills.

    parse_gl_xml(xml, kind load|generation) -> load: ts_utc, load_mw, resolution, zone; generation long: ts_utc, psr_type, psr_name, kind, value_mw, resolution, zone with PSR_NAMES dict Appendix B.

    infer_resolution(ts Series) -> PT60M or PT15M from median spacing.

    hourly_mean(df, value_col) -> hourly rows via ts_utc.floor h and mean aggregation (ING-061/062).

    iter_chunks(start, end) -> quarterly date pairs covering iter_month_starts.

    Fixtures: PT60M day, PT15M day (4 quarters known mean), A03 omitted positions, DST spring/fall days, load, gen, Acknowledgement — each ≤200 rows equivalent when parsed.
  </behavior>
  <implementation>
RED: commit failing tests reading fixtures from tests/fixtures/entsoe/. GREEN: implement parsers in entsoe.py without removing existing stub functions (backfill still raises — orchestration plan 02-05). Use lxml or xml.etree per Appendix A field paths; convert all timestamps with to_utc immediately after parse. REFACTOR: extract shared period-walking helper if needed but keep functions under ~60 lines each per W-3.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Commit XML fixtures and README</name>
  <files>tests/fixtures/entsoe/README.md, tests/fixtures/entsoe/*.xml</files>
  <action>
Create tests/fixtures/entsoe/ with README documenting provenance (synthetic or truncated real pulls), dataset type, and REQ coverage per file. Add minimal valid XML snippets: prices_pt60m_at, prices_pt15m_at, prices_a03_forward_fill, load_at, gen_at (multi-psr), acknowledgement (NoData), dst_spring, dst_fall. No API tokens in fixtures. Each snippet small enough for fast CI.

If live pull needed for one fixture, mark procedure in README; prefer hand-crafted minimal XML matching Appendix A structure.
  </action>
  <verify>
    <automated>test -d tests/fixtures/entsoe &amp;&amp; ls tests/fixtures/entsoe/*.xml | wc -l | grep -qv "^0$"</automated>
  </verify>
  <done>Fixture set exists with README; covers PT60M, PT15M, A03, load, gen, Ack, DST.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parser tests and implementation</name>
  <files>src/epra/ingest/entsoe.py, tests/unit/test_entsoe_parse.py</files>
  <behavior>
    - PT60M fixture: resolution PT60M, ts_utc UTC-aware, zone AT
    - PT15M fixture: resolution PT15M
    - A03 fixture: forward-filled positions match expected count; a03_fills attr >0
    - Wrong currency/unit raises ContractError
    - Acknowledgement raises NoDataError
    - Generation long format includes psr_type and psr_name from Appendix B
    - infer_resolution matches declared resolution on fixtures
  </behavior>
  <action>
Implement parse_publication_xml, parse_gl_xml, infer_resolution, PSR_NAMES constant, iter_chunks in entsoe.py per 03_MODULES internal API. Keep backfill/ingest_incremental/latest_complete_month/main as NotImplementedError stubs for plan 02-05.

Docstrings cite Implements: ING-031, ING-032, ING-050, ING-060, ING-063.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_entsoe_parse.py -x</automated>
  </verify>
  <done>All parser fixture tests pass; UTC normalization at boundary verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ING-062 hourly mean tests and helper</name>
  <files>src/epra/ingest/entsoe.py, tests/unit/test_aggregate_hourly.py</files>
  <behavior>
    - Synthetic PT15M day with values [10,20,30,40] for one hour aggregates to mean 25.0 not sum 100
    - Helper works for price_eur_mwh and load_mw column names
  </behavior>
  <action>
Implement hourly_mean (or hourly_mean_mw) pure function in entsoe.py for reuse by validate plan. Dedicated fixture in test_aggregate_hourly.py per ING-062. Explicit .mean() in groupby — never .sum() (T-2 trap).

Docstring: Implements ING-061, ING-062.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_aggregate_hourly.py -x</automated>
  </verify>
  <done>ING-062 guard test green; mean-not-sum enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External XML → parser | Malformed or hostile XML must not crash process or escape contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | XML fixture bombs | medium | mitigate | Parse bounded fixtures; fail on unexpected root; no eval |
| T-02-09 | Elevation | XXE in XML parser | medium | mitigate | Use defusedxml or disable external entities if using ElementTree |
</threat_model>

<verification>
uv run pytest tests/unit/test_entsoe_parse.py tests/unit/test_aggregate_hourly.py -x
</verification>

<success_criteria>
Parser API complete and fixture-tested; orchestration plan can import parsers unchanged.
</success_criteria>

<output>
Create `.planning/phases/EPRA-02-m1-entso-e-ingestion/02-04-SUMMARY.md` when done
</output>
