---
phase: EPRA-02-m1-entso-e-ingestion
plan: 05
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - src/epra/ingest/entsoe.py
  - Makefile
  - tests/unit/test_entsoe_orchestration.py
  - tests/unit/test_stubs_fail_loudly.py
autonomous: true
requirements:
  - REQ-ING-01
  - ING-001
  - ING-002
  - ING-010
  - ING-040
  - ING-041
  - ING-042
user_setup:
  - service: ENTSO-E Transparency Platform
    why: Live backfill in plan 02-07 requires API token
    env_vars:
      - name: ENTSOE_API_TOKEN
        source: "https://transparency.entsoe.eu account settings after API access approval (ING-020)"
must_haves:
  truths:
    - backfill iterates four datasets over 2019→latest complete month in ≤90-day chunks
    - ingest_incremental rewrites last 45 days of affected months
    - latest_complete_month follows ADR-005 min(AT, DE-LU) rule
    - make backfill and make ingest invoke entsoe CLI successfully (stub transport in unit tests)
  artifacts:
    - src/epra/ingest/entsoe.py (orchestration + CLI)
    - Makefile (wired targets)
  key_links:
    - Orchestration calls _fetch → parsers → _io.write_month per month per dataset
    - CLI returns exit 0/1; logging.setup at main entry
---

<objective>
Wire end-to-end ingest orchestration: backfill, incremental refresh, latest_complete_month, and Makefile/CLI entrypoints.

Purpose: Delivers ROADMAP success criterion 1 path (make backfill produces data/raw parquet).
Output: Working entsoe orchestration and Makefile targets; M1 rows removed from stubs test.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/ADR/ADR-005_latest-complete-month-sg02.md
@docs/EXECUTION_BLUEPRINT/03_MODULES.md
@src/epra/ingest/_io.py
@src/epra/ingest/_fetch.py
@src/epra/ingest/entsoe.py
@src/epra/common/logging.py
@src/epra/common/timeutil.py
@Makefile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dataset orchestration helpers</name>
  <files>src/epra/ingest/entsoe.py, tests/unit/test_entsoe_orchestration.py</files>
  <behavior>
    - ingest_dataset(settings, dataset_key, start, end, transport) writes one parquet per month under correct §7 path
    - Four dataset keys map to entsoe_prices_at, entsoe_prices_delu, entsoe_load_at, entsoe_gen_at
    - Failed month aborts before partial write for that month
  </behavior>
  <action>
Add internal functions: build EntsoeQuery per dataset/zone from settings.zones; loop iter_chunks; fetch_entsoe (injectable transport for tests); parse with parse_publication_xml or parse_gl_xml; split frame by calendar month; write_month with request_hash from fetch.

Map zones: AT prices/load/gen use settings.zones at; DE-LU prices use delu. Zone column values AT and DE_LU per §7.

Log A03 fill counts when attrs a03_fills > 0. On NoDataError for historical window, log and skip chunk per spec Ack handling.

Do not implement backfill CLI yet — function-level only.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_entsoe_orchestration.py -k ingest_dataset -x</automated>
  </verify>
  <done>Single-dataset month loop proven with stub XML transport and tmp_path settings.</done>
</task>

<task type="auto">
  <name>Task 2: backfill, ingest_incremental, latest_complete_month</name>
  <files>src/epra/ingest/entsoe.py, tests/unit/test_entsoe_orchestration.py</files>
  <action>
Implement backfill(settings, start, end): for each of four datasets, ingest_dataset over window. Default CLI backfill uses start=2019-01-01, end=last day of month before latest_complete_month (compute after data exists — for empty raw, end from settings.ingest or today minus one month conservatively).

Implement ingest_incremental(settings): end=today; start=today-45 days; re-ingest all four datasets (ING-041).

Implement latest_complete_month(settings) per ADR-005: scan entsoe_prices_at and entsoe_prices_delu monthly parquets (or empty → raise clear error); for each zone find last month where every calendar day has ≥1 price row after hourly_mean; return month_start(min(at_month, delu_month)).

Update module docstring to list all implemented ING IDs. Remove _MSG from implemented functions.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_entsoe_orchestration.py -x</automated>
  </verify>
  <done>Public API functions work with fixture-backed raw layout; latest_complete_month unit-tested with synthetic parquets.</done>
</task>

<task type="auto">
  <name>Task 3: CLI, Makefile, stub cleanup</name>
  <files>src/epra/ingest/entsoe.py, Makefile, tests/unit/test_stubs_fail_loudly.py</files>
  <action>
Implement main(argv) with argparse: --backfill, --incremental, --start, --end, --no-cache flags per ING-002/009. Call logging.setup with reports/ingestion logfile. Load settings via load_settings(); dispatch to backfill or ingest_incremental; return 0 on success, 1 on user/validation errors.

Replace Makefile stub targets backfill and ingest with uv run python -m epra.ingest.entsoe invocations matching PATTERNS.

Remove M1 entsoe rows from tests/unit/test_stubs_fail_loudly.py STUBS list. Keep validate rows until plan 02-06.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_stubs_fail_loudly.py -x &amp;&amp; grep -q "epra.ingest.entsoe" Makefile</automated>
  </verify>
  <done>make backfill and make ingest invoke real CLI; entsoe stubs removed from fail-loudly test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → CLI | argv controls date window; must not escape data/raw paths |
| Orchestration → filesystem | Only writes under settings.paths.data_raw |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | CLI date args | low | mitigate | Parse dates strictly; reject inverted ranges |
| T-02-11 | Information disclosure | ingest logs | medium | mitigate | logging.setup to reports/; no token in argparse help or logs |
</threat_model>

<verification>
uv run pytest tests/unit/test_entsoe_orchestration.py tests/unit/test_stubs_fail_loudly.py -x; make lint
</verification>

<success_criteria>
Orchestration complete; Makefile backfill/ingest wired; ready for live backfill in 02-07.
</success_criteria>

<output>
Create `.planning/phases/EPRA-02-m1-entso-e-ingestion/02-05-SUMMARY.md` when done
</output>
