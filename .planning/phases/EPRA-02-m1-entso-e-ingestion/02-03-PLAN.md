---
phase: EPRA-02-m1-entso-e-ingestion
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/epra/ingest/_fetch.py
  - tests/unit/test_fetch.py
autonomous: true
requirements:
  - REQ-ING-01
  - ING-006
  - ING-007
  - ING-008
  - ING-009
  - ING-020
  - ING-021
  - ING-030
user_setup: []
must_haves:
  truths:
    - ENTSO-E fetch uses EntsoeRawClient per ADR-003 with sequential requests and ≥0.5s sleep
    - Raw XML cached under data/cache/entsoe with token-stripped hash keys
    - 429/5xx retry with tenacity; 400/401/403 fail fast without retry
    - Logs never contain API token
  artifacts:
    - src/epra/ingest/_fetch.py
    - tests/unit/test_fetch.py
  key_links:
    - fetch_entsoe uses epra.common.config.entsoe_token for auth
    - Cache hit when window end >7 days ago and cache file exists (ING-009)
---

<objective>
Build cached, retried ENTSO-E HTTP transport (_fetch) using EntsoeRawClient per ADR-003.

Purpose: Single external API boundary for M1; satisfies ING-006..009 and A-7 secret handling.
Output: _fetch.py with EntsoeQuery and fetch_entsoe; comprehensive stub-transport tests.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/ADR/ADR-003_entsoe-raw-client-sg01.md
@docs/EXECUTION_BLUEPRINT/03_MODULES.md
@docs/SPEC-01_data_ingestion.md
@src/epra/common/config.py
@src/epra/ingest/exceptions.py
@src/epra/ingest/_io.py
@scripts/check_no_token_in_code.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EntsoeQuery and window validation</name>
  <files>src/epra/ingest/_fetch.py, tests/unit/test_fetch.py</files>
  <behavior>
    - EntsoeQuery frozen dataclass: period_end > period_start; window ≤90 days raises
    - Invalid window raises before any HTTP call
  </behavior>
  <action>
Define EntsoeQuery as frozen dataclass or pydantic _Frozen model per PATTERNS: fields for document_type, domain EIC, period_start/end as UTC-aware datetimes, optional psr_type for generation. Validate ING-030 90-day max in constructor or factory. Import IngestAuthError, IngestTransportError from exceptions.

Docstring: Implements ING-030.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_fetch.py -k EntsoeQuery -x</automated>
  </verify>
  <done>Query object enforces chunk bounds; immutable for hash stability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fetch_entsoe with cache, retry, politeness</name>
  <files>src/epra/ingest/_fetch.py, tests/unit/test_fetch.py</files>
  <behavior>
    - Live fetch writes cache file under settings.paths.data_cache/entsoe/{hash}.bin
    - Cache used when use_cache=True, file exists, and period_end >7 days before today (UTC)
    - use_cache=False always hits network stub
    - 429 raises retry; 401 raises IngestAuthError without retry
    - sleep ≥0.5s between consecutive live calls (mock time.sleep)
    - Log line matches ING-008 pattern; token absent from log message
  </behavior>
  <action>
Implement fetch_entsoe(params, settings, use_cache=True) returning XML str. Wire EntsoeRawClient(api_key=entsoe_token()) per ADR-003; map EntsoeQuery to appropriate query_day_ahead_prices / query_load / query_generation calls with pd.Timestamp tz Europe/Vienna boundaries per ING-031 at client boundary only.

Use tenacity: wait_exponential multiplier=2 min=2 max=120, stop_after_attempt(6), retry on 429/5xx/connection; no retry on 400/401/403 — raise IngestAuthError with response body snippet.

Cache: reuse _io.request_hash on request URL; write raw bytes to cache path; read on hit per ING-009 seven-day rule. Inject optional transport callable for tests.

Between live requests call time.sleep(0.5) minimum (ING-007). Log INFO with source, window, status, rows placeholder, elapsed_ms; strip token from any logged URL.

Never log or persist ENTSOE_API_TOKEN (A-7).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_fetch.py -x</automated>
  </verify>
  <done>Transport passes retry/cache/sleep matrix tests with stub HTTP; no token in logs.</done>
</task>

<task type="auto">
  <name>Task 3: Token fail-fast integration test</name>
  <files>tests/unit/test_fetch.py</files>
  <action>
Add test_fetch_entsoe_fails_without_token: monkeypatch delenv ENTSOE_API_TOKEN; fetch_entsoe raises RuntimeError from entsoe_token() before network. Mark any test hitting real ENTSO-E with @pytest.mark.live (excluded in CI per EN-070).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_fetch.py -k token -x</automated>
  </verify>
  <done>ING-021 fail-fast verified at fetch entrypoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process → ENTSO-E API | Untrusted network; token crosses boundary once per request |
| Cache files → parser | Cached XML treated as untrusted input to parser (next plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information disclosure | ING-008 logs | critical | mitigate | Strip securitytoken from URLs before log; test asserts token substring absent |
| T-02-06 | Spoofing | ENTSO-E 401/403 | high | mitigate | IngestAuthError with body; no retry; human rotates token per AGENTS §2 |
| T-02-07 | Denial of service | retry storm | medium | mitigate | tenacity cap 6 attempts; exponential backoff ING-006 |
| T-02-SC | Tampering | entsoe-py transport | high | mitigate | ADR-003 locked client; pin entsoe-py>=0.6 in pyproject |
</threat_model>

<verification>
uv run pytest tests/unit/test_fetch.py -x; uv run pytest -m "not live" includes fetch tests green.
</verification>

<success_criteria>
_fetch ready for parser plan; ING-006..009 behavior proven without live network in CI.
</success_criteria>

<output>
Create `.planning/phases/EPRA-02-m1-entso-e-ingestion/02-03-SUMMARY.md` when done
</output>
