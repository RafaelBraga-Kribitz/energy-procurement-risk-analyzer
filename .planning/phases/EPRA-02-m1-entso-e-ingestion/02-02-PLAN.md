---
phase: EPRA-02-m1-entso-e-ingestion
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/epra/ingest/_io.py
  - tests/unit/test_io.py
autonomous: true
requirements:
  - REQ-ING-01
  - ING-003
  - ING-004
  - ING-005
  - ING-070
must_haves:
  truths:
    - Monthly parquet writes are atomic (temp then rename) under data/raw layout
    - request_hash strips security token from URLs for cache keys and ING-004 column
    - Naive or out-of-month ts_utc rows rejected before write
  artifacts:
    - src/epra/ingest/_io.py
    - tests/unit/test_io.py
  key_links:
    - write_month appends ingested_at_utc, source, request_hash per ING-004
    - Path pattern matches SPEC-01 §7 data/raw/dataset/YYYY/dataset_YYYY-MM.parquet
---

<objective>
Implement the single raw parquet writer (_io) with contract enforcement at the persistence boundary.

Purpose: All four datasets share one writer; ING-003 idempotency and ING-070 dtype stability depend on this module.
Output: _io.py with request_hash and write_month; unit tests with tmp_path.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/EPRA-02-m1-entso-e-ingestion/02-PATTERNS.md
@docs/EXECUTION_BLUEPRINT/03_MODULES.md
@docs/SPEC-01_data_ingestion.md
@src/epra/common/db.py
@src/epra/common/config.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: request_hash and raw_month_path helpers</name>
  <files>src/epra/ingest/_io.py, tests/unit/test_io.py</files>
  <behavior>
    - request_hash: identical URLs differing only by securitytoken query param produce same 64-hex hash
    - request_hash: empty URL raises ValueError
    - raw_month_path: returns settings.paths.data_raw / dataset / YYYY / dataset_YYYY-MM.parquet
  </behavior>
  <action>
Implement request_hash(url) using urlparse: strip query param securitytoken (case-insensitive) before sha256 hex digest. Implement raw_month_path(dataset, month, settings) mirroring db.warehouse_path absolute/relative resolution via REPO_ROOT.

Docstrings: Implements ING-004 (hash column), ING-003 (path layout). Follow PATTERNS db.py path patterns.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_io.py -k "request_hash or raw_month_path" -x</automated>
  </verify>
  <done>Hash stable with token stripped; path matches §7 layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: write_month atomic parquet persistence</name>
  <files>src/epra/ingest/_io.py, tests/unit/test_io.py</files>
  <behavior>
    - write_month: appends ingested_at_utc (UTC ISO), source string, request_hash; writes via .parquet.tmp then os.replace
    - write_month: rejects naive ts_utc with ValueError
    - write_month: rejects rows whose ts_utc fall outside target calendar month
    - Round-trip read preserves column names and dtypes for a minimal prices frame
  </behavior>
  <action>
Implement write_month(frame, dataset, month, request_hash, settings) per 03_MODULES §_io. Pre-write validation: ts_utc must be timezone-aware UTC (use ts_utc.dt.tz or equivalent); all timestamps within month bounds. Append ING-004 columns. Log one INFO line with dataset, month, row count, path. Use pandas to_parquet with index=False (pyarrow engine from plan 02-01).

Do not deduplicate or unit-convert values (ING-004 raw semantics).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_io.py -x</automated>
  </verify>
  <done>Atomic write proven; contract columns present; boundary validation enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency and dtype stability test</name>
  <files>tests/unit/test_io.py</files>
  <action>
Add test_write_month_idempotent_byte_stable: write same frame twice to same month path; assert second write overwrites cleanly; re-read yields identical data columns (ingested_at_utc may differ — document whether ingested_at_utc is excluded from byte identity or frozen in test via monkeypatch of datetime.utcnow). Prefer monkeypatching ingested_at_utc source so two runs with same clock are byte-identical per ING-003.

Assert fixed column order in writer for determinism.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_io.py -k idempotent -x</automated>
  </verify>
  <done>ING-003 overwrite semantics verified in unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataFrame → filesystem | Untrusted parsed frames must not write outside data/raw or with wrong schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | write_month path traversal | medium | mitigate | dataset string allowlist or reject path separators; paths only under settings.paths.data_raw |
| T-02-04 | Denial of service | huge DataFrame write | low | accept | month-scoped rows; ENTSO-E chunking limits size; out-of-month rejection |
</threat_model>

<verification>
uv run pytest tests/unit/test_io.py -x; make lint passes on _io.py.
</verification>

<success_criteria>
_io module complete; all ING-003/004/005 persistence rules enforced at write boundary.
</success_criteria>

<output>
Create `.planning/phases/EPRA-02-m1-entso-e-ingestion/02-02-SUMMARY.md` when done
</output>
