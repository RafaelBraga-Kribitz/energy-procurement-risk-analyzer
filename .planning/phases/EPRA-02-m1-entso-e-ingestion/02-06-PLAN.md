---
phase: EPRA-02-m1-entso-e-ingestion
plan: 06
type: execute
wave: 4
depends_on:
  - 02-04
files_modified:
  - src/epra/ingest/validate.py
  - tests/unit/test_ingest_gates.py
autonomous: true
requirements:
  - REQ-ING-01
  - ING-080
  - ING-081
  - ING-082
  - ING-083
  - ING-084
  - ING-085
must_haves:
  truths:
    - run_gates loads raw parquet, aggregates to hourly mean, runs ING-080..085
    - Failed gate raises GateFailure with gate id (EN-061); report lists every gate once
    - Validation markdown written to reports/ingestion/validation_YYYY-MM-DD.md
    - make validate-ingest invokes validate CLI
  artifacts:
    - src/epra/ingest/validate.py
    - tests/unit/test_ingest_gates.py
  key_links:
    - validate imports hourly_mean from entsoe.py before gating
    - ING-080 uses timeutil.local_hours_in_day for DST 23/25 check
---

<objective>
Implement the ENTSO-E validation gate framework (ING-080..085) and validate-ingest CLI.

Purpose: ROADMAP success criterion 3 — make validate-ingest with gates green on real data.
Output: Pure gate functions, ValidationReport, run_gates, Makefile validate-ingest target.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/SPEC-01_data_ingestion.md
@docs/EXECUTION_BLUEPRINT/03_MODULES.md
@src/epra/ingest/entsoe.py
@src/epra/common/timeutil.py
@src/epra/ingest/exceptions.py
@scripts/oespi_reconcile.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GateResult, ValidationReport, pure gate functions</name>
  <files>src/epra/ingest/validate.py, tests/unit/test_ingest_gates.py</files>
  <behavior>
    - gate_ing_080: fails when missing hours >24 per zone-year; DST last Sunday Mar has 23 local hours, Oct has 25
    - gate_ing_081: fails when price outside [-500, 5000]
    - gate_ing_082: fails when annual mean outside spec table for that year
    - gate_ing_083: fails when no negative prices in 2023/2024/2025
    - gate_ing_084: fails when load outside hourly or annual mean bands
    - gate_ing_085: fails when price-load join coverage below 99.5% per year
    - Each gate has one pass and one fail synthetic test
  </behavior>
  <action>
Implement frozen GateResult dataclass and ValidationReport with render_markdown and raise_if_failed raising GateFailure listing failed gate_ids.

Implement gate_ing_080 through gate_ing_085 as pure functions taking hourly-aggregated DataFrames per 03_MODULES. ING-080: expected hours 8760/8784; list missing hours in evidence; DST check via local_hours_in_day on last Sunday March/October for AT.

Do not clip outliers (ING-081). ING-082 use exact table from SPEC-01 §8 — do not widen bands.

M1 scope only — do not implement ING-094/101/103 (M2); run_gates registers ENTSO-E gates only for now.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ingest_gates.py -x</automated>
  </verify>
  <done>All six gates have pass/fail unit tests; pure functions mutate no input frames.</done>
</task>

<task type="auto">
  <name>Task 2: run_gates loader and report writer</name>
  <files>src/epra/ingest/validate.py</files>
  <action>
Implement run_gates(settings): glob read monthly parquets for four ENTSO-E datasets under settings.paths.data_raw; concatenate; apply hourly_mean from entsoe for prices and load before gates (T-2, RESEARCH pitfall 6). Build AT prices hourly, DE-LU prices hourly, AT load hourly as needed per gate.

Run gates in order ING-080..085; collect GateResults; write reports/ingestion/validation_{date}.md with pass/fail summary and evidence snippets; call raise_if_failed on any failure (EN-061, A-2).

Log gate summaries at INFO. Create reports/ingestion directory if missing.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ingest_gates.py -k run_gates -x</automated>
  </verify>
  <done>Integration test with synthetic parquets in tmp_path runs full gate suite and writes report file.</done>
</task>

<task type="auto">
  <name>Task 3: validate CLI and Makefile</name>
  <files>src/epra/ingest/validate.py, Makefile, tests/unit/test_stubs_fail_loudly.py</files>
  <action>
Implement validate.main(argv) calling load_settings(), logging.setup, run_gates; return 0 if all pass else 1.

Wire Makefile validate-ingest to uv run python -m epra.ingest.validate.

Remove M1 validate rows from test_stubs_fail_loudly.py. Update validate module docstring Implements list to ING-080..085 for M1.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_stubs_fail_loudly.py -x &amp;&amp; grep -q "epra.ingest.validate" Makefile</automated>
  </verify>
  <done>make validate-ingest runs real gate runner; no validate stubs remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw parquet → gate inputs | Corrupt parquet must not crash; fail gate with evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Tampering | widening ING-082 bands | high | mitigate | Hard-code spec table; A-2 forbids adjusting data or gates without ADR |
| T-02-13 | Repudiation | silent gate skip | medium | mitigate | ValidationReport lists every registered gate exactly once |
</threat_model>

<verification>
uv run pytest tests/unit/test_ingest_gates.py -x; make lint
</verification>

<success_criteria>
validate-ingest runnable; ING-080..085 implemented with synthetic test coverage.
</success_criteria>

<output>
Create `.planning/phases/EPRA-02-m1-entso-e-ingestion/02-06-SUMMARY.md` when done
</output>
