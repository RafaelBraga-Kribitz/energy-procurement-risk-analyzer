---
phase: EPRA-02-m1-entso-e-ingestion
plan: 07
type: execute
wave: 6
depends_on:
  - 02-05
  - 02-06
files_modified:
  - tests/test_raw_contracts.py
  - tests/fixtures/entsoe/entsoe_prices_at_2024-01.parquet
  - tests/fixtures/entsoe/entsoe_prices_delu_2024-01.parquet
  - tests/fixtures/entsoe/entsoe_load_at_2024-01.parquet
  - tests/fixtures/entsoe/entsoe_gen_at_2024-01.parquet
  - docs/BUILD_LOG.md
autonomous: false
requirements:
  - REQ-ING-01
  - ING-070
user_setup:
  - service: ENTSO-E Transparency Platform
    why: Live backfill and real-data gate verification (ROADMAP criteria 1 and 3)
    env_vars:
      - name: ENTSOE_API_TOKEN
        source: "Copy .env.example to .env; paste token from transparency.entsoe.eu (ING-020/021)"
must_haves:
  truths:
    - ING-070 contract tests pass in CI against committed fixture parquets
    - Human confirms make backfill produced four dataset trees under data/raw/
    - Human confirms make validate-ingest green on real backfill data
    - BUILD_LOG documents M1 gate evidence
  artifacts:
    - tests/test_raw_contracts.py
    - tests/fixtures/entsoe/*.parquet
    - docs/BUILD_LOG.md
  key_links:
    - Fixture parquets generated via _io.write_month from parser output (≤200 rows each)
    - CI runs pytest -m "not live" including test_raw_contracts
---

<objective>
Close M1 with ING-070 contract tests, committed fixture parquets, live backfill verification, and BUILD_LOG gate evidence.

Purpose: Satisfies all three ROADMAP Phase 2 success criteria including CI contract tests and real-data validation.
Output: test_raw_contracts.py green; human-approved backfill; BUILD_LOG entry.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/SPEC-01_data_ingestion.md
@src/epra/ingest/_io.py
@src/epra/ingest/entsoe.py
@tests/unit/test_config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ING-070 raw contract tests and fixture parquets</name>
  <files>tests/test_raw_contracts.py, tests/fixtures/entsoe/*.parquet</files>
  <behavior>
    - One committed parquet per dataset dir naming entsoe_*_2024-01.parquet
    - Assert exact column names and dtypes per SPEC-01 §7 table
    - Assert zone values AT or DE_LU as specified
    - Row count ≤200 per fixture file
  </behavior>
  <action>
Create tests/test_raw_contracts.py following PATTERNS drift-guard style. For each dataset, open tests/fixtures/entsoe/ fixture parquet and assert columns: prices ts_utc, price_eur_mwh, resolution, zone plus ING-004 columns; load load_mw; gen long columns per §7.

Generate fixture parquets by running parsers on XML fixtures through write_month into tests/fixtures/entsoe (or hand-trim to ≤200 rows). Commit parquets to git (exception to ING-010 for test fixtures only).

Docstring: Implements ING-070.
  </action>
  <verify>
    <automated>uv run pytest tests/test_raw_contracts.py -x</automated>
  </verify>
  <done>Contract tests pass in CI without network.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify live backfill and validate-ingest</name>
  <files>data/raw/entsoe_*/**, reports/ingestion/validation_*.md</files>
  <action>Blocking human checkpoint after Tasks 1 and automated plans 02-01..02-06 are complete. Executor pauses; operator runs verification steps in how-to-verify before Task 3 proceeds.</action>
  <verify>
    <human-check>Operator confirms make backfill produced four dataset trees under data/raw/ and make validate-ingest reports ING-080..085 PASS.</human-check>
  </verify>
  <done>Resume signal "approved" received; real-data gates green or fix cycle documented.</done>
  <what-built>Full M1 pipeline: make backfill, make validate-ingest on real ENTSO-E data with operator-supplied token.</what-built>
  <how-to-verify>
1. Copy .env.example to .env and set ENTSOE_API_TOKEN (ING-020/021).
2. Run: make setup (if needed), then make lint && make test — all green including test_raw_contracts and gate unit tests.
3. Run: make backfill — expect progress logs; verify data/raw/entsoe_prices_at/, entsoe_prices_delu/, entsoe_load_at/, entsoe_gen_at/ contain YYYY/*.parquet files from 2019 onward.
4. Run: make validate-ingest — expect reports/ingestion/validation_*.md with ING-080 through ING-085 all PASS.
5. If any gate fails: do not widen bands — investigate parser/timezone/units per A-2 and file ADR if spec deviation needed.
  </how-to-verify>
  <resume-signal>Type "approved" if backfill and validate-ingest are green, or paste gate failure details for fix cycle.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: BUILD_LOG and phase verification snippet</name>
  <files>docs/BUILD_LOG.md</files>
  <action>
Append docs/BUILD_LOG.md entry: date 2026-07-21 (or execution date), M1 ENTSO-E shipped, gate evidence snippet (pytest count, validate-ingest PASS line, backfill dataset paths confirmed). List open questions: none if gates green. Note ADR-003/004/005 merged.

Run final verification: make lint && make test && make validate-ingest (after human checkpoint approved).
  </action>
  <verify>
    <automated>grep -q "M1" docs/BUILD_LOG.md &amp;&amp; uv run pytest -m "not live" -q</automated>
  </verify>
  <done>BUILD_LOG updated; full CI-equivalent test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator machine → ENTSO-E | Live backfill uses real token locally only |
| Committed fixtures → CI | Fixture parquets must contain no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Information disclosure | .env token | critical | mitigate | .env gitignored; check_no_token_in_code pre-commit; never commit data/raw |
| T-02-15 | Tampering | committed fixture parquets | low | accept | Small static fixtures; contract tests detect schema drift |
</threat_model>

<verification>
uv run pytest tests/test_raw_contracts.py -x; after human approval: make validate-ingest exits 0; make test -m "not live" green.
</verification>

<success_criteria>
All ROADMAP Phase 2 success criteria met; M1 ready for M2 auxiliary data phase.
</success_criteria>

<output>
Create `.planning/phases/EPRA-02-m1-entso-e-ingestion/02-07-SUMMARY.md` when done
</output>
