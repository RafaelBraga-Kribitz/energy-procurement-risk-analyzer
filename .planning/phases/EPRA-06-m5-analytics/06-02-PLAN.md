---
phase: EPRA-06-m5-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/epra/analytics/descriptive.py
  - tests/unit/test_analytics_a1.py
  - tests/unit/test_stubs_fail_loudly.py
autonomous: true
requirements:
  - AN-101
  - AN-102
  - AN-103
  - AN-104
  - AN-105
  - AN-704
must_haves:
  truths:
    - "annual_summary has AN-101 columns; synthetic year with known mean/negatives matches hand calc"
    - "NULL price_at_eur_mwh hours are dropped, not treated as 0"
    - "heatmap has 5 panels (2021–2025); missing year is empty titled no complete data; shared vmin/vmax"
    - "duration curves style 2022 vermillion; x is fraction of hours 0–100, y EUR/MWh linear (not log)"
    - "neg_hours_<year> SSOT rows VERIFIED; a1_annual_summary.md ends with ≥400 chars prose mentioning solar midday depression, 2022 crisis, negative hours for a flexible consumer — numbers from the table not invented literals"
  artifacts:
    - src/epra/analytics/descriptive.py
    - tests/unit/test_analytics_a1.py
  key_links:
    - "T5.01 kit save_png / write_markdown / ssot"
  prohibitions:
    - "do not use log axis (AN-103 log-free)"
    - "do not invent 2025 heatmap values"
    - "do not commit reports/analytics"
    - "do not implement A2–A4"
---

<objective>
A1 descriptive: AN-101..105 artifacts + SSOT neg_hours / annual_mean_price keys.

Purpose: T5.02 — Q2 seasonality / negatives / duration.
Output: descriptive.run + tests.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-06-m5-analytics/06-CONTEXT.md
</execution_context>

<context>
@docs/SPEC-04_analytics.md
@src/epra/analytics/_kit.py
@dbt/models/marts/fct_price_hourly.sql
</context>

<tasks>
<task type="auto">
  <name>Task 1: A1 pure functions + run() + tests</name>
  <read_first>
    - docs/SPEC-04_analytics.md A1
    - 06-RESEARCH.md base/peak/NULL notes
    - src/epra/report/format.py
  </read_first>
  <files>src/epra/analytics/descriptive.py, tests/unit/test_analytics_a1.py, tests/unit/test_stubs_fail_loudly.py</files>
  <action>
    annual_summary(hourly_df) → AN-101. Peak = is_peak_hour; off-peak = ~is_peak_hour; dropna on price_at_eur_mwh. Heatmap hour_local × month_local mean price, 5 panels. Duration curves. Negative hours chart + SSOT. Prose paragraph interpolates computed stats (RP-703). run() loads via kit, writes SPEC-04 §6 A1 files under reports/analytics, collects SSOT rows. Delete descriptive.run from STUBS. Tests use synthetic frames + tmp_settings (no DuckDB required for pure functions; run() may use a tiny duckdb if kit requires — prefer injecting frames via optional parameter or monkeypatch load_price_hourly).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_analytics_a1.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - AN-704 ≥400 chars after last table.
    - Stub row for descriptive.run gone.
  </acceptance_criteria>
  <done>A1 artifacts reproducible from frames.</done>
</task>
</tasks>
