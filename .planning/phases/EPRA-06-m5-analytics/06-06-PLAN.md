---
phase: EPRA-06-m5-analytics
plan: 06
type: execute
wave: 4
depends_on:
  - 06-05
files_modified:
  - src/epra/analytics/regimes.py
  - tests/unit/test_analytics_a3.py
autonomous: true
requirements:
  - AN-303
must_haves:
  truths:
    - "GARCH(1,1) constant mean on d_t; rescale /10 only if arch scale warning; documented in artifact"
    - "garch_persistence = α+β in SSOT VERIFIED; if ≥1 reported as near-integrated, never clamped"
    - "a3_garch_vs_realized.png overlays conditional vol vs 30-day realized"
    - "two-run persistence identical at full float precision"
  artifacts:
    - src/epra/analytics/regimes.py
    - tests/unit/test_analytics_a3.py
  key_links:
    - "shares d_t with T5.05"
  prohibitions:
    - "do not clamp α+β"
    - "do not always rescale"
    - "do not mix M6 work"
---

<objective>
A3 GARCH complement + garch_persistence SSOT.

Purpose: T5.06.
Output: overlay PNG + persistence key.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-06-m5-analytics/06-CONTEXT.md
</execution_context>

<context>
@docs/SPEC-04_analytics.md
@src/epra/analytics/regimes.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: GARCH(1,1) + overlay + SSOT</name>
  <read_first>
    - docs/SPEC-04_analytics.md AN-303
    - arch package GARCH API
  </read_first>
  <files>src/epra/analytics/regimes.py, tests/unit/test_analytics_a3.py</files>
  <action>
    Fit GARCH(1,1) on d_t (same series as HMM). Handle scale warning per D-11. Overlay chart. SSOT garch_persistence. Tests: two fits same persistence; synthetic series; ≥1 path documented not clamped (can inject params).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_analytics_a3.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - Persistence identity; overlay file written in run().
  </acceptance_criteria>
  <done>AN-303 complete.</done>
</task>
</tasks>
