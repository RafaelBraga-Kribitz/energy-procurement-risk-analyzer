---
phase: EPRA-06-m5-analytics
plan: 05
type: execute
wave: 3
depends_on:
  - 06-01
files_modified:
  - src/epra/analytics/regimes.py
  - tests/unit/test_analytics_a3.py
  - tests/unit/test_stubs_fail_loudly.py
autonomous: true
requirements:
  - AN-301
  - AN-302
  - AN-304
  - AN-705
must_haves:
  truths:
    - "d_t is arithmetic daily difference of price_base_eur_mwh, not log"
    - "fit_hmm uses GaussianHMM(3, full, n_iter=500), seeds 42..51, max LL, lower seed on LL tie, labels by ascending std"
    - "BLAS/OMP/MKL/OPENBLAS/NUMEXPR threads set to 1 before fit; two fits → identical state_sequence"
    - "check_an304 skip/incomplete vs fail-closed per D-06; pytest skip when coverage missing; do not widen 70/60"
    - "december_regime(year) = majority state of that December's days"
    - "a3_realized_vol.png twin axis; a3_regimes.png colored bands; a3_regime_stats.md occupancy/mean |d_t|/mean price"
  artifacts:
    - src/epra/analytics/regimes.py
    - tests/unit/test_analytics_a3.py
  key_links:
    - "T5.06 GARCH reuses d_t builder"
  prohibitions:
    - "do not use log returns"
    - "do not widen AN-304"
    - "do not invent 2019 prices for the gate"
    - "do not write ADR-014 unless AN-705 still fails after thread pin (stop instead)"
    - "GARCH may remain stub until 06-06"
---

<objective>
A3 HMM + realized vol + AN-304 + december_regime. GARCH in 06-06.

Purpose: T5.05 — fiddly regimes last among A1–A4.
Output: fit_hmm, check_an304, charts (except GARCH overlay).
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-06-m5-analytics/06-CONTEXT.md
</execution_context>

<context>
@docs/SPEC-04_analytics.md
@docs/EXECUTION_BLUEPRINT/03_MODULES.md
@docs/EXECUTION_BLUEPRINT/12_RISK_REGISTER.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: d_t, HMM, AN-304, charts</name>
  <read_first>
    - docs/SPEC-04_analytics.md A3
    - 06-CONTEXT D-06 D-09 D-10
    - hmmlearn GaussianHMM docs via installed package if needed
  </read_first>
  <files>src/epra/analytics/regimes.py, tests/unit/test_analytics_a3.py, tests/unit/test_stubs_fail_loudly.py</files>
  <action>
    daily_diff, zscore, fit_hmm → HmmFit, relabel calm/elevated/crisis. AN-304 check with coverage detection. december_regime. Realized vol 30-day std twin axis with base price. Regime timeline. Stats md. run() may still skip GARCH overlay until 06-06 (write realized+regimes+stats now). Delete regimes.run stub only if run() no longer raises — if GARCH still missing, keep run() completing HMM artifacts and leave GARCH to 06-06 via a helper flag or implement GARCH placeholder file in 06-06 only. Prefer: run() writes AN-301/302 artifacts now; 06-06 adds AN-303 files inside same run(). Delete stub when run() no longer raises NotImplementedError.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_analytics_a3.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - Two fit_hmm calls identical states; AN-304 skip test; synthetic 3-regime labels ordered by std.
  </acceptance_criteria>
  <done>HMM deterministic; gate honest.</done>
</task>
</tasks>
