---
phase: EPRA-06-m5-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/epra/analytics/_kit.py
  - src/epra/analytics/__main__.py
  - src/epra/analytics/__init__.py
  - tests/unit/test_analytics_kit.py
autonomous: true
requirements:
  - REQ-ANA-01
  - AN-703
  - RP-701
  - RP-702
must_haves:
  truths:
    - "load helpers query marts.* via connect(read_only=True) and raise with the SQL text when the result is empty"
    - "save_png uses FIGSIZE/DPI/SOURCE_NOTE from epra.report.style and stamps VERIFIED bottom-right"
    - "SSOT emitter schema is key,value,unit,tag,produced_by with tag VERIFIED; atomic write"
    - "chart test inspects Figure artists (size, source note, tag) — no image hash"
    - "python -m epra.analytics with missing warehouse exits 1 (actionable make warehouse); does not call dbt"
  artifacts:
    - src/epra/analytics/_kit.py
    - src/epra/analytics/__main__.py
    - tests/unit/test_analytics_kit.py
  key_links:
    - "T5.02–06 run() write only through kit"
    - "T5.07 un-stubs Makefile analyze: to this module"
  prohibitions:
    - "do not implement A1–A4 charts yet"
    - "do not invoke dbt"
    - "do not commit reports/analytics PNGs"
    - "do not read data/raw"
---

<objective>
Shared analytics kit: mart loaders, markdown/PNG/SSOT writers, CLI shell that fails loud without a warehouse.

Purpose: T5.01 — every later module must share RP-701/702 and AN-703.
Output: `_kit.py`, `__main__.py`, kit tests.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-06-m5-analytics/06-CONTEXT.md
</execution_context>

<context>
@src/epra/common/db.py
@src/epra/report/style.py
@src/epra/consumer/profile.py
@src/epra/warehouse/report.py
@docs/SPEC-04_analytics.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: _kit loaders + writers + CLI stub orchestration</name>
  <read_first>
    - src/epra/common/db.py
    - src/epra/report/style.py
    - src/epra/consumer/profile.py write_profile_outputs / _atomic_write_parquet
    - .planning/phases/EPRA-06-m5-analytics/06-PATTERNS.md
  </read_first>
  <files>src/epra/analytics/_kit.py, src/epra/analytics/__main__.py, src/epra/analytics/__init__.py, tests/unit/test_analytics_kit.py</files>
  <action>
    Implement load_price_hourly / load_price_daily (SQL constants; empty → RuntimeError including SQL). save_png(fig, path) applying RP-701/702. write_markdown(path, body). append/write ssot rows (schema D-03). ARTIFACT_NAMES frozenset of the 12 SPEC-04 §6 filenames. main(): if warehouse missing exit 1; else call descriptive.run, spread.run, weather.run, regimes.run in that order (still stubs — T5.02+ replace bodies). Tests: empty SQL raise; PNG figure inspection; SSOT roundtrip on tmp_settings; missing warehouse exit 1. matplotlib Agg.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_analytics_kit.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - Kit tests green; no A1 filenames committed under reports/analytics.
    - STYLE constants imported from epra.report.style, not duplicated hex.
  </acceptance_criteria>
  <done>Later plans can write artifacts without re-deriving chart chrome.</done>
</task>
</tasks>
