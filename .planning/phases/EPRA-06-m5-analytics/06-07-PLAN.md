---
phase: EPRA-06-m5-analytics
plan: 07
type: execute
wave: 5
depends_on:
  - 06-02
  - 06-03
  - 06-04
  - 06-06
files_modified:
  - Makefile
  - docs/BUILD_LOG.md
  - tests/unit/test_analytics_gates.py
  - tests/unit/test_stubs_fail_loudly.py
autonomous: true
requirements:
  - AN-701
  - AN-705
  - EN-050
must_haves:
  truths:
    - "make analyze is $(UV) run python -m epra.analytics and does not invoke dbt"
    - "from a wiped analytics out dir, run writes all 12 SPEC-04 §6 filenames (AN-701) on tmp_settings with a synthetic or fixture warehouse"
    - "two runs → identical SSOT analytics parquet values (AN-705)"
    - "AN-704 still holds on written md files"
    - "BUILD_LOG M5 entry appended; no invented real-market EUR headlines"
    - "M5 stub rows gone"
  artifacts:
    - Makefile
    - docs/BUILD_LOG.md
  key_links:
    - "verify-work checks ROADMAP SC and operator AN-304 on real data"
  prohibitions:
    - "do not commit fixture PNGs as Q2 evidence"
    - "do not invoke dbt from analyze"
    - "do not mix M6/M7"
    - "do not invent SSOT euros in BUILD_LOG"
---

<objective>
Wire `make analyze`, AN-70x tests, BUILD_LOG, M5 close-out.

Purpose: T5.07 operator interface.
Output: Makefile + gates + BUILD_LOG.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-06-m5-analytics/06-CONTEXT.md
</execution_context>

<context>
@Makefile
@docs/SPEC-04_analytics.md
@docs/BUILD_LOG.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Makefile, AN-701/705 tests, BUILD_LOG</name>
  <read_first>
    - Makefile analyze: stub
    - SPEC-04 §6 file list
    - 05-05 Makefile/CLI analog
  </read_first>
  <files>Makefile, tests/unit/test_analytics_gates.py, docs/BUILD_LOG.md, tests/unit/test_stubs_fail_loudly.py</files>
  <action>
    Un-stub analyze:. Test: tmp reports/analytics wiped then main() with monkeypatched loaders or a tiny duckdb mart — assert 12 files exist; second run SSOT values equal. Confirm no M5 stubs remain. BUILD_LOG: date, shipped, pytest snippet, open questions (AN-304 real warehouse is operator; do not commit fixture charts). Wipe only the 12 known filenames, not all of reports/.
  </action>
  <verify>
    <automated>make lint && uv run pytest -m "not live" -q</automated>
  </verify>
  <acceptance_criteria>
    - make analyze recipe is python -m epra.analytics
    - AN-701/705 tests green
    - BUILD_LOG M5 entry present
    - coverage ≥ 80%
  </acceptance_criteria>
  <done>M5 operator close-out ready for verify-work.</done>
</task>
</tasks>
