---
phase: EPRA-01-m0-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - REQ-ENG-01
user_setup: []
must_haves:
  - "`make setup && make lint && make test` all pass locally and in CI"
  - "Repository layout matches SPEC-07 §2: `src/epra/` package + dbt skeleton present"
  - "Every Makefile pipeline target exists; unimplemented stages exit non-zero with a milestone message"
status: complete
---

# Phase 1 (M0 Bootstrap) — Plan 01: Repo, tooling, CI, pipeline skeleton

> **Retroactive reconciliation record.** M0 was shipped directly via commit
> `c043933` ("chore: M0 bootstrap + breadth foundation") on 2026-07-19, before
> GSD phase-tracking was applied to this repo. This PLAN/SUMMARY/VERIFICATION set
> is created after the fact so `.planning/` state reflects reality (Phase 1
> complete). It does **not** represent a GSD-executor run — no code was written
> or changed while creating these records.

## Objective

Stand up the engineering foundation so a fresh clone can run quality gates, and
the Makefile pipeline skeleton fails loudly until domain milestones (M1–M7) ship.

## Scope delivered (see commit c043933)

- Repository layout per SPEC-07 §2: `src/epra/` package, `tests/`, `scripts/`,
  `config/`, `dbt/` skeleton (`dbt_project.yml`, `profiles.yml`, `models/staging`,
  `models/marts`, `seeds/dim_strategy.csv`).
- Tooling: `Makefile` targets, `.pre-commit-config.yaml`, `pyproject.toml` +
  `uv.lock`, CI at `.github/workflows/ci.yml`.
- Quality gates wired: ruff (lint), mypy (type), pytest + coverage (fail-under 80%).
- Governance/docs breadth: ADR-001/002, SPEC-01…08, LICENSE, README, LIMITATIONS.
- Pipeline skeleton: Makefile stage targets present; unimplemented domain stages
  exit non-zero with a milestone message.

## Requirements

- REQ-ENG-01 — engineering foundation and reproducible quality gates.
