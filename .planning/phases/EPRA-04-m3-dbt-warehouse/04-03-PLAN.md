---
phase: EPRA-04-m3-dbt-warehouse
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - dbt/models/marts/dim_calendar.sql
  - dbt/models/marts/dims.yml
autonomous: true
requirements:
  - REQ-DWH-01
  - DM-011
  - DM-012
  - DM-060
must_haves:
  truths:
    - "dim_calendar loads the calendar parquet (ING-110) via source('raw_calendar','calendar') and joins daily weather from stg_weather_graz_daily WITHOUT any timezone-conversion call — all local attributes come only from the calendar (DM-011)"
    - "dim_calendar exposes ts_utc, date_local, year_local, month_local, hour_local, dow_local, is_weekend, is_holiday_at, is_peak_hour, season, hdd_18, cdd_22 with season = 'winter' if month_local in (11,12,1,2,3) else 'summer' (SPEC-02 §4)"
    - "hdd_18 = greatest(0, 18 - tavg_c) and cdd_22 = greatest(0, tavg_c - 22), the daily value repeated across all 24 local hours of the day (SPEC-02 §4)"
    - "the DST fall-back/spring-forward local hours resolve via the ts_utc join: 2024-03-31 has 23 rows and 2024-10-27 has 25 rows in dim_calendar, each ts_utc appearing exactly once (DM-012 adjacency edge)"
    - "unique + not_null hold on dim_calendar.ts_utc and on dim_strategy.strategy_id (the committed seed's 6 strategies) so downstream FK/keys are trustworthy (DM-060)"
  artifacts:
    - dbt/models/marts/dim_calendar.sql
    - dbt/models/marts/dims.yml
  key_links:
    - "dim_calendar is joined by every fct_* mart in 04-04 on ts_utc to attach local attributes (DM-011) — it is the single source of local calendar truth"
    - "dim_strategy (already-seeded, 04-01) supplies the DM-063 FK target for fct_procurement_cost_monthly in 04-05"
  prohibitions:
    - "no timezone conversion function anywhere in dim_calendar — local attributes are read from the calendar parquet only, never recomputed (DM-011)"
    - "hdd_18/cdd_22 must be joined from daily weather and repeated across the day's 24 hours — do not recompute degree-days per hour from an hourly temperature (there is none; SPEC-02 §4)"
---

<objective>
Build `dim_calendar` (hour grain) — the single source of local calendar truth every mart joins on — by loading the ING-110 calendar parquet and joining daily weather for `season`, `hdd_18`, `cdd_22`, with zero timezone-conversion calls (DM-011). Add `dims.yml` pinning unique/not_null on `dim_calendar.ts_utc` and `dim_strategy.strategy_id`, plus the DM-012 DST edge-hour assertions.

Purpose: T3.03 — marts (04-04) cannot attach `is_peak_hour`/`year_local`/`season` etc. without this dimension; DM-011 forbids them from computing those themselves.
Output: `dim_calendar.sql`, `dims.yml`.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md
@docs/SPEC-02_data_model.md
@dbt/models/sources.yml
@dbt/seeds/dim_strategy.csv
@src/epra/ingest/calendar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: dim_calendar — calendar load + weather join, no TZ calls (DM-011)</name>
  <read_first>
    - docs/SPEC-02_data_model.md (§4 dim_calendar exact column list, season rule, hdd_18/cdd_22 formulas repeated over the day's 24 hours; DM-011 no-TZ doctrine; DM-012 DST edge hours)
    - src/epra/ingest/calendar.py (build_calendar — the parquet columns already computed: ts_utc, date_local, year_local, month_local, hour_local, dow_local, is_weekend, is_holiday_at, is_peak_hour — dim_calendar loads these, never recomputes)
    - dbt/models/sources.yml (source('raw_calendar','calendar'); stg_weather_graz_daily is a ref, not a source)
    - dbt/models/staging/stg_weather_graz_daily.sql (date_local, tavg_c — the weather join key + degree-day input)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-CONTEXT.md (Specific Ideas: season rule, price_peak NULL policy context)
  </read_first>
  <files>dbt/models/marts/dim_calendar.sql</files>
  <action>
    Create `dbt/models/marts/dim_calendar.sql` (materialized table via the marts config). Select the ING-110 calendar columns directly from `source('raw_calendar','calendar')` — `ts_utc, date_local, year_local, month_local, hour_local, dow_local, is_weekend, is_holiday_at, is_peak_hour` — WITHOUT any timezone-conversion function (they are pre-computed by `calendar.py`/`timeutil`; DM-011). Add `season` = `case when month_local in (11,12,1,2,3) then 'winter' else 'summer' end` (Austrian energy convention). Left-join `ref('stg_weather_graz_daily')` on `date_local` and add `hdd_18 = greatest(0, 18 - tavg_c)` and `cdd_22 = greatest(0, tavg_c - 22)` — the daily degree-day value repeats across all 24 local hours because the join is on the local day (SPEC-02 §4). Add an `Implements: DM-011, SPEC-02 §4` header comment and a comment documenting the season rule. Verify by building `dim_calendar` and confirming the DST edge hours (2024-03-31 = 23 rows, 2024-10-27 = 25 rows) and no duplicate ts_utc.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/dbt" && uv run dbt build --select dim_calendar</automated>
  </verify>
  <acceptance_criteria>
    - `dbt build --select dim_calendar` exits 0 on real local data.
    - Source assertion: `dim_calendar.sql` contains NO timezone-conversion call (grep for `AT TIME ZONE`/`timezone(`/`::timestamptz` finds nothing) — all local attributes come from the calendar source (DM-011).
    - Source assertion: `season` uses `month_local in (11,12,1,2,3)`; `hdd_18` is `greatest(0, 18 - tavg_c)`; `cdd_22` is `greatest(0, tavg_c - 22)` (SPEC-02 §4).
    - Behavior (DST adjacency, DM-012): a spot query shows `count(*)` on `date_local = 2024-03-31` is 23 and on `date_local = 2024-10-27` is 25 (verified here or by the DM-065 singular test in 04-06).
  </acceptance_criteria>
  <done>dim_calendar builds with the full §4 column set, season + degree-days, and correct DST hour counts, using zero TZ calls.</done>
</task>

<task type="auto">
  <name>Task 2: dims.yml — unique/not_null on dim_calendar + dim_strategy (DM-060)</name>
  <read_first>
    - docs/SPEC-02_data_model.md (DM-060 keys; §4 dim_strategy seed 6 rows S1/S2/S3/S4_30/S4_50/S4_70)
    - dbt/seeds/dim_strategy.csv (the 6 strategy_id values that must be unique/not_null — DM-063 FK target)
    - dbt/models/marts/dim_calendar.sql (Task 1 — the ts_utc grain key)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md (YAML description spec-ID convention)
  </read_first>
  <files>dbt/models/marts/dims.yml</files>
  <action>
    Create `dbt/models/marts/dims.yml` (version 2, a separate file from the fact YAMLs in 04-04/04-05 to avoid cross-plan file conflicts). Declare `dim_calendar` with a `description:` citing `SPEC-02 §4 / DM-011` and DM-060 tests `unique` + `not_null` on `ts_utc`. Declare the `dim_strategy` seed with a `description:` citing `SPEC-02 §4` and DM-060 tests `unique` + `not_null` on `strategy_id` (this makes it a trustworthy FK target for DM-063 in 04-05). Verify by building the dims selection so both generic tests run.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/dbt" && uv run dbt build --select dim_calendar dim_strategy</automated>
  </verify>
  <acceptance_criteria>
    - `dbt build --select dim_calendar dim_strategy` exits 0 with unique + not_null green on `dim_calendar.ts_utc` and `dim_strategy.strategy_id`.
    - `dims.yml` enumerates both dims with DM-060 tests and spec-ID descriptions; it is a distinct file (not shared with the 04-04/04-05 fact YAMLs).
  </acceptance_criteria>
  <done>Unique/not_null pinned on both dimension keys; dim_strategy is a validated FK target for downstream marts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calendar parquet + weather staging → dim_calendar | pre-computed local attributes cross in; the risk is a model recomputing them and diverging from timeutil |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | local-attribute derivation | high | mitigate | DM-011 enforced: dim_calendar reads local attributes only from the calendar source (never a TZ call); the DST edge-hour counts (DM-012, verified in 04-06) catch any accidental TZ recomputation as a red test |
| T-04-06 | Tampering | weather degree-day join | low | mitigate | hdd_18/cdd_22 joined on date_local from a single staging source; left-join keeps hours even when a day lacks weather (NULL degree-days rather than dropped hours) |
| T-04-SC | Tampering | dbt/duckdb packages | low | accept | No install this plan; core packages pinned in uv.lock (RESEARCH §Package Legitimacy Audit) |

Auth (V2), Session (V3), Access control (V4), Cryptography (V6): N/A — local single-operator warehouse.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `dbt/models/marts/dim_calendar.sql` — hour-grain calendar dimension (§4): calendar load + season rule + weather degree-days, zero TZ calls (DM-011).
- `dbt/models/marts/dims.yml` — DM-060 unique/not_null tests on `dim_calendar.ts_utc` and `dim_strategy.strategy_id` (the DM-063 FK target).
- (dim_strategy is the already-committed seed from 04-01/skeleton — validated here, not recreated.)
</artifacts_produced>

<verification>
- `cd dbt && dbt build --select dim_calendar dim_strategy` green on real local data.
- Grep confirms zero timezone-conversion calls in `dim_calendar.sql` (DM-011).
- Spot check / DM-065 (04-06): 2024-03-31 = 23 rows, 2024-10-27 = 25 rows.
</verification>

<success_criteria>
`dim_calendar` is the single source of local calendar truth — full §4 column set, correct season/degree-days, correct DST hour counts — built with zero TZ calls (DM-011); both dimension keys are unique/not_null (DM-060), making `dim_strategy` a valid DM-063 FK target.
</success_criteria>

<output>
Create `.planning/phases/EPRA-04-m3-dbt-warehouse/04-03-SUMMARY.md` when done.
</output>
