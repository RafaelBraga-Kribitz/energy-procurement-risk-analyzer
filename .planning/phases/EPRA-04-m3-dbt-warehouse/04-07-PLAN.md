---
phase: EPRA-04-m3-dbt-warehouse
plan: 07
type: execute
wave: 6
depends_on:
  - 04-06
files_modified:
  - src/epra/warehouse/__init__.py
  - src/epra/warehouse/report.py
  - tests/unit/test_warehouse_report.py
  - Makefile
autonomous: true
requirements:
  - REQ-DWH-01
  - DM-060
  - D-02
must_haves:
  truths:
    - "the build-report writer queries the built warehouse read-only via epra.common.db.connect(settings, read_only=True) and renders a human-readable reports/warehouse/dbt_build_<date>.md (D-02, mirroring reports/ingestion/validation_<date>.md)"
    - "the report includes the key sanity numbers: per-year fct_price_hourly row counts, monthly-mart month coverage, and the 2022-08 reconciliation delta (fct_price_monthly base minus mean-of-hourly), reusing the GateResult/ValidationReport render_markdown pattern"
    - "the report flags fct_consumer_load_hourly and fct_procurement_cost_monthly as 'stand-in (M4/M6 pending)' (D-05/D-06)"
    - "make transform runs cd dbt && dbt build (un-stubbed from the loud-fail placeholder) keeping all: and refresh: wiring intact; a warehouse target runs the build then writes the report"
    - "the report renderer is unit-tested (render + write path) network-free without needing a live warehouse (dataclass render tested on constructed inputs)"
  artifacts:
    - src/epra/warehouse/__init__.py
    - src/epra/warehouse/report.py
    - tests/unit/test_warehouse_report.py
    - Makefile
  key_links:
    - "report.py reuses the src/epra/ingest/validate.py GateResult/ValidationReport render_markdown + _write_report shape, swapping reports/ingestion -> reports/warehouse and validation_ -> dbt_build_"
    - "Makefile transform target feeds all:/refresh:; the report reads the warehouse epra.common.db populates via dbt build"
  prohibitions:
    - "the report writer must NOT recompute mart values in Python — it only READS the warehouse dbt built (db.connect read_only), mirroring the ingestion report precedent"
    - "do NOT commit data/warehouse/epra.duckdb — it stays gitignored (DM-001); only the markdown report under reports/warehouse/ is committed (D-02)"
---

<objective>
Deliver the D-02 human-readable build report and the operator interface. `src/epra/warehouse/report.py` reads the built warehouse (read-only) and renders `reports/warehouse/dbt_build_<date>.md` with per-year row counts, month coverage, and the 2022-08 reconciliation delta — reusing the `GateResult`/`ValidationReport` pattern — flagging the two stand-in marts. Un-stub the Makefile `transform:` target to `cd dbt && dbt build` and add a `warehouse` target that builds then reports.

Purpose: D-02 (build report at the real-data checkpoint) + the "Makefile as canonical operator interface" close; feeds the 04-08 phase-close.
Output: `src/epra/warehouse/{__init__.py,report.py}`, `tests/unit/test_warehouse_report.py`, un-stubbed `Makefile`.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md
@docs/SPEC-02_data_model.md
@src/epra/ingest/validate.py
@src/epra/common/db.py
@Makefile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: warehouse.report — build-report writer (GateResult/ValidationReport reuse)</name>
  <read_first>
    - src/epra/ingest/validate.py (GateResult/ValidationReport dataclasses lines 66-131, render_markdown, _write_report lines 887-895, main() CLI shape lines 940-965 — the exact structural reuse target per PATTERNS.md)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md (Build-report writer section — rename to BuildReport/ModelResult, evidence DataFrame rendering, settings.paths.reports usage)
    - src/epra/common/db.py (connect(settings, read_only=True) + warehouse_path — the read-only warehouse access)
    - docs/SPEC-02_data_model.md (DM-062 per-year counts, DM-064 2022-08 reconciliation — the sanity numbers to render)
    - src/epra/common/config.py (Settings.paths.reports field — reuse, do not invent a new path)
  </read_first>
  <files>src/epra/warehouse/__init__.py, src/epra/warehouse/report.py, tests/unit/test_warehouse_report.py</files>
  <behavior>
    - A BuildReport (list of result rows) renders markdown with an overall header and per-item sections, reusing the ValidationReport.render_markdown shape (status + summary + optional evidence DataFrame via to_string(index=False)).
    - build_report(settings) opens db.connect(read_only=True), pulls per-year fct_price_hourly row counts, monthly-mart month coverage, and the 2022-08 reconciliation delta, and returns a BuildReport including a row flagging fct_consumer_load_hourly + fct_procurement_cost_monthly as 'stand-in (M4/M6 pending)'.
    - _write_report(report, settings) writes reports/warehouse/dbt_build_<date>.md under settings.paths.reports, creating the warehouse/ subdir (mirrors validate._write_report).
    - render_markdown is pure/deterministic given constructed result rows (unit-testable without a live warehouse).
    - main(argv) -> int loads settings, builds+writes the report, returns 0 (or 1 on a read failure), mirroring validate.main().
  </behavior>
  <action>
    Create the `src/epra/warehouse/` package (`__init__.py`) and `report.py` reusing the `validate.py` report framework: define a `ModelBuildResult`/`BuildReport` pair with the same `render_markdown` aggregation and an `evidence: pd.DataFrame | None` field rendered via `to_string(index=False)` (PATTERNS.md). Add `build_report(settings)` that opens `epra.common.db.connect(settings, read_only=True)` and queries the marts for the D-02 sanity numbers — per-year `fct_price_hourly` row counts, monthly-mart month coverage, the 2022-08 `fct_price_monthly.price_base_eur_mwh` minus mean-of-hourly delta — and adds a row flagging the two stand-in marts. Add `_write_report(report, settings)` writing `reports/warehouse/dbt_build_<date>.md` under `settings.paths.reports` (copy-adapt validate's `_write_report`: swap `ingestion`->`warehouse`, `validation_`->`dbt_build_`). Add `main(argv: Sequence[str] | None = None) -> int` with the validate.main logging/exit-code shape; `Implements: D-02, D-05, D-06 (stand-in flags), DM-060..066` docstring. Do NOT recompute mart values in Python — READ only. In `tests/unit/test_warehouse_report.py` unit-test `render_markdown` on constructed `ModelBuildResult`s (network-free, no live warehouse): assert PASS/FAIL rendering, the 2022-08 delta row, and the stand-in flag text appear.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run pytest tests/unit/test_warehouse_report.py -m "not live" -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_warehouse_report.py -m "not live" -x` exits 0 (render + write-path tested on constructed inputs, no live warehouse).
    - Source assertion: `report.py` uses `epra.common.db.connect(..., read_only=True)` and `settings.paths.reports` (grep confirms both; no raw `duckdb.connect`, no new reports path field).
    - Source assertion: the renderer emits a 'stand-in (M4/M6 pending)' flag for the two future marts (D-05/D-06).
  </acceptance_criteria>
  <done>report.py reads the warehouse read-only and renders reports/warehouse/dbt_build_<date>.md reusing the ingestion report framework; unit-tested network-free.</done>
</task>

<task type="auto">
  <name>Task 2: Makefile transform un-stub + warehouse target</name>
  <read_first>
    - Makefile (the transform: loud-fail stub lines 42-43; the all:/refresh: wiring lines 63-65; the sibling live targets ingest:/validate-ingest: for the ## doc-comment + $(UV) run convention)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md (Claude's Discretion / Summary: un-stub transform to cd dbt && dbt build; do not invent a new name — already wired into all:/refresh:)
    - src/epra/warehouse/report.py (Task 1 — the module the warehouse target invokes for the build report)
  </read_first>
  <files>Makefile</files>
  <action>
    Replace the `transform:` loud-fail stub body with `cd dbt && $(UV) run dbt build`, keeping the `##`-doc-comment convention and leaving `all:`/`refresh:` untouched (transform is already a dependency there). Add a `warehouse:` target (operator interface, Claude's Discretion) that runs `transform` then `$(UV) run python -m epra.warehouse.report` to emit the D-02 build report (add `warehouse` to `.PHONY`). Do NOT touch the other still-stubbed M4-M7 targets. Verify the transform target actually runs dbt build on real local data (green) and the warehouse target additionally writes the report.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && make warehouse && test -n "$(ls reports/warehouse/dbt_build_*.md 2>/dev/null)" && echo REPORT_WRITTEN</automated>
  </verify>
  <acceptance_criteria>
    - `make transform` runs `cd dbt && dbt build` and exits 0 on real local data (no longer the loud-fail stub).
    - `make warehouse` builds then writes a `reports/warehouse/dbt_build_<date>.md` (file exists after the run).
    - `all:`/`refresh:` still list `transform` (grep confirms the wiring is intact); the M4-M7 stubs are unchanged.
  </acceptance_criteria>
  <done>make transform builds the warehouse; make warehouse additionally emits the D-02 build report; downstream Makefile wiring intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built warehouse (epra.duckdb) → report reader | read-only query surface; risk is accidental write or committing the DB file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-14 | Tampering | warehouse access from report | low | mitigate | Report opens the warehouse with `read_only=True` (db.connect) — cannot mutate marts; reuses the sanctioned shared helper, no raw duckdb.connect |
| T-04-15 | Information disclosure | epra.duckdb commit | low | mitigate | DB stays gitignored (DM-001); only the markdown report under reports/warehouse/ is committed (D-02); git-clean verified at 04-08 |
| T-04-SC | Tampering | dbt/duckdb/pandas packages | low | accept | No new install; pandas/duckdb pinned in uv.lock (RESEARCH §Package Legitimacy Audit) |

Auth (V2), Session (V3), Access control (V4), Cryptography (V6): N/A — local single-operator warehouse.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `src/epra/warehouse/__init__.py` — new package.
- `src/epra/warehouse/report.py` — D-02 build-report writer (GateResult/ValidationReport reuse; db.connect read_only; stand-in flags).
- `tests/unit/test_warehouse_report.py` — network-free render/write-path unit test.
- `Makefile` — `transform:` un-stubbed to `cd dbt && dbt build`; new `warehouse:` target (build + report).
- (output at runtime, committed at 04-08) `reports/warehouse/dbt_build_<date>.md`.
</artifacts_produced>

<verification>
- `uv run pytest tests/unit/test_warehouse_report.py -m "not live"` green (render + write path).
- `make transform` green on real data; `make warehouse` writes `reports/warehouse/dbt_build_<date>.md`.
- `uv run ruff check src/epra/warehouse tests/unit/test_warehouse_report.py` and `uv run mypy` clean.
</verification>

<success_criteria>
The operator interface is live (`make transform` builds the warehouse; `make warehouse` also emits the D-02 report), the build report reads the warehouse read-only and surfaces per-year counts + month coverage + the 2022-08 reconciliation delta and flags the two stand-in marts, and the renderer is unit-tested network-free — with the DuckDB file kept gitignored (DM-001).
</success_criteria>

<output>
Create `.planning/phases/EPRA-04-m3-dbt-warehouse/04-07-SUMMARY.md` when done.
</output>
