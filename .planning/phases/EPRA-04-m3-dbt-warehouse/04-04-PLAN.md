---
phase: EPRA-04-m3-dbt-warehouse
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - dbt/models/marts/fct_price_hourly.sql
  - dbt/models/marts/fct_price_daily.sql
  - dbt/models/marts/fct_price_monthly.sql
  - dbt/models/marts/fct_generation_monthly.sql
  - dbt/models/marts/facts_price.yml
  - docs/ADR/ADR-011_holiday-aware-peak.md
  - LIMITATIONS.md
autonomous: true
requirements:
  - REQ-DWH-01
  - DM-050
  - DM-061
  - SG-05
  - SG-14
must_haves:
  truths:
    - "fct_price_hourly (hour grain) has exactly the SG-05 enumeration: ts_utc, price_at_eur_mwh, price_delu_eur_mwh, spread_at_delu_eur_mwh (= at - delu), load_at_mw, is_negative_price (at < 0), plus ALL dim_calendar attributes joined on ts_utc (date_local, year_local, month_local, hour_local, dow_local, is_weekend, is_holiday_at, is_peak_hour, season, hdd_18, cdd_22)"
    - "price_peak_eur_mwh is NULL on local days with no peak hours (holidays) because the peak mean is taken over is_peak_hour = true rows only (holiday-aware is_peak_hour, ADR-011/SG-14) — the empty-input edge"
    - "fct_price_daily and fct_price_monthly are aggregations of fct_price_hourly grouped by dim_calendar local day/month, so each monthly base equals the mean of that month's hourly prices (the DM-064 reconciliation contract, verified in 04-06)"
    - "fct_generation_monthly (month × psr_type) computes gen_gwh = sum(gen_mw)/1000 per hour count and share_of_total within each month, with strategy-free psr grain (SPEC-02 §5)"
    - "the price/generation marts build (dbt build --select fct_price_hourly fct_price_daily fct_price_monthly fct_generation_monthly exits 0) on real local data with DM-061 accepted ranges green"
  artifacts:
    - dbt/models/marts/fct_price_hourly.sql
    - dbt/models/marts/fct_price_daily.sql
    - dbt/models/marts/fct_price_monthly.sql
    - dbt/models/marts/fct_generation_monthly.sql
    - dbt/models/marts/facts_price.yml
    - docs/ADR/ADR-011_holiday-aware-peak.md
    - LIMITATIONS.md
  key_links:
    - "every price mart joins dim_calendar (04-03) on ts_utc for local attributes and peak flags (DM-011) — no mart recomputes them"
    - "fct_price_monthly joins stg_oespi_monthly on year_local/month_local to attach oespi_base, oespi_peak (SPEC-02 §5)"
  prohibitions:
    - "no mart calls a timezone conversion function — local day/month/peak come only from the dim_calendar join (DM-011)"
    - "M3 does NOT write exports/ or any SPEC-02 §7 CSV — export_marts.py / make export is M7/Phase 8, out of this phase"
    - "price_peak uses the ONE holiday-aware is_peak_hour definition everywhere (ADR-011/SG-14) — never a separate Mon-Fri-08-20 peak that ignores holidays"
---

<objective>
Build the four price/generation marts off real local raw data: `fct_price_hourly` (the SG-05 full enumeration, dim_calendar-joined), `fct_price_daily` and `fct_price_monthly` (aggregations, holiday-aware peak with NULL-on-no-peak-days, ÖSPI join), and `fct_generation_monthly` (gen_gwh + share_of_total). Record the SG-14 holiday-aware peak decision in ADR-011 and the ÖSPI-peak-convention caveat in LIMITATIONS.md §2. Add `facts_price.yml` with DM-060/DM-061 tests for these marts.

Purpose: T3.04 (price/gen half) — these are four of the six marts the M3 exit-gate schema contract (04-06) diff-checks.
Output: 4 `fct_*.sql`, `facts_price.yml`, `docs/ADR/ADR-011_holiday-aware-peak.md`, LIMITATIONS.md §2 entry.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md
@docs/SPEC-02_data_model.md
@docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md
@dbt/models/marts/dim_calendar.sql
@docs/ADR/ADR-006_validation-gate-scope-local-year.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: fct_price_hourly — SG-05 full enumeration, dim_calendar-joined</name>
  <read_first>
    - docs/SPEC-02_data_model.md (§5 fct_price_hourly row: ts_utc, price_at_eur_mwh, price_delu_eur_mwh, spread_at_delu_eur_mwh, load_at_mw, is_negative_price + all dim_calendar attributes; DM-050 no NULL keys)
    - docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md (SG-05 frozen enumeration = ING-110 calendar list + season, hdd_18, cdd_22 — the exact dim_calendar attribute set)
    - dbt/models/marts/dim_calendar.sql (the joinable attribute columns on ts_utc)
    - dbt/models/staging/stg_prices_at_hourly.sql, stg_prices_delu_hourly.sql, stg_load_at_hourly.sql (the ref inputs)
  </read_first>
  <files>dbt/models/marts/fct_price_hourly.sql</files>
  <action>
    Create `dbt/models/marts/fct_price_hourly.sql` (table). Join `ref('stg_prices_at_hourly')`, `ref('stg_prices_delu_hourly')`, `ref('stg_load_at_hourly')`, and `ref('dim_calendar')` all on `ts_utc`. Emit exactly the SG-05 enumeration: `ts_utc`, `price_at_eur_mwh` (from AT hourly), `price_delu_eur_mwh` (from DE-LU hourly), `spread_at_delu_eur_mwh = price_at_eur_mwh - price_delu_eur_mwh`, `load_at_mw`, `is_negative_price = price_at_eur_mwh < 0`, plus ALL dim_calendar attributes (`date_local, year_local, month_local, hour_local, dow_local, is_weekend, is_holiday_at, is_peak_hour, season, hdd_18, cdd_22`). Use dim_calendar as the spine (left join prices/load) so every calendar hour is present and `ts_utc` is never NULL (DM-050). No timezone call (DM-011). Header comment `Implements: SPEC-02 §5 fct_price_hourly, SG-05, DM-050`. Verify by building it on real local data.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/dbt" && uv run dbt build --select fct_price_hourly</automated>
  </verify>
  <acceptance_criteria>
    - `dbt build --select fct_price_hourly` exits 0 on real local data.
    - Source assertion: the select list is exactly the SG-05 enumeration (grep confirms `spread_at_delu_eur_mwh`, `is_negative_price`, and the 11 dim_calendar attribute names) and contains no timezone-conversion call (DM-011).
    - The model joins `ref('dim_calendar')` on ts_utc as the spine (no independent local-attribute derivation).
  </acceptance_criteria>
  <done>fct_price_hourly emits the frozen SG-05 columns, spined on dim_calendar, built on real data.</done>
</task>

<task type="auto">
  <name>Task 2: fct_price_daily + fct_price_monthly (holiday-aware peak, ÖSPI join) + ADR-011 + LIMITATIONS §2</name>
  <read_first>
    - docs/SPEC-02_data_model.md (§5 fct_price_daily and fct_price_monthly exact columns; price_peak NULL on days without peak hours; DM-050 no-gap months)
    - docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md (SG-14 one holiday-aware is_peak_hour everywhere + ÖSPI-peak note to LIMITATIONS)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-CONTEXT.md (Specific Ideas: price_peak_eur_mwh NULL on no-peak days; D-08 ADR-011 scope)
    - dbt/models/marts/fct_price_hourly.sql (Task 1 — the aggregation input, carries is_peak_hour + degree-days)
    - dbt/models/staging/stg_oespi_monthly.sql (month_local, oespi_base, oespi_peak — the monthly join)
    - docs/ADR/ADR-006_validation-gate-scope-local-year.md (ADR heading template)
  </read_first>
  <files>dbt/models/marts/fct_price_daily.sql, dbt/models/marts/fct_price_monthly.sql, docs/ADR/ADR-011_holiday-aware-peak.md, LIMITATIONS.md</files>
  <action>
    Create `fct_price_daily` (local day grain) from `ref('fct_price_hourly')` grouped by `date_local`: `price_base_eur_mwh` = mean of all 24 hours; `price_peak_eur_mwh` = mean of hours where `is_peak_hour` is true, which is NULL on days with no peak hours (holidays) because the filtered aggregate has no rows (SG-14/ADR-011 — the empty-input edge); `price_min`, `price_max`, `price_std`, `n_negative_hours` = count where is_negative_price; plus `tavg_c`, `hdd_18`, `cdd_22` from the day. Create `fct_price_monthly` (local month grain) grouped by `year_local, month_local`: `price_base_eur_mwh`, `price_peak_eur_mwh` (peak-hours mean), `price_offpeak_eur_mwh` (non-peak-hours mean), `n_negative_hours`, and `oespi_base`, `oespi_peak` left-joined from `ref('stg_oespi_monthly')` on the month. Both marts are gap-free because they aggregate the contiguous calendar spine (DM-050). Use the ONE holiday-aware `is_peak_hour` from dim_calendar everywhere (never a separate Mon-Fri-08-20 rule). Author `docs/ADR/ADR-011_holiday-aware-peak.md` (Context/Decision/Consequences/Spec deviations): Decision = single `is_peak_hour` (ING-110, holiday-aware) drives `price_peak_eur_mwh` and downstream anchors; Consequences = ÖSPI's own peak convention may treat holidays differently, anchor ratios absorb the level offset. Add a LIMITATIONS.md §2 entry recording that ÖSPI-peak vs internal-peak convention difference. Verify by building both marts.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/dbt" && uv run dbt build --select fct_price_daily fct_price_monthly</automated>
  </verify>
  <acceptance_criteria>
    - `dbt build --select fct_price_daily fct_price_monthly` exits 0 on real local data.
    - Source assertion: `price_peak_eur_mwh` aggregates over `is_peak_hour` rows only (grep confirms the peak mean is filtered by `is_peak_hour`), yielding NULL on no-peak days (SG-14/ADR-011).
    - `fct_price_monthly` left-joins `stg_oespi_monthly` for `oespi_base`, `oespi_peak`; no timezone call in either mart (DM-011).
    - `docs/ADR/ADR-011_holiday-aware-peak.md` exists with the 4 ADR sections; LIMITATIONS.md has a §2 entry on the ÖSPI-peak convention (SG-14).
  </acceptance_criteria>
  <done>Daily/monthly price marts built with holiday-aware peak (NULL on no-peak days) and ÖSPI join; ADR-011 + LIMITATIONS §2 recorded.</done>
</task>

<task type="auto">
  <name>Task 3: fct_generation_monthly + facts_price.yml (DM-060/DM-061 tests)</name>
  <read_first>
    - docs/SPEC-02_data_model.md (§5 fct_generation_monthly: year_local, month_local, psr_type, psr_name, gen_gwh = sum(gen_mw)/1000 per hour count, share_of_total; DM-060 keys; DM-061 accepted ranges)
    - dbt/models/staging/stg_gen_at_hourly.sql (ts_utc, psr_type, psr_name, gen_mw — the input)
    - dbt/models/marts/dim_calendar.sql (year_local, month_local for the local-month grouping)
    - dbt/macros/test_accepted_range.sql (04-01 — the DM-061 generic test macro to reference)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md (YAML description spec-ID convention)
  </read_first>
  <files>dbt/models/marts/fct_generation_monthly.sql, dbt/models/marts/facts_price.yml</files>
  <action>
    Create `fct_generation_monthly` (local month × psr_type) from `ref('stg_gen_at_hourly')` joined to `ref('dim_calendar')` on ts_utc for `year_local, month_local`: `gen_gwh` = sum(gen_mw)/1000 aggregated to the month (per SPEC-02 §5 "sum(gen_mw)/1000 per hour count"), and `share_of_total` = each psr_type's gen_gwh divided by the month's total gen_gwh (window over year_local, month_local). Create `dbt/models/marts/facts_price.yml` (a distinct file from dims.yml / facts_future.yml) declaring `fct_price_hourly`, `fct_price_daily`, `fct_price_monthly`, `fct_generation_monthly` with `description:` fields citing their §5 contract, and DM-060/DM-061 tests: `unique`+`not_null` on each grain key (`ts_utc` for hourly; `date_local` for daily; `[year_local, month_local]` for monthly price; `[year_local, month_local, psr_type]` for generation); `accepted_range` on `fct_price_hourly.price_at_eur_mwh` {min_value: -500, max_value: 5000} and `load_at_mw` {min_value: 3000, max_value: 13000}; `accepted_range` on `fct_price_daily.tavg_c` {min_value: -30, max_value: 42}; and an `oespi_base` positivity check (accepted_range min_value just above 0 or a not_null+`> 0`) on `fct_price_monthly` (DM-061). Verify by building the four price/gen marts with their tests.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/dbt" && uv run dbt build --select fct_price_hourly fct_price_daily fct_price_monthly fct_generation_monthly</automated>
  </verify>
  <acceptance_criteria>
    - `dbt build --select` of the four marts exits 0 with all DM-060/DM-061 generic tests green on real local data.
    - Source assertion: `facts_price.yml` declares `accepted_range` for `price_at_eur_mwh` [-500, 5000], `load_at_mw` [3000, 13000], `tavg_c` [-30, 42], and an `oespi_base > 0` check (DM-061 exact ranges).
    - `fct_generation_monthly` computes `gen_gwh` from sum(gen_mw)/1000 and `share_of_total` summing to ~1.0 within each month.
  </acceptance_criteria>
  <done>fct_generation_monthly built; facts_price.yml pins DM-060 keys + DM-061 ranges green on the four price/gen marts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staging views + dim_calendar → price/gen marts | aggregation drift and peak-definition ambiguity are the risks crossing into published mart numbers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | peak-hour definition | medium | mitigate | One holiday-aware `is_peak_hour` from dim_calendar drives price_peak everywhere (SG-14/ADR-011); price_peak is NULL (not 0, not a wrong-average) on no-peak days — the empty-input contract |
| T-04-08 | Tampering | monthly aggregation drift | medium | mitigate | fct_price_monthly base is the mean of fct_price_hourly for the month; the DM-064 2022-08 reconciliation singular test (04-06) fails on any aggregation drift beyond 0.01 |
| T-04-SC | Tampering | dbt/duckdb packages | low | accept | No install this plan; core packages pinned in uv.lock (RESEARCH §Package Legitimacy Audit) |

Auth (V2), Session (V3), Access control (V4), Cryptography (V6): N/A — local single-operator warehouse.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `dbt/models/marts/fct_price_hourly.sql` — SG-05 enumeration, dim_calendar-spined (§5, DM-050).
- `dbt/models/marts/fct_price_daily.sql` — daily aggregates, holiday-aware `price_peak_eur_mwh` NULL on no-peak days (SG-14).
- `dbt/models/marts/fct_price_monthly.sql` — monthly base/peak/offpeak + ÖSPI join (§5).
- `dbt/models/marts/fct_generation_monthly.sql` — gen_gwh + share_of_total (§5).
- `dbt/models/marts/facts_price.yml` — DM-060 keys + DM-061 accepted ranges for the four price/gen marts.
- `docs/ADR/ADR-011_holiday-aware-peak.md` — SG-14 holiday-aware peak decision + ÖSPI-convention caveat.
- `LIMITATIONS.md` §2 entry — ÖSPI-peak vs internal-peak convention note.
</artifacts_produced>

<verification>
- `cd dbt && dbt build --select fct_price_hourly fct_price_daily fct_price_monthly fct_generation_monthly` green on real local data (models + DM-060/DM-061 tests).
- Grep confirms zero timezone calls (DM-011) and holiday-aware `is_peak_hour` peak filtering.
- ADR-011 + LIMITATIONS §2 present.
</verification>

<success_criteria>
The four price/generation marts build green on real local data with the frozen SG-05 enumeration, holiday-aware peak (NULL on no-peak days), ÖSPI join, and DM-060/DM-061 tests passing — using zero timezone calls (DM-011); ADR-011 and LIMITATIONS §2 record the SG-14 decision. No exports/ is written (M7 scope).
</success_criteria>

<output>
Create `.planning/phases/EPRA-04-m3-dbt-warehouse/04-04-SUMMARY.md` when done.
</output>
