---
phase: EPRA-04-m3-dbt-warehouse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dbt/models/sources.yml
  - dbt/macros/generate_schema_name.sql
  - dbt/macros/month_spine.sql
  - dbt/macros/test_accepted_range.sql
  - docs/ADR/ADR-009_generate-schema-name-macro.md
autonomous: true
requirements:
  - REQ-DWH-01
  - DM-003
  - DM-004
  - SG-13
must_haves:
  truths:
    - "dbt seed builds dim_strategy into a DuckDB schema literally named marts (not main_marts), proving the SG-13 generate_schema_name override omits default_schema (DM-003, ADR-009)"
    - "sources.yml exposes every raw dataset exactly once via read_parquet/read_csv globs prefixed ../data/ (dbt runs from dbt/), so DuckDB reads files as external relations with no copy step (DM-004)"
    - "dbt parse succeeds with sources.yml + all three macros present, proving the sources block and hand-rolled macros are structurally valid dbt with zero dbt_utils dependency"
    - "month_spine(min, max) and the accepted_range generic test macro are available to later marts/tests (DM-050/DM-061 helpers) built on DuckDB generate_series, no packages.yml"
  artifacts:
    - dbt/models/sources.yml
    - dbt/macros/generate_schema_name.sql
    - dbt/macros/month_spine.sql
    - dbt/macros/test_accepted_range.sql
    - docs/ADR/ADR-009_generate-schema-name-macro.md
  key_links:
    - "generate_schema_name returns custom_schema_name trimmed when set, else target.schema — dbt_project.yml +schema staging/marts configs then land models in literal schemas"
    - "every downstream stg_* / fct_* model reads only via source('raw', ...) / source('raw_*', ...) defined here (DM-004 single-glob-per-source)"
  prohibitions:
    - "no source or model may read data/ with a bare (non ../) relative path — dbt cwd is dbt/, a wrong relative glob silently returns zero rows (Pitfall 1)"
    - "do NOT add dbt_utils or any packages.yml / dbt deps step — month_spine and accepted_range are hand-rolled (lean-repo, ADR-001)"
---

<objective>
Lay the dbt plumbing every later model depends on: the SG-13 `generate_schema_name` override (so DuckDB schemas are literally `staging`/`marts`, DM-003), the DM-004 `sources.yml` exposing each raw dataset exactly once via `read_parquet`/`read_csv` globs, and the two hand-rolled helper macros (`month_spine`, `accepted_range`) that later marts/tests use with zero `dbt_utils` dependency. ADR-009 records the schema-macro tradeoff.

Purpose: T3.01 — without literal schema names and readable external sources, no staging or mart model can build. This is the foundation wave.
Output: `sources.yml`, `generate_schema_name.sql`, `month_spine.sql`, `test_accepted_range.sql`, `docs/ADR/ADR-009_generate-schema-name-macro.md`.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-CONTEXT.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md
@docs/SPEC-02_data_model.md
@dbt/dbt_project.yml
@dbt/profiles.yml
@docs/ADR/ADR-006_validation-gate-scope-local-year.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: generate_schema_name override macro (SG-13) + ADR-009</name>
  <read_first>
    - dbt/dbt_project.yml (the +schema: staging / +schema: marts configs and seeds dim_strategy +schema: marts — these are the custom_schema_name values the macro receives)
    - dbt/profiles.yml (single dev target, DuckDB local file — the reason omitting default_schema is safe)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md (Pattern 2 for the exact macro shape; Pitfall 2 for the accepted tradeoff)
    - docs/ADR/ADR-006_validation-gate-scope-local-year.md (heading template: Context / Decision / Consequences / Spec deviations)
    - docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md (SG-13 row — the resolution being adopted)
  </read_first>
  <files>dbt/macros/generate_schema_name.sql, docs/ADR/ADR-009_generate-schema-name-macro.md</files>
  <action>
    Create `dbt/macros/generate_schema_name.sql` overriding dbt's built-in `generate_schema_name(custom_schema_name, node)` to return the trimmed `custom_schema_name` when a custom schema is set and `target.schema` otherwise — deliberately OMITTING the default-schema prefix dbt normally prepends (SG-13 / DM-003), so `dim_strategy` and later marts land in a schema literally named `marts` and staging in `staging`. Add a macro-header comment citing `DM-003, SG-13, ADR-009` and noting this is the pattern dbt's own docs label unsafe for shared warehouses, accepted here because `profiles.yml` defines exactly one local single-operator target (per D-08). Author `docs/ADR/ADR-009_generate-schema-name-macro.md` using the ADR-006 heading structure (Context / Decision / Consequences / Spec deviations): Decision = adopt the literal-schema override; Consequences = record that generalizing this macro to any future multi-target/multi-developer warehouse must revisit the tradeoff (Pitfall 2). Verify by running `dbt seed --full-refresh` (builds the existing `dim_strategy` seed) and querying `information_schema.schemata` for a schema named `marts`.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/dbt" && uv run dbt seed --full-refresh && uv run python -c "import duckdb; con=duckdb.connect('../data/warehouse/epra.duckdb', read_only=True); schemas=[r[0] for r in con.execute('select schema_name from information_schema.schemata').fetchall()]; assert 'marts' in schemas and 'main_marts' not in schemas, schemas; print('OK', schemas)"</automated>
  </verify>
  <acceptance_criteria>
    - `dbt seed --full-refresh` exits 0; querying `information_schema.schemata` on `../data/warehouse/epra.duckdb` returns a schema named `marts` and NOT `main_marts` (SG-13/DM-003 proof — the executor may run this query via `uv run python -c` or `epra.common.db.connect(read_only=True)`).
    - Source assertion: `dbt/macros/generate_schema_name.sql` returns the trimmed `custom_schema_name` on the custom branch (no `target.schema`-prefix concatenation there).
    - `docs/ADR/ADR-009_generate-schema-name-macro.md` exists with Context/Decision/Consequences/Spec deviations sections and cites SG-13/DM-003.
  </acceptance_criteria>
  <done>generate_schema_name yields literal `staging`/`marts` schemas; ADR-009 records the accepted single-target tradeoff.</done>
</task>

<task type="auto">
  <name>Task 2: sources.yml — every raw dataset exposed once via read_parquet/read_csv (DM-004)</name>
  <read_first>
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md (Pattern 1 for external_location / read_parquet shape; Pitfall 1 for the mandatory ../ prefix; Pitfall 6 for union_by_name)
    - docs/SPEC-02_data_model.md (§3 staging table — the eight raw datasets and their grains; §5 for the two data/processed stand-in sources)
    - dbt/profiles.yml (path ../data/warehouse/epra.duckdb — the reason source globs also need the ../data/... prefix)
    - src/epra/ingest/_io.py (raw_month_path / _dataset_root — the real on-disk layout data/raw/<dataset>/<YYYY>/<dataset>_<YYYY-MM>.parquet each glob must match)
  </read_first>
  <files>dbt/models/sources.yml</files>
  <action>
    Create `dbt/models/sources.yml` (version 2) exposing each raw dataset exactly once via DuckDB external locations, every path prefixed `../data/` (dbt cwd is `dbt/`; a bare `data/...` glob silently returns zero rows — Pitfall 1). Define a `raw` source group with tables `entsoe_prices_at`, `entsoe_prices_delu`, `entsoe_load_at`, `entsoe_gen_at`, `geosphere_graz_daily`, each reading `read_parquet('../data/raw/<name>/**/*.parquet', union_by_name=true)`; a `raw_calendar` source with table `calendar` reading `../data/raw/calendar/calendar.parquet`; a `raw_manual` source with table `oespi_monthly` reading `read_csv('../data/manual/oespi_monthly.csv', header=true)`; and a `raw_processed` source group with tables `consumer_load_hourly` and `procurement_cost_monthly` reading `read_parquet('../data/processed/<name>/**/*.parquet', union_by_name=true)` (fed by the D-05/D-06 stand-in generator in 04-05). Give each table a `description:` citing `DM-004` and its SPEC-02 §3/§5 grain. Pass `union_by_name=true` on every glob (Pitfall 6, defensive). Do NOT add any model or touch `data/` — verification is `dbt parse` proving the sources block compiles; the non-zero-rows proof happens when 04-02 staging builds on real local raw data.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/dbt" && uv run dbt parse</automated>
  </verify>
  <acceptance_criteria>
    - `dbt parse` exits 0 with `sources.yml` present (the sources block is valid dbt-duckdb).
    - Source assertion: every `external_location` / glob in `sources.yml` begins with `../data/` (grep finds zero bare `data/raw` or `data/processed` globs without the `../` prefix).
    - All ten raw tables enumerated exactly once: `entsoe_prices_at`, `entsoe_prices_delu`, `entsoe_load_at`, `entsoe_gen_at`, `geosphere_graz_daily`, `calendar`, `oespi_monthly`, `consumer_load_hourly`, `procurement_cost_monthly` (DM-004 single-glob-per-source).
  </acceptance_criteria>
  <done>sources.yml exposes all raw/manual/processed datasets once via ../data/ globs; dbt parse green.</done>
</task>

<task type="auto">
  <name>Task 3: hand-rolled month_spine + accepted_range helper macros (no dbt_utils)</name>
  <read_first>
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md (Pattern 3 month_spine on DuckDB generate_series; Pattern 4 accepted_range generic test signature; Alternatives Considered = why skip dbt_utils)
    - docs/SPEC-02_data_model.md (DM-050 no-gap month spine; DM-061 accepted ranges these macros will serve)
    - dbt/dbt_project.yml (macro-paths: ["macros"], test-paths: ["tests"] — where these land)
  </read_first>
  <files>dbt/macros/month_spine.sql, dbt/macros/test_accepted_range.sql</files>
  <action>
    Create `dbt/macros/month_spine.sql`: a macro `month_spine(min_month_expr, max_month_expr)` emitting one row per local calendar month between the two bounds using DuckDB native `generate_series(date_trunc('month', min), date_trunc('month', max), interval '1 month')` unnested and cast to `date as month_local` (RESEARCH Pattern 3; serves the DM-050 no-gap test in 04-06). Create `dbt/macros/test_accepted_range.sql`: a generic test `accepted_range(model, column_name, min_value=none, max_value=none)` following dbt's `{% test %}` convention returning rows where the column is below `min_value` or above `max_value` (RESEARCH Pattern 4; serves DM-061 range checks in 04-04/04-05). Add header comments citing `DM-050` and `DM-061` respectively. Do NOT add `packages.yml` or run `dbt deps` — both macros are hand-rolled to keep zero dbt package dependencies (ADR-001, lean-repo). Verify both compile via `dbt parse` (they are first exercised by real models in 04-04/04-06).
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/dbt" && uv run dbt parse && test ! -f packages.yml && echo "no packages.yml (lean-repo OK)"</automated>
  </verify>
  <acceptance_criteria>
    - `dbt parse` exits 0 with both macros present (syntactically valid Jinja/SQL).
    - No `dbt/packages.yml` exists and no `dbt deps` step is introduced (grep for `dbt_utils` across `dbt/` finds nothing).
    - Source assertion: `month_spine.sql` uses DuckDB `generate_series` (not a `dbt_utils.date_spine` call); `test_accepted_range.sql` defines a `{% test accepted_range(...) %}` block.
  </acceptance_criteria>
  <done>month_spine and accepted_range macros compile and are available to later marts/tests with zero package dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/raw + data/manual files → dbt sources | hardcoded read_parquet/read_csv globs (no user/network input) cross into the warehouse |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | sources.yml glob paths | low | mitigate | All globs are static literals prefixed `../data/`; no path is derived from external/user input (RESEARCH Security Domain V5); DuckDB `read_parquet` is core-bundled, no extension autoinstall / network |
| T-04-02 | Information disclosure | schema override macro | low | accept | Override only affects local schema NAMES in the gitignored `epra.duckdb`; no secret exposure (`profiles.yml` credential-free, DM-002) |
| T-04-SC | Tampering | dbt/duckdb packages | low | accept | No install this plan — `dbt-core`/`dbt-duckdb`/`duckdb` already pinned+resolved in `uv.lock` (RESEARCH §Package Legitimacy Audit, all Approved); no packages.yml / dbt deps added |

Auth (V2), Session (V3), Access control (V4), Cryptography (V6): N/A — local single-operator CLI, no network service, credential-free profiles.yml (DM-002).
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `dbt/macros/generate_schema_name.sql` — SG-13/ADR-009 literal-schema override (`staging`/`marts`).
- `dbt/models/sources.yml` — DM-004 external sources: `raw` (5 parquet datasets), `raw_calendar.calendar`, `raw_manual.oespi_monthly` (CSV), `raw_processed` (`consumer_load_hourly`, `procurement_cost_monthly`).
- `dbt/macros/month_spine.sql` — DM-050 no-gap month-spine helper (DuckDB generate_series).
- `dbt/macros/test_accepted_range.sql` — DM-061 accepted-range generic test macro.
- `docs/ADR/ADR-009_generate-schema-name-macro.md` — governance record for the SG-13 tradeoff.
</artifacts_produced>

<verification>
- `cd dbt && dbt seed --full-refresh` green; `information_schema.schemata` shows `marts` (not `main_marts`).
- `cd dbt && dbt parse` green with sources.yml + all three macros present.
- No `dbt/packages.yml`; no `dbt_utils` reference anywhere under `dbt/`.
</verification>

<success_criteria>
DuckDB schemas are literally `staging`/`marts` (SG-13/DM-003), every raw dataset is exposed exactly once via `../data/` external globs (DM-004), and the two hand-rolled helper macros are available to later plans — all with zero dbt package dependencies. ADR-009 is committed.
</success_criteria>

<output>
Create `.planning/phases/EPRA-04-m3-dbt-warehouse/04-01-SUMMARY.md` when done.
</output>
