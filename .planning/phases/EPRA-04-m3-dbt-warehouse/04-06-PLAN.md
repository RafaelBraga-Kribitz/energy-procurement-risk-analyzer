---
phase: EPRA-04-m3-dbt-warehouse
plan: 06
type: execute
wave: 5
depends_on:
  - 04-04
  - 04-05
files_modified:
  - dbt/tests/fct_price_hourly_row_count_per_year.sql
  - dbt/tests/reconcile_price_monthly_2022_08.sql
  - dbt/tests/dst_hour_counts_fct_price_hourly.sql
  - dbt/tests/no_gap_monthly_marts.sql
  - dbt/tests/freshness_stg_prices_at_hourly.sql
  - dbt/contracts/marts_contract.yml
  - tests/unit/test_marts_contract.py
autonomous: true
requirements:
  - REQ-DWH-01
  - DM-050
  - DM-062
  - DM-064
  - DM-065
  - DM-066
  - D-07
must_haves:
  truths:
    - "DM-062 boundary: fct_price_hourly per year_local has 8760 (or 8784 in a leap year) rows within +/-24, one step either side fails the singular row-count test"
    - "DM-064 precision: for the hardcoded month 2022-08, fct_price_monthly.price_base_eur_mwh equals the mean of fct_price_hourly for that month within a 0.01 tolerance (the exact reconciliation contract; guards aggregation drift)"
    - "DM-065 adjacency: the DST fall-back/spring-forward local hours resolve via ts_utc + dim_calendar — 2024-03-31 has 23 rows and 2024-10-27 has 25 rows in fct_price_hourly (never a model TZ call)"
    - "DM-050 ordering: no_gap_monthly_marts asserts a contiguous month spine (one row per month, no gaps) via the month_spine macro across fct_price_monthly, fct_generation_monthly, fct_procurement_cost_monthly"
    - "D-07 encoding: test_marts_contract.py diffs information_schema.columns names + DuckDB canonical types for schema='marts' against the hand-authored marts_contract.yml (all 6 marts, SG-05 enumeration for fct_price_hourly), parsed with yaml.safe_load; editing any mart column name/type breaks it"
    - "DM-066 freshness is wired as a var-gated singular test (config enabled=var('check_freshness', false)) so a normal dbt build never runs it — exercised only at M7 make refresh"
  artifacts:
    - dbt/tests/fct_price_hourly_row_count_per_year.sql
    - dbt/tests/reconcile_price_monthly_2022_08.sql
    - dbt/tests/dst_hour_counts_fct_price_hourly.sql
    - dbt/tests/no_gap_monthly_marts.sql
    - dbt/tests/freshness_stg_prices_at_hourly.sql
    - dbt/contracts/marts_contract.yml
    - tests/unit/test_marts_contract.py
  key_links:
    - "no_gap_monthly_marts.sql calls the month_spine macro from 04-01; the singular tests ref the 04-04/04-05 marts"
    - "test_marts_contract.py reads dbt/contracts/marts_contract.yml (yaml.safe_load) and queries the marts schema via epra.common.db.connect(read_only=True)"
  prohibitions:
    - "the DM-064/DM-065 singular tests use the spec-literal hardcoded dates (2022-08, 2024-03-31, 2024-10-27) — NOT parametrized/derived — so CI fixtures run them byte-identically to the real build (D-03)"
    - "marts_contract.yml is consumed by the pytest diff test ONLY (it lives outside model-paths); do NOT expect dbt to auto-enforce it (Pitfall 3)"
    - "the contract test must use yaml.safe_load, never yaml.load with the default Loader (unsafe deserialization, RESEARCH Security Domain)"
---

<objective>
Complete the DM-060..066 test set and the D-07 schema contract — the M3 exit gate. Author the four/five singular dbt tests (row-count ±24, 2022-08 reconciliation 0.01, DST 23/25, no-gap month spine, var-gated freshness), the hand-authored `marts_contract.yml` enumerating all 6 marts, and the pytest diff test that byte-matches `information_schema.columns` against that contract.

Purpose: T3.05 — SC#2 (mart schemas byte-match SPEC-02 §5) plus the DM-062/064/065/050/066 data contracts. This wave makes `dbt build` fail on any aggregation/schema drift.
Output: 5 `dbt/tests/*.sql`, `dbt/contracts/marts_contract.yml`, `tests/unit/test_marts_contract.py`.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-VALIDATION.md
@docs/SPEC-02_data_model.md
@dbt/macros/month_spine.sql
@tests/test_raw_contracts.py
@src/epra/common/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Singular dbt tests — DM-062/064/065 + DM-050 no-gap + DM-066 freshness</name>
  <read_first>
    - docs/SPEC-02_data_model.md (DM-062 row counts 8760/8784 ±24; DM-064 2022-08 reconciliation 0.01; DM-065 DST 23/25; DM-050 no-gap months; DM-066 freshness < 40 days, refresh-only)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md (Pattern 5 the three singular-test SQL shapes; Pattern 3 no-gap spine using month_spine; Pattern 7 var-gated freshness config)
    - dbt/macros/month_spine.sql (04-01 — the DM-050 spine helper to call)
    - dbt/models/marts/fct_price_hourly.sql, fct_price_monthly.sql (04-04 — the refs these tests assert over)
    - dbt/models/marts/fct_procurement_cost_monthly.sql (04-05 — third monthly mart for the no-gap test)
  </read_first>
  <files>dbt/tests/fct_price_hourly_row_count_per_year.sql, dbt/tests/reconcile_price_monthly_2022_08.sql, dbt/tests/dst_hour_counts_fct_price_hourly.sql, dbt/tests/no_gap_monthly_marts.sql, dbt/tests/freshness_stg_prices_at_hourly.sql</files>
  <action>
    Create five singular tests (a `.sql` in test-paths returning failing rows = a failing test). `fct_price_hourly_row_count_per_year.sql` (DM-062): group `ref('fct_price_hourly')` by year_local, expected 8784 for leap years else 8760, return years where `abs(n_hours - expected) > 24`. `reconcile_price_monthly_2022_08.sql` (DM-064): compare `fct_price_monthly.price_base_eur_mwh` for the HARDCODED month 2022-08 against `avg(price_at_eur_mwh)` of `fct_price_hourly` for year_local=2022, month_local=8, return the row if `abs(diff) > 0.01` (spec-literal, not parametrized — D-03). `dst_hour_counts_fct_price_hourly.sql` (DM-065): assert `fct_price_hourly` has 23 rows on date_local 2024-03-31 and 25 on 2024-10-27 (hardcoded), return mismatches. `no_gap_monthly_marts.sql` (DM-050): using the `month_spine` macro over each monthly mart's min/max month, left-join the mart's distinct months and return spine months with no matching row (apply to fct_price_monthly, fct_generation_monthly, fct_procurement_cost_monthly). `freshness_stg_prices_at_hourly.sql` (DM-066): `{{ config(enabled=var('check_freshness', false)) }}` then return a row when `max(ts_utc)` in `ref('stg_prices_at_hourly')` is older than 40 days — so a normal `dbt build` never runs it (M7 refresh sets the var). Verify by a full `dbt build` (all singular tests green on real data; freshness skipped).
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/dbt" && uv run dbt build</automated>
  </verify>
  <acceptance_criteria>
    - `dbt build` exits 0 on real local data with zero failing tests; the freshness test is skipped (disabled) in the normal build (grep confirms `config(enabled=var('check_freshness', false))`).
    - Source assertion: DM-064 uses literal `2022`/`8` and DM-065 uses literal `2024-03-31`/`2024-10-27` (not parametrized — D-03).
    - Source assertion: `no_gap_monthly_marts.sql` calls the `month_spine` macro (DM-050) and covers all three monthly marts.
  </acceptance_criteria>
  <done>All DM-050/062/064/065 singular tests green in dbt build; DM-066 freshness wired but disabled by default.</done>
</task>

<task type="auto">
  <name>Task 2: marts_contract.yml — hand-authored 6-mart schema contract (D-07)</name>
  <read_first>
    - docs/SPEC-02_data_model.md (§5 the exact column list of all 6 marts — the diff target)
    - docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md (SG-05 frozen fct_price_hourly enumeration = ING-110 list + season, hdd_18, cdd_22)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md (Pattern 6 contract-outside-model-paths; Open Question 2 name+type inclusion; DuckDB canonical type strings)
    - dbt/models/marts/fct_price_hourly.sql (04-04 — confirm actual emitted columns match SG-05)
    - dbt/models/marts/fct_procurement_cost_monthly.sql, fct_consumer_load_hourly.sql (04-05 — the two stand-in-fed marts also in the contract)
  </read_first>
  <files>dbt/contracts/marts_contract.yml</files>
  <action>
    Hand-author `dbt/contracts/marts_contract.yml` enumerating all 6 marts (`fct_price_hourly`, `fct_price_daily`, `fct_price_monthly`, `fct_generation_monthly`, `fct_consumer_load_hourly`, `fct_procurement_cost_monthly`) with each column's name AND its DuckDB canonical type string (e.g. `TIMESTAMP WITH TIME ZONE`, `DOUBLE`, `BIGINT`, `VARCHAR`, `DATE`, `BOOLEAN`) exactly per SPEC-02 §5 — using the SG-05 frozen enumeration for `fct_price_hourly` (ts_utc, price_at_eur_mwh, price_delu_eur_mwh, spread_at_delu_eur_mwh, load_at_mw, is_negative_price + date_local, year_local, month_local, hour_local, dow_local, is_weekend, is_holiday_at, is_peak_hour, season, hdd_18, cdd_22). This file lives OUTSIDE `model-paths` on purpose (Pitfall 3) — it is the human-reviewed diff target, not a dbt-native contract. Add header comments citing `D-07, SPEC-02 §5, SG-05`. To capture the canonical types accurately, build the marts first and read `information_schema.columns` for schema='marts' as the source of truth for the type strings, then commit those into the YAML. Verify the YAML parses with yaml.safe_load.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run python -c "import yaml,pathlib; d=yaml.safe_load(pathlib.Path('dbt/contracts/marts_contract.yml').read_text(encoding='utf-8')); marts=set(d) if isinstance(d,dict) else {m for m in d}; print(sorted(d)); assert len({'fct_price_hourly','fct_price_daily','fct_price_monthly','fct_generation_monthly','fct_consumer_load_hourly','fct_procurement_cost_monthly'})==6"</automated>
  </verify>
  <acceptance_criteria>
    - `marts_contract.yml` parses via `yaml.safe_load` and enumerates all 6 marts with per-column name + DuckDB type.
    - `fct_price_hourly` entry matches the SG-05 frozen enumeration exactly (17 columns incl. the 11 dim_calendar attributes).
    - The file is under `dbt/contracts/` (outside `model-paths`), cited `D-07`.
  </acceptance_criteria>
  <done>The 6-mart schema contract is committed, spec-derived, safe-loadable, and outside model-paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_marts_contract.py — information_schema diff vs contract (D-07)</name>
  <read_first>
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md (test_raw_contracts.py parametrize-per-dataset + REPO_ROOT convention; DB access via epra.common.db.connect read_only)
    - tests/test_raw_contracts.py (the contract-drift-guard structure to mirror: parametrize over table name, assert exact column layout)
    - src/epra/common/db.py (connect(settings, read_only=True) — the sanctioned warehouse reader)
    - dbt/contracts/marts_contract.yml (Task 2 — the yaml.safe_load target)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md (Pattern 6 pytest shape; Security Domain: safe_load only)
  </read_first>
  <files>tests/unit/test_marts_contract.py</files>
  <behavior>
    - Loads dbt/contracts/marts_contract.yml via yaml.safe_load (never yaml.load) from REPO_ROOT / 'dbt' / 'contracts' / 'marts_contract.yml'.
    - Connects to the warehouse via epra.common.db.connect(settings, read_only=True) and queries information_schema.columns for table_schema='marts' ordered by table_name, ordinal_position.
    - Parametrized per mart: asserts the actual (column_name, data_type) list equals the contract's enumeration for that mart, failing with a readable diff naming the mart + offending column.
    - Skips (or xfails with a clear reason) gracefully if the marts schema is empty (warehouse not yet built), so the test is meaningful only after dbt build.
  </behavior>
  <action>
    Create `tests/unit/test_marts_contract.py` mirroring `tests/test_raw_contracts.py`: import `REPO_ROOT` from `epra.common.config`, `connect` from `epra.common.db`, `load_settings`; `CONTRACT_PATH = REPO_ROOT / 'dbt' / 'contracts' / 'marts_contract.yml'` loaded via `yaml.safe_load`; `@pytest.mark.parametrize('mart', sorted(contract))` iterating the 6 marts; in each, open `connect(load_settings(), read_only=True)`, query `information_schema.columns` for `table_schema='marts'` and the mart's `table_name` ordered by `ordinal_position`, and assert the `(column_name, data_type)` sequence equals the contract's — with an assertion message naming the mart and the first mismatching column (D-07: "editing any mart column name/type breaks it"). Use `yaml.safe_load` ONLY. Verify the marts are built (run dbt build if needed) then run the pytest.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run pytest tests/unit/test_marts_contract.py -m "not live" -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_marts_contract.py -m "not live" -x` exits 0 against a built warehouse (all 6 marts match the contract).
    - Source assertion: the test uses `yaml.safe_load` and `epra.common.db.connect(..., read_only=True)` (grep confirms both; no bare `yaml.load`, no raw `duckdb.connect`).
    - Drift proof (verify once, revert): renaming any one mart column makes this test fail naming that column.
  </acceptance_criteria>
  <done>The schema-contract diff test passes on a built warehouse and fails loudly on any mart column name/type drift (SC#2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marts_contract.yml (repo file) → pytest deserialization | a YAML file parsed in a test is an untrusted-deserialization surface if loaded unsafely |
| built marts → information_schema diff | schema drift (renamed/retyped column) crossing silently into downstream analytics is the risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Tampering | marts_contract.yml deserialization | high | mitigate | Parse with `yaml.safe_load` only (never `yaml.load` default Loader) — no arbitrary object construction (RESEARCH Security Domain V5) |
| T-04-12 | Tampering | mart schema drift | high | mitigate | D-07 pytest diffs information_schema.columns name+type vs the committed contract; any drift is a red test (SC#2) |
| T-04-13 | Tampering | aggregation drift | medium | mitigate | DM-064 reconciliation (0.01) + DM-062 row-count (±24) singular tests fail the build on drift |
| T-04-SC | Tampering | dbt/duckdb/pyyaml packages | low | accept | No new install; pyyaml is a dbt-core transitive dep, duckdb pinned in uv.lock (RESEARCH §Package Legitimacy Audit) |

Auth (V2), Session (V3), Access control (V4), Cryptography (V6): N/A — local single-operator warehouse, no network.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `dbt/tests/fct_price_hourly_row_count_per_year.sql` — DM-062 (±24 boundary).
- `dbt/tests/reconcile_price_monthly_2022_08.sql` — DM-064 (0.01 precision reconciliation).
- `dbt/tests/dst_hour_counts_fct_price_hourly.sql` — DM-065 (DST 23/25 adjacency).
- `dbt/tests/no_gap_monthly_marts.sql` — DM-050 (contiguous month spine ordering, via month_spine).
- `dbt/tests/freshness_stg_prices_at_hourly.sql` — DM-066 (var-gated, refresh-only).
- `dbt/contracts/marts_contract.yml` — D-07 hand-authored 6-mart name+type contract (outside model-paths).
- `tests/unit/test_marts_contract.py` — D-07 pytest information_schema diff (yaml.safe_load, db.connect read_only).
</artifacts_produced>

<verification>
- `cd dbt && dbt build` green on real local data (all singular + generic tests; freshness skipped).
- `uv run pytest tests/unit/test_marts_contract.py -m "not live"` green against the built warehouse.
- Greps confirm literal DM-064/065 dates, `yaml.safe_load`, `db.connect(read_only=True)`, and `config(enabled=var('check_freshness', false))`.
</verification>

<success_criteria>
`dbt build` fails on any aggregation drift (DM-062/064/065), monthly marts are provably gap-free (DM-050), freshness is wired but dormant (DM-066), and the D-07 pytest byte-matches all 6 mart schemas to the committed contract (SC#2) — completing the M3 test gate.
</success_criteria>

<output>
Create `.planning/phases/EPRA-04-m3-dbt-warehouse/04-06-SUMMARY.md` when done.
</output>
