---
phase: EPRA-04-m3-dbt-warehouse
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - dbt/models/staging/stg_prices_at_native.sql
  - dbt/models/staging/stg_prices_delu_native.sql
  - dbt/models/staging/stg_prices_at_hourly.sql
  - dbt/models/staging/stg_prices_delu_hourly.sql
  - dbt/models/staging/stg_load_at_hourly.sql
  - dbt/models/staging/stg_gen_at_hourly.sql
  - dbt/models/staging/stg_weather_graz_daily.sql
  - dbt/models/staging/stg_oespi_monthly.sql
  - dbt/models/staging/staging.yml
  - dbt/tests/predup_count_prices.sql
autonomous: true
requirements:
  - REQ-DWH-01
  - DM-005
  - DM-020
  - SG-16
  - SG-17
must_haves:
  truths:
    - "the 8 staging views build (dbt build --select staging exits 0) on real local raw data with exact SPEC-02 §3 grains/columns/unit suffixes (DM-005)"
    - "stg_prices_at_hourly / stg_prices_delu_hourly aggregate PT15M quarters to the hourly MEAN and record n_subhours (1 for PT60M, 4 for PT15M), truncating ts to hour — never a naive avg that drops n_subhours (Pitfall 4)"
    - "stg_prices_at_native / stg_prices_delu_native dedup exactly once via qualify row_number() over (partition by ts_utc order by ingested_at_utc desc) = 1 — the ONLY sanctioned dedup (DM-020)"
    - "stg_gen_at_hourly keeps only kind='aggregated' rows (SG-17) and stg_weather_graz_daily documents in its YAML that GeoSphere date is the local civil date (SPEC-02 §3)"
    - "unique + not_null hold on every staging grain key and a pre-dedup duplicate-count test warns (not errors) when any month has more than 30 duplicate ts_utc (DM-020)"
  artifacts:
    - dbt/models/staging/stg_prices_at_native.sql
    - dbt/models/staging/stg_prices_delu_native.sql
    - dbt/models/staging/stg_prices_at_hourly.sql
    - dbt/models/staging/stg_prices_delu_hourly.sql
    - dbt/models/staging/stg_load_at_hourly.sql
    - dbt/models/staging/stg_gen_at_hourly.sql
    - dbt/models/staging/stg_weather_graz_daily.sql
    - dbt/models/staging/stg_oespi_monthly.sql
    - dbt/models/staging/staging.yml
    - dbt/tests/predup_count_prices.sql
  key_links:
    - "every staging model reads only source('raw', ...) / source('raw_calendar'/'raw_manual', ...) from 04-01 sources.yml — no model touches data/ files directly (DM-004)"
    - "hourly means here feed dim_calendar-joined marts in 04-04; n_subhours is the DM-064 reconciliation guard downstream"
  prohibitions:
    - "no staging model may call a timezone conversion function or derive local calendar attributes — ts_utc stays UTC; local attributes come only from dim_calendar in the marts (DM-011)"
    - "dedup happens in exactly ONE place (stg_*_native qualify row_number); no other model silently resolves duplicates (DM-020)"
    - "MW is not converted to MWh in staging (load/gen stay in MW; MWh is dbt mart work, ING-051)"
---

<objective>
Build the eight SPEC-02 §3 staging views over the 04-01 sources: passthrough+dedup native price models, PT15M→hourly MEAN price/load/generation models carrying `n_subhours`, the daily weather rename, and the monthly ÖSPI CSV reader — plus the staging generic tests (unique/not_null on grain keys) and the DM-020 pre-dedup duplicate-count warn test.

Purpose: T3.02 — staging is the only layer that transforms raw; marts (04-04/04-05) build exclusively on these views.
Output: 8 `stg_*.sql`, `staging.yml`, `predup_count_prices.sql`.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md
@docs/SPEC-02_data_model.md
@dbt/models/sources.yml
@docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Price staging — native dedup + hourly mean with n_subhours</name>
  <read_first>
    - docs/SPEC-02_data_model.md (§3 rows for stg_prices_at_native, stg_prices_delu_native, stg_prices_at_hourly, stg_prices_delu_hourly — exact columns and the PT60M/PT15M mean logic; DM-020 dedup rule)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md (Pitfall 4 the n_subhours off-by-one; Architecture diagram staging layer)
    - dbt/models/sources.yml (source('raw','entsoe_prices_at') / entsoe_prices_delu — the ts_utc, price_eur_mwh, resolution, zone, ingested_at_utc columns available)
    - tests/fixtures/entsoe (real single-month excerpts showing the raw column shapes and PT60M/PT15M resolution values)
  </read_first>
  <files>dbt/models/staging/stg_prices_at_native.sql, dbt/models/staging/stg_prices_delu_native.sql, dbt/models/staging/stg_prices_at_hourly.sql, dbt/models/staging/stg_prices_delu_hourly.sql</files>
  <action>
    Create the four price staging views. `stg_prices_at_native` / `stg_prices_delu_native`: select `ts_utc, price_eur_mwh, resolution, zone` from the corresponding `raw` source, deduped by the SINGLE sanctioned rule `qualify row_number() over (partition by ts_utc order by ingested_at_utc desc) = 1` (DM-020 — the only place duplicates are resolved). `stg_prices_at_hourly` / `stg_prices_delu_hourly`: from the native view, produce hour grain `ts_utc, price_eur_mwh, n_subhours` where `ts_utc` is truncated to the hour via `date_trunc('hour', ts_utc)`, `price_eur_mwh` is `avg(price_eur_mwh)` over that hour, and `n_subhours` is `count(*)` (1 for a PT60M source hour, 4 for PT15M; mixed months handled per-row by the group-by-truncated-hour — SPEC-02 §3, Pitfall 4). Always emit `n_subhours` — never a bare `avg` that drops it. Do NOT call any timezone function (DM-011); `ts_utc` stays UTC. Add an `Implements: DM-005, DM-020` header comment to each model. Verify by building the four price staging views against real local data.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/dbt" && uv run dbt build --select stg_prices_at_native stg_prices_delu_native stg_prices_at_hourly stg_prices_delu_hourly</automated>
  </verify>
  <acceptance_criteria>
    - `dbt build --select` of the four price views exits 0 on real local raw data (non-zero rows, proving the 04-01 globs resolve).
    - Source assertion: each `stg_*_native` uses `qualify row_number() over (partition by ts_utc order by ingested_at_utc desc) = 1` exactly once (DM-020).
    - Source assertion: `stg_prices_at_hourly`/`stg_prices_delu_hourly` select `n_subhours` alongside the mean (grep confirms `n_subhours` present) and contain no `AT TIME ZONE` / timezone-conversion call (DM-011).
  </acceptance_criteria>
  <done>Four price staging views build on real data with single-point dedup and hour-grain means carrying n_subhours.</done>
</task>

<task type="auto">
  <name>Task 2: Load, generation, weather, ÖSPI staging</name>
  <read_first>
    - docs/SPEC-02_data_model.md (§3 rows for stg_load_at_hourly, stg_gen_at_hourly, stg_weather_graz_daily, stg_oespi_monthly — grains, columns, the kind='aggregated' filter, the GeoSphere date-is-local note)
    - dbt/models/sources.yml (source('raw','entsoe_load_at'/'entsoe_gen_at'/'geosphere_graz_daily'), source('raw_manual','oespi_monthly'))
    - docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md (SG-17 persist both gen kinds, staging filters aggregated)
    - src/epra/ingest/geosphere.py (the raw geosphere column names — date, tl_mittel/tavg source column — so the rename maps correctly)
  </read_first>
  <files>dbt/models/staging/stg_load_at_hourly.sql, dbt/models/staging/stg_gen_at_hourly.sql, dbt/models/staging/stg_weather_graz_daily.sql, dbt/models/staging/stg_oespi_monthly.sql</files>
  <action>
    Create the remaining four staging views. `stg_load_at_hourly`: hour grain `ts_utc, load_mw` = mean of sub-hourly MW grouped by `date_trunc('hour', ts_utc)` (MW stays MW — no MWh conversion, ING-051). `stg_gen_at_hourly`: grain hour × psr_type, columns `ts_utc, psr_type, psr_name, gen_mw`, filtering `kind = 'aggregated'` (SG-17) and taking the mean of sub-hourly MW per hour × psr_type. `stg_weather_graz_daily`: day grain `date_local, tavg_c`, renaming the GeoSphere daily-mean-temperature column to `tavg_c` and the date column to `date_local`; add a model-YAML/`description` note (in staging.yml, Task 3) that GeoSphere `date` is the local civil date (SPEC-02 §3). `stg_oespi_monthly`: month grain `month_local` (DATE, first of month), `oespi_base`, `oespi_peak` from `source('raw_manual','oespi_monthly')` via the CSV source. No timezone-conversion calls anywhere (DM-011). Add `Implements: DM-005` header comments (plus `SG-17` on the generation model). Verify by building these four views against real local data.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/dbt" && uv run dbt build --select stg_load_at_hourly stg_gen_at_hourly stg_weather_graz_daily stg_oespi_monthly</automated>
  </verify>
  <acceptance_criteria>
    - `dbt build --select` of the four views exits 0 on real local data.
    - Source assertion: `stg_gen_at_hourly` filters `kind = 'aggregated'` (SG-17) and produces `ts_utc, psr_type, psr_name, gen_mw`.
    - Source assertion: `stg_weather_graz_daily` exposes `date_local, tavg_c`; `stg_oespi_monthly` exposes `month_local, oespi_base, oespi_peak`; no timezone-conversion call in any of the four (DM-011).
  </acceptance_criteria>
  <done>Load/gen/weather/ÖSPI staging views build on real data with exact §3 columns and the aggregated-only generation filter.</done>
</task>

<task type="auto">
  <name>Task 3: staging.yml generic tests + DM-020 pre-dedup duplicate-count warn</name>
  <read_first>
    - docs/SPEC-02_data_model.md (DM-060 unique/not_null on grain keys; DM-020 pre-dedup count warn > 30/month)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md (spec-ID-in-YAML description convention; singular test shape)
    - dbt/models/sources.yml (raw price sources — the pre-dedup count reads the RAW source, not the deduped native view)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-RESEARCH.md (Pattern 5 singular-test .sql shape; test severity: warn config)
  </read_first>
  <files>dbt/models/staging/staging.yml, dbt/tests/predup_count_prices.sql</files>
  <action>
    Create `dbt/models/staging/staging.yml` (version 2) declaring all eight staging models with `description:` fields citing their `DM-005`/§3 contract (include the GeoSphere-date-is-local note on `stg_weather_graz_daily`), and generic tests per DM-060: `unique` + `not_null` on each grain key — `ts_utc` for the four hourly/native price models and load; the composite `[ts_utc, psr_type]` for `stg_gen_at_hourly` (use a `unique` combination-of-columns test or a surrogate); `date_local` for weather; `month_local` for ÖSPI. Create `dbt/tests/predup_count_prices.sql`: a singular test configured `severity: warn` (via `{{ config(severity='warn') }}`) that counts pre-dedup duplicate `ts_utc` per local month in the RAW price sources and returns rows where a month has more than 30 duplicates (DM-020 defense-in-depth — warn, never fail the build). Do NOT dedup anywhere except the native views. Verify with a staging-scoped `dbt build`.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/dbt" && uv run dbt build --select staging</automated>
  </verify>
  <acceptance_criteria>
    - `dbt build --select staging` exits 0 with zero failing generic tests on real local data (unique/not_null green on all 8 grain keys).
    - `predup_count_prices.sql` is configured `severity: warn` (grep confirms `severity='warn'`) so a duplicate spike warns without failing the build (DM-020).
    - `staging.yml` enumerates all 8 models with DM-citing descriptions and the GeoSphere-local-date note on `stg_weather_graz_daily`.
  </acceptance_criteria>
  <done>staging.yml pins unique/not_null on every grain key; the DM-020 duplicate-count test warns; the full staging layer builds green on real data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw parquet/CSV (04-01 sources) → staging views | ingested data crosses into transform layer; duplicates/resolution drift are the risks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | dedup ordering | medium | mitigate | Single sanctioned dedup `qualify row_number() over (partition by ts_utc order by ingested_at_utc desc)` keeps the latest ingest; DM-020 warn-count test surfaces anomalous duplicate spikes (>30/month) without silently masking them |
| T-04-04 | Tampering | PT15M→hourly aggregation | medium | mitigate | `n_subhours = count(*)` is always emitted alongside the mean so partial-hour aggregation is detectable downstream (DM-064 reconciliation guard); Pitfall 4 |
| T-04-SC | Tampering | dbt/duckdb packages | low | accept | No install this plan; core packages pinned in uv.lock (RESEARCH §Package Legitimacy Audit) |

Auth (V2), Session (V3), Access control (V4), Cryptography (V6): N/A — local single-operator warehouse, no network service.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `dbt/models/staging/stg_prices_at_native.sql`, `stg_prices_delu_native.sql` — DM-020 deduped native MTU price views.
- `dbt/models/staging/stg_prices_at_hourly.sql`, `stg_prices_delu_hourly.sql` — hour-grain means with `n_subhours` (Pitfall 4).
- `dbt/models/staging/stg_load_at_hourly.sql` — hour-grain load MW mean (ING-051, MW stays MW).
- `dbt/models/staging/stg_gen_at_hourly.sql` — hour × psr_type generation, `kind='aggregated'` only (SG-17).
- `dbt/models/staging/stg_weather_graz_daily.sql` — daily `date_local, tavg_c` rename.
- `dbt/models/staging/stg_oespi_monthly.sql` — monthly `month_local, oespi_base, oespi_peak` from the manual CSV.
- `dbt/models/staging/staging.yml` — DM-060 unique/not_null generic tests + §3-citing descriptions.
- `dbt/tests/predup_count_prices.sql` — DM-020 pre-dedup duplicate-count warn test.
</artifacts_produced>

<verification>
- `cd dbt && dbt build --select staging` green on real local data (all 8 views + generic tests).
- Source greps confirm single-point dedup (DM-020), `n_subhours` presence, `kind='aggregated'` filter, and zero timezone-conversion calls (DM-011).
- `predup_count_prices.sql` configured `severity: warn`.
</verification>

<success_criteria>
All eight SPEC-02 §3 staging views build green on real local data with exact grains/columns/units, dedup occurs in exactly one place (DM-020), hourly means carry `n_subhours`, and unique/not_null hold on every grain key — with no timezone calls in the staging layer (DM-011).
</success_criteria>

<output>
Create `.planning/phases/EPRA-04-m3-dbt-warehouse/04-02-SUMMARY.md` when done.
</output>
