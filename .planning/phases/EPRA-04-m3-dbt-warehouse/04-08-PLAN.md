---
phase: EPRA-04-m3-dbt-warehouse
plan: 08
type: execute
wave: 7
depends_on:
  - 04-07
files_modified:
  - .github/workflows/ci.yml
  - docs/BUILD_LOG.md
autonomous: false
requirements:
  - REQ-DWH-01
  - D-01
  - D-02
must_haves:
  truths:
    - "the CI dbt-check job runs scripts/bootstrap_fixture_warehouse.py (network-free, synthesizing the 2022-2024 fixture warehouse) then cd dbt && dbt build, and is a genuinely separate required job (EN-080 job 3, TP.02) not folded into test:"
    - "the CI fixture dbt build is provably green network-free WITHOUT a full local backfill (SC#3) — verified locally by running the same bootstrap + dbt build sequence into a clean data root"
    - "the local real-data dbt build is green on real data/raw 2019->latest (SC#1) and the committed reports/warehouse/dbt_build_<date>.md records it (D-01/D-02)"
    - "the D-07 schema-contract pytest is green so mart schemas byte-match SPEC-02 §5 (SC#2)"
    - "git status stays clean of data/ — epra.duckdb and synthesized data/raw|processed remain gitignored (DM-001, D-02); only the markdown build report + BUILD_LOG entry are committed"
  artifacts:
    - .github/workflows/ci.yml
    - docs/BUILD_LOG.md
    - reports/warehouse/dbt_build_<date>.md
  key_links:
    - "the dbt-check job invokes the 04-05 bootstrap generator + the 04-01..04-06 dbt project; the close reads the 04-07 build report"
    - "the exit gate closes only when BOTH builds (local real D-01 + CI fixture D-03) are green AND the schema contract byte-matches (SC#1/#2/#3)"
  prohibitions:
    - "the CI job must NOT require live network or a real backfill — it runs the synthesized fixture warehouse only (D-01 split-by-environment)"
    - "do NOT commit any data/ artifact (epra.duckdb, synthesized raw/processed) — git status must stay clean of data/ (DM-001, D-02)"
    - "do NOT auto-approve the phase-exit human checkpoint — the operator confirms both builds green + report committed before close (D-01/D-02)"
---

<objective>
Close M3: add the required CI `dbt-check` job (bootstrap fixture warehouse + `dbt build`, network-free — SC#3), run and commit the local real-data build report (SC#1/D-02), confirm the D-07 schema contract byte-matches (SC#2), record the BUILD_LOG entry, and gate the phase close on an operator confirmation that both builds are green and `git status` is clean of `data/`.

Purpose: T3.06 (CI job) + T3.07 (PR assembly) — the phase closes only when BOTH the local real-data build and the CI fixture build are green and mart schemas byte-match.
Output: `.github/workflows/ci.yml` dbt-check job, committed `reports/warehouse/dbt_build_<date>.md`, `docs/BUILD_LOG.md` entry.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-CONTEXT.md
@.planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md
@.github/workflows/ci.yml
@docs/BUILD_LOG.md
@scripts/bootstrap_fixture_warehouse.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI dbt-check job — bootstrap fixture warehouse + dbt build (network-free, required)</name>
  <read_first>
    - .github/workflows/ci.yml (lint:/test: jobs lines 13-34 for the runs-on/checkout/setup-uv/uv pip install shape; the commented dbt-check placeholder line 36; the top-of-file comment line 2)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-PATTERNS.md (ci.yml dbt-check job section — separate job, uncomment placeholder, bootstrap then dbt build, EN-080 job 3)
    - scripts/bootstrap_fixture_warehouse.py (04-05 — the network-free synth the job runs; --force to build the fixture warehouse)
    - docs/EXECUTION_BLUEPRINT/02_WBS.md (T3.06 — CI job required, runtime < 5 min)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Replace the commented `# dbt-check:` placeholder with a real `dbt-check` job mirroring the `test:` job's steps (checkout, setup-uv python 3.12, `uv venv && uv pip install -e ".[dev]"`), then two run steps: `uv run python scripts/bootstrap_fixture_warehouse.py --force` (synthesize the 2022-2024 fixture warehouse, network-free — D-04) followed by `cd dbt && uv run dbt build` (full DAG on the fixtures). Keep it a genuinely separate job (not folded into `test:`) so it can be made a required check independently (EN-080 job 3, TP.02). Update the top-of-file comment (line 2) noting job 3 is now live. The actual GitHub branch-protection/required-check flip is TP.02 (operator, out of code scope). Verify LOCALLY that the exact command sequence is green network-free by running it into a clean/isolated data root (proving SC#3 without pushing to GitHub).
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run python -c "import yaml,pathlib; d=yaml.safe_load(pathlib.Path('.github/workflows/ci.yml').read_text(encoding='utf-8')); assert 'dbt-check' in d['jobs'], list(d['jobs']); print('dbt-check job present')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` has a `dbt-check` job (yaml.safe_load confirms) with a bootstrap step then `cd dbt && dbt build`, separate from `test:`.
    - Local proof of SC#3: running `python scripts/bootstrap_fixture_warehouse.py --force` then `cd dbt && dbt build` exits 0 network-free without a real backfill (operator/executor runs this; if real data/raw is present, use an isolated data root so the fixture path is exercised).
    - The job requires no live network / secret (network-free — D-01).
  </acceptance_criteria>
  <done>The required dbt-check CI job runs the fixture warehouse + dbt build network-free; SC#3 proven locally.</done>
</task>

<task type="auto">
  <name>Task 2: Real-data dbt build + committed build report + BUILD_LOG entry</name>
  <read_first>
    - docs/BUILD_LOG.md (the append-only per-milestone entry format from M0/M1/M2 to mirror — date, milestone, evidence)
    - src/epra/warehouse/report.py (04-07 — the build-report writer invoked here)
    - Makefile (make warehouse — build + report; make transform — build only)
    - .planning/phases/EPRA-04-m3-dbt-warehouse/04-CONTEXT.md (D-01 real-data local build; D-02 build report contents + git-clean-of-data requirement)
  </read_first>
  <files>docs/BUILD_LOG.md</files>
  <action>
    Run the real-data build and report: `make warehouse` (or `cd dbt && dbt build` then `python -m epra.warehouse.report`) against the real local `data/raw` 2019->latest (D-01, SC#1), producing `reports/warehouse/dbt_build_<date>.md` with per-year row counts, month coverage, the 2022-08 reconciliation delta, and the two stand-in-mart flags (D-02). Run the D-07 schema-contract pytest to confirm SC#2. Append a `docs/BUILD_LOG.md` M3 entry (append-only, mirroring the M1/M2 entries) recording: models built, DM-060..066 test pass counts, both-builds-green status, and that `data/` stayed uncommitted. Confirm `git status` is clean of `data/` (epra.duckdb + synthesized parquet gitignored, DM-001). Commit the markdown report + BUILD_LOG only — never any `data/` artifact.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run pytest tests/unit/test_marts_contract.py -m "not live" -x && git status --porcelain data/ && test -z "$(git status --porcelain data/)" && echo DATA_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `reports/warehouse/dbt_build_<date>.md` exists and records per-year counts + month coverage + 2022-08 delta + stand-in flags (D-02).
    - `uv run pytest tests/unit/test_marts_contract.py -m "not live"` exits 0 (SC#2 schema byte-match).
    - `git status --porcelain data/` is EMPTY (no data/ artifact staged/committed — DM-001/D-02); a BUILD_LOG M3 entry is appended.
  </acceptance_criteria>
  <done>Real-data build report committed, schema contract green, BUILD_LOG updated, git clean of data/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Phase-exit checkpoint — both builds green + PR assembly</name>
  <action>
    Operator-confirmed close (not agent-auto-approvable, D-01/D-02): verify BOTH builds are green — the local real-data `dbt build` on real data/raw 2019->latest (SC#1) and the network-free CI fixture `dbt build` via the bootstrap generator (SC#3) — and the D-07 schema-contract pytest (SC#2); confirm `reports/warehouse/dbt_build_<date>.md` + the BUILD_LOG M3 entry are committed and `git status` is clean of `data/` (DM-001); then push the branch and open the M3 PR (GitHub push / branch-protection required-check flip is human-only, TP.02). See how-to-verify for exact steps. Do NOT auto-approve.
  </action>
  <what-built>
    The full dbt warehouse (16 models, DM-060..066 tests, D-07 schema contract), the deterministic CI fixture generator, the required dbt-check CI job, the D-02 build report, and the BUILD_LOG entry all ship automated. This checkpoint is the M3 exit-gate sign-off: the operator confirms BOTH builds are green and opens the M3 PR (the GitHub push / branch-protection flip is human-only).
  </what-built>
  <how-to-verify>
    1. Confirm the LOCAL REAL-DATA build (D-01, SC#1): `make warehouse` exits 0 on real `data/raw` 2019->latest; `reports/warehouse/dbt_build_<date>.md` shows all models built and DM-060..066 tests passing.
    2. Confirm the CI FIXTURE build (SC#3): `python scripts/bootstrap_fixture_warehouse.py --force` then `cd dbt && dbt build` exits 0 network-free (run in an isolated data root so real data is untouched).
    3. Confirm the SCHEMA CONTRACT (SC#2): `uv run pytest tests/unit/test_marts_contract.py -m "not live"` exits 0; optionally rename one mart column to prove the contract test fails, then revert.
    4. Confirm `git status` is clean of `data/` (no epra.duckdb / synthesized parquet committed — DM-001/D-02).
    5. Push the branch and open the M3 PR ticking the exit-gate items with evidence snippets (dbt build green real+fixtures; schemas byte-match); ensure the dbt-check job is a required check (TP.02). This push/PR/branch-protection step is human-only.
  </how-to-verify>
  <resume-signal>Type "approved" once both builds are green, the schema contract passes, the build report + BUILD_LOG are committed, git is clean of data/, and the M3 PR is opened — or describe which gate failed.</resume-signal>
  <verify>
    <human-check>Operator confirms local real-data dbt build (SC#1), CI fixture dbt build network-free (SC#3), and the D-07 schema-contract pytest (SC#2) are all green, the build report + BUILD_LOG are committed, git status is clean of data/, and the M3 PR is opened.</human-check>
  </verify>
  <done>Both builds green (real + CI fixtures), mart schemas byte-match SPEC-02 §5, build report + BUILD_LOG committed, git clean of data/, M3 PR opened — REQ-DWH-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → dbt build | the CI job must stay network-free and never leak/require secrets; risk is a live-network dependency creeping in |
| local build outputs → git | risk is committing gitignored data/ artifacts (epra.duckdb, synthesized parquet) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Information disclosure | epra.duckdb / synthesized data committed to git | medium | mitigate | `git status --porcelain data/` asserted empty at close (DM-001 gitignore); only markdown report + BUILD_LOG committed (D-02) |
| T-04-17 | Denial of service (CI reliability) | dbt-check network dependency | low | mitigate | Job runs only the synthesized fixture warehouse (no network, no secret, no backfill); parquet reading is core-bundled DuckDB (RESEARCH Environment Availability) |
| T-04-18 | Repudiation | phase-close sign-off | medium | mitigate | Blocking human checkpoint — operator confirms both builds green + git clean before close; never auto-approved (D-01/D-02) |
| T-04-SC | Tampering | dbt/duckdb packages | low | accept | No new install; core packages pinned in uv.lock (RESEARCH §Package Legitimacy Audit) |

Auth (V2), Session (V3), Access control (V4), Cryptography (V6): N/A — local single-operator warehouse; the CI job is credential-free.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `.github/workflows/ci.yml` — required `dbt-check` job (bootstrap fixture warehouse + `dbt build`, network-free, EN-080 job 3).
- `docs/BUILD_LOG.md` — appended M3 entry (models built, DM-060..066 pass counts, both-builds-green, data/ uncommitted).
- `reports/warehouse/dbt_build_<date>.md` — the committed D-02 real-data build report (generated in 04-07/here).
- Phase-exit human sign-off + M3 PR (evidence, not code).
</artifacts_produced>

<verification>
- `yaml.safe_load` confirms a `dbt-check` job in `ci.yml`; local run of `bootstrap_fixture_warehouse.py --force` + `dbt build` green network-free (SC#3).
- `make warehouse` green on real data (SC#1); `uv run pytest tests/unit/test_marts_contract.py -m "not live"` green (SC#2).
- `git status --porcelain data/` empty (DM-001/D-02); BUILD_LOG M3 entry present.
- Human checkpoint: operator confirms both builds green + report/BUILD_LOG committed + M3 PR opened.
</verification>

<success_criteria>
REQ-DWH-01 satisfied: `dbt build` is green on real data (SC#1) AND on the network-free CI fixture warehouse (SC#3), mart schemas byte-match the SPEC-02 §5 contract (SC#2), the D-02 build report + BUILD_LOG are committed, `git status` is clean of `data/`, and the M3 PR is opened.
</success_criteria>

<output>
Create `.planning/phases/EPRA-04-m3-dbt-warehouse/04-08-SUMMARY.md` when done.
</output>
