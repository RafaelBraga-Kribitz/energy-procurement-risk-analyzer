---
phase: EPRA-05-m4-consumer-profile
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - docs/ADR/ADR-013_peak-share-reference-year-sg03.md
  - docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md
  - src/epra/consumer/profile.py
  - tests/unit/test_profile.py
  - tests/conftest.py
  - dbt/models/sources.yml
  - scripts/bootstrap_fixture_warehouse.py
  - tests/unit/test_bootstrap_fixture_warehouse.py
autonomous: true
requirements:
  - REQ-LP-01
  - LP-003
  - LP-020
  - LP-021
  - SG-03
  - D-08
must_haves:
  truths:
    - "ADR-013: SSOT consumer_peak_share is the 2019 local-year value; yearly |Δ| vs 2019 &lt; 1 pp else test fails"
    - "2019 peak share ∈ [0.42, 0.48] using calendar is_peak_hour"
    - "hourly parquet is the single file data/processed/consumer_load_hourly.parquet (ts_utc, load_mwh); monthly sibling per LP-021"
    - "ssot_inputs_profile.parquet has key,value,unit,tag,produced_by with unit=fraction tag=CALIBRATED produced_by=epra.consumer.profile"
    - "sources.yml reads the single hourly file; bootstrap writes that same path; bootstrap tests updated"
  artifacts:
    - docs/ADR/ADR-013_peak-share-reference-year-sg03.md
    - src/epra/consumer/profile.py
    - dbt/models/sources.yml
    - scripts/bootstrap_fixture_warehouse.py
  key_links:
    - "fct_consumer_load_hourly.sql unchanged (thin loader)"
    - "05-05 wires make profile CLI to these writers"
  prohibitions:
    - "do not use write_month / ING-004 provenance columns on processed consumer files"
    - "do not publish a non-2019 year to SSOT"
    - "do not change procurement stand-in layout"
---

<objective>
Persist LP-003/LP-021 parquet, compute LP-020 peak share (ADR-013), emit ssot_inputs_profile.parquet, and realign dbt/bootstrap to the SPEC-02/SPEC-03 single hourly file (D-08).

Purpose: T4.03.
Output: writers, ADR-013, sources.yml + bootstrap path fix.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-05-m4-consumer-profile/05-RESEARCH.md
</execution_context>

<context>
@.planning/phases/EPRA-05-m4-consumer-profile/05-CONTEXT.md
@docs/SPEC-03_consumer_load_profile.md
@docs/SPEC-02_data_model.md
@dbt/models/sources.yml
@scripts/bootstrap_fixture_warehouse.py
@tests/unit/test_bootstrap_fixture_warehouse.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-013 + peak share + monthly_volumes (TDD)</name>
  <read_first>
    - docs/SPEC-03_consumer_load_profile.md LP-020 LP-021
    - docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md SG-03
    - .planning/phases/EPRA-05-m4-consumer-profile/05-RESEARCH.md Pattern 3, Pitfall 9
    - docs/ADR/ADR-006_validation-gate-scope-local-year.md
  </read_first>
  <files>docs/ADR/ADR-013_peak-share-reference-year-sg03.md, docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md, src/epra/consumer/profile.py, tests/unit/test_profile.py</files>
  <action>
    ADR-013: publish 2019 local-year peak share; test max |year - 2019| &lt; 0.01 (1 pp). Peak = sum(load_mwh where is_peak_hour) / sum(load_mwh) joined from calendar. Implement `monthly_volumes` and `peak_share_by_year` / `reference_peak_share`. Remove monthly_volumes from stub tests. Mark SG-03 adopted.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_profile.py tests/unit/test_stubs_fail_loudly.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - 2019 peak share in [0.42, 0.48]; max yearly deviation vs 2019 &lt; 1 pp on complete years in the fixture calendar.
    - monthly_volumes columns year_local, month_local, volume_mwh; no M4 rows left in STUBS.
    - ADR-013 + SG-03 adopted.
  </acceptance_criteria>
  <done>Peak share contract is 2019 + deviation gate; monthly grain exists.</done>
</task>

<task type="auto">
  <name>Task 2: atomic processed writers + tmp_settings data_processed</name>
  <read_first>
    - scripts/bootstrap_fixture_warehouse.py `_atomic_write_parquet`
    - tests/conftest.py tmp_settings
    - .planning/phases/EPRA-05-m4-consumer-profile/05-RESEARCH.md Pattern 3
    - src/epra/common/config.py PathsCfg
  </read_first>
  <files>src/epra/consumer/profile.py, tests/unit/test_profile.py, tests/conftest.py</files>
  <action>
    Add `write_profile_outputs(profile_df, calendar_df, cfg, settings)` (or equivalent) writing hourly, monthly, and ssot_inputs_profile.parquet via atomic replace. Hourly columns only ts_utc, load_mwh. SSOT one row: key=consumer_peak_share, unit=fraction, tag=CALIBRATED, produced_by=epra.consumer.profile. Extend tmp_settings to redirect data_processed (and keep tests off the real tree). Tests write under tmp_path and read back.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_profile.py tests/unit/test_logging_and_db.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - Round-trip hourly/monthly/ssot files under tmp_path; schemas match CONTEXT D-05/D-08.
    - tmp_settings redirects data_processed; existing tests still pass.
  </acceptance_criteria>
  <done>Processed artifacts land atomically with spec columns only.</done>
</task>

<task type="auto">
  <name>Task 3: D-08 sources.yml + bootstrap single-file consumer stand-in</name>
  <read_first>
    - dbt/models/sources.yml raw_processed.consumer_load_hourly and raw_calendar analog
    - scripts/bootstrap_fixture_warehouse.py `_write_processed_standins`
    - tests/unit/test_bootstrap_fixture_warehouse.py `_read_dataset`
  </read_first>
  <files>dbt/models/sources.yml, scripts/bootstrap_fixture_warehouse.py, tests/unit/test_bootstrap_fixture_warehouse.py</files>
  <action>
    Point consumer source at `read_parquet('../data/processed/consumer_load_hourly.parquet')`. Bootstrap writes that file with `_atomic_write_parquet` (only ts_utc, load_mwh — no write_month provenance). Update `_read_dataset` (or a dedicated helper) so consumer tests read the single file. Procurement unchanged. Run bootstrap unit tests.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_bootstrap_fixture_warehouse.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - sources.yml consumer location is the single parquet (../data/ prefix).
    - bootstrap tests green; consumer stand-in is one file not a monthly glob.
  </acceptance_criteria>
  <done>CI stand-in and real LP-003 path are the same filename.</done>
</task>

</tasks>
