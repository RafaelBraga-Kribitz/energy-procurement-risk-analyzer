---
phase: EPRA-05-m4-consumer-profile
plan: 05
type: execute
wave: 5
depends_on:
  - 05-04
files_modified:
  - src/epra/consumer/profile.py
  - Makefile
  - src/epra/warehouse/report.py
  - tests/unit/test_warehouse_report.py
  - docs/BUILD_LOG.md
  - LIMITATIONS.md
autonomous: true
requirements:
  - REQ-LP-01
  - EN-050
  - LP-051
must_haves:
  truths:
    - "make profile runs python -m epra.consumer.profile and is idempotent (second run byte-identical hourly parquet)"
    - "make all lists profile before transform"
    - "CLI --profile flat_baseload selects the LP-030 variant via cfg.model_copy"
    - "warehouse report stand-in list is only fct_procurement_cost_monthly"
    - "LIMITATIONS.md §1 still contains the LP-051 sentence; BUILD_LOG M4 entry appended"
    - "06_CHECKLISTS.md §6.7 M4 items are evidenced in the M4 PR description (not a code change unless a checklist tick file exists)"
  artifacts:
    - Makefile
    - src/epra/consumer/profile.py
    - docs/BUILD_LOG.md
  key_links:
    - "T4.03 writers are the CLI body"
    - "Phase verify-work comes after this plan's SUMMARY"
  prohibitions:
    - "do not invoke dbt from make profile"
    - "do not commit data/processed parquet"
    - "do not invent SSOT euro figures in BUILD_LOG or README"
    - "do not mix M5–M7 work"
---

<objective>
Wire the operator interface (`python -m epra.consumer.profile`, `make profile`, `all:` order), drop the consumer stand-in flag, confirm LP-051, append BUILD_LOG, close T4.05.

Purpose: T4.05 M4 assembly.
Output: CLI + Makefile + BUILD_LOG + report flag.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-05-m4-consumer-profile/05-CONTEXT.md
</execution_context>

<context>
@Makefile
@src/epra/ingest/calendar.py
@src/epra/warehouse/report.py
@docs/BUILD_LOG.md
@LIMITATIONS.md
@docs/EXECUTION_BLUEPRINT/06_CHECKLISTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI + Makefile profile / all order</name>
  <read_first>
    - src/epra/ingest/calendar.py main()
    - Makefile profile: and all:
    - src/epra/common/config.py PathsCfg data_raw data_processed
  </read_first>
  <files>src/epra/consumer/profile.py, Makefile, tests/unit/test_profile.py</files>
  <action>
    Add `main(argv)`: load settings + consumer cfg; `--profile` optional override; read `data_raw/calendar/calendar.parquet` (resolve relative to REPO_ROOT); write outputs via T4.03 helper. Missing calendar → actionable error exit 1. Un-stub Makefile `profile:` to `$(UV) run python -m epra.consumer.profile`. Set `all: profile transform analyze simulate ssot export report`. CLI test with tmp_settings-style redirected paths (or monkeypatch settings) so the suite never writes repo data/.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_profile.py -m "not live" --no-cov -q && rg -n "^all: profile transform" Makefile</automated>
  </verify>
  <acceptance_criteria>
    - make profile recipe is the python -m line; all: starts with profile then transform.
    - CLI test writes hourly parquet twice with identical file bytes (no provenance clock).
  </acceptance_criteria>
  <done>Operator can build the profile without dbt; full pipeline orders profile first.</done>
</task>

<task type="auto">
  <name>Task 2: warehouse stand-in flag, LP-051 confirm, BUILD_LOG</name>
  <read_first>
    - src/epra/warehouse/report.py _STAND_IN_MARTS
    - tests/unit/test_warehouse_report.py
    - LIMITATIONS.md §1
    - docs/BUILD_LOG.md last entries
  </read_first>
  <files>src/epra/warehouse/report.py, tests/unit/test_warehouse_report.py, docs/BUILD_LOG.md, LIMITATIONS.md</files>
  <action>
    `_STAND_IN_MARTS` = only `fct_procurement_cost_monthly`; update copy and tests. Confirm LIMITATIONS §1 already has the constructed-profile / LP-051 text — do not invent M6 sensitivity numbers. Append BUILD_LOG M4 entry: date, what shipped, gate evidence (pytest snippet), open questions (golden regen = human; real-data make profile still needs calendar.parquet locally).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_warehouse_report.py -m "not live" --no-cov -q && make lint && uv run pytest -m "not live" -q</automated>
  </verify>
  <acceptance_criteria>
    - Report no longer flags fct_consumer_load_hourly as stand-in.
    - BUILD_LOG has an M4 append-only entry with gate evidence.
    - make lint && pytest -m "not live" green (coverage ≥ 80%).
  </acceptance_criteria>
  <done>M4 operator close-out artifacts exist; consumer mart is no longer labeled stand-in.</done>
</task>

</tasks>
