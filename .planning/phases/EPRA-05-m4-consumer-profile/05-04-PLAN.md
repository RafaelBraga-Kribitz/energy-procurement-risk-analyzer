---
phase: EPRA-05-m4-consumer-profile
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - src/epra/consumer/profile.py
  - tests/unit/test_profile.py
  - tests/golden/consumer_load_2023.sha256
autonomous: true
requirements:
  - REQ-LP-01
  - LP-030
  - LP-040
  - LP-041
  - LP-042
  - EN-072
must_haves:
  truths:
    - "flat_baseload uses the same functions with profile_name flat_baseload; pre-normalization weights are all 1.0; no second YAML"
    - "LP-040 2023: annual sum 50000±0.01; weekday-14 mean / Sunday-03 mean ∈ [2.8, 3.6]; August total &lt; July total; Dec 25 hourly load == Dec 26 hourly load"
    - "tests/golden/consumer_load_2023.sha256 is committed (payload SHA-256 of sorted 2023 load_mwh float64) and asserted"
    - "LP-041: no negatives/NULLs; every calendar hour once; DST 23/25"
    - "LP-042: annual_consumption_mwh 50001 yields a different checksum"
    - "two consecutive build_profile calls on the same calendar+cfg produce identical checksums"
  artifacts:
    - tests/golden/consumer_load_2023.sha256
    - src/epra/consumer/profile.py
    - tests/unit/test_profile.py
  key_links:
    - "EN-072: do not silently regenerate the golden later without human approval"
  prohibitions:
    - "do not add config/consumer_profile_flat.yaml"
    - "do not hash parquet file bytes (metadata flake)"
    - "do not invent LP-040 numbers — compute them"
---

<objective>
Finish LP-030 flat_baseload and the LP-040..042 golden/property/meta tests, committing the 2023 checksum in this PR.

Purpose: T4.04 — ROADMAP SC#1.
Output: golden file + passing LP-040..042.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-05-m4-consumer-profile/05-RESEARCH.md
</execution_context>

<context>
@docs/SPEC-03_consumer_load_profile.md §5 §7
@.planning/phases/EPRA-05-m4-consumer-profile/05-CONTEXT.md D-06 D-07
@.planning/phases/EPRA-05-m4-consumer-profile/05-RESEARCH.md Pattern 4
</context>

<tasks>

<task type="auto">
  <name>Task 1: flat_baseload + LP-040..042 goldens (TDD)</name>
  <read_first>
    - docs/SPEC-03_consumer_load_profile.md LP-030 LP-040 LP-041 LP-042
    - .planning/phases/EPRA-05-m4-consumer-profile/05-RESEARCH.md Pattern 4 Pitfall 6
    - src/epra/consumer/profile.py
  </read_first>
  <files>src/epra/consumer/profile.py, tests/unit/test_profile.py, tests/golden/consumer_load_2023.sha256</files>
  <action>
    Ensure hourly_weights returns ones for profile_name flat_baseload then the same normalizer (full years still 50000±0.01). Implement checksum helper hashing sorted 2023 load_mwh float64 little-endian bytes (merge calendar for year_local). First green run: write tests/golden/consumer_load_2023.sha256 (hex + newline) and assert. LP-040 ratio uses local weekday hour_local==14 vs Sunday hour_local==3 (dow_local==6). LP-042 via cfg.model_copy annual_consumption_mwh=50001. Run the 2023 checksum twice in one test (identity).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_profile.py -m "not live" --no-cov -q && test -s tests/golden/consumer_load_2023.sha256</automated>
  </verify>
  <acceptance_criteria>
    - All LP-040 clauses pass; golden file committed and matches helper output.
    - LP-041 and LP-042 pass; flat_baseload has no second YAML.
    - Two in-process rebuilds yield the same digest.
  </acceptance_criteria>
  <done>Golden contract is in git; shape vs flat variant both work.</done>
</task>

</tasks>
