---
phase: EPRA-05-m4-consumer-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ADR/ADR-012_maintenance-week-sg04.md
  - docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md
  - src/epra/consumer/profile.py
  - tests/unit/test_profile.py
autonomous: true
requirements:
  - REQ-LP-01
  - LP-001
  - LP-002
  - SG-04
must_haves:
  truths:
    - "ADR-012 records SG-04: first Monday m ≥ 1 August, window [m, m+6]; 1 Aug Monday → 1–7 Aug"
    - "hourly_weights is vectorized (no itertuples/apply over hours) and uses only cfg numerics — grep -E '0\\.18|0\\.60|1\\.06' src/ finds nothing"
    - "day_type precedence: shutdown window > is_holiday_at → weekend > Sat/Sun → weekend > weekday; maintenance dates KEEP weekday/weekend day_type"
    - "2022-08-01 through 2022-08-07 are maintenance; 2022-08-08 is not; a Styrian holiday Monday gets weekend shape"
    - "Dec 25 and Dec 26 weights are equal (both shutdown shape × identity special factor)"
    - "unknown profile_name raises ValueError; styriametal_v1 uses YAML shapes; flat_baseload short-circuit may wait until 05-04 but unknown names must already fail"
  artifacts:
    - docs/ADR/ADR-012_maintenance-week-sg04.md
    - src/epra/consumer/profile.py
    - tests/unit/test_profile.py
  key_links:
    - "05-02 normalize_by_local_year consumes hourly_weights"
    - "calendar.build_calendar supplies columns; do not re-derive holidays or peak"
  prohibitions:
    - "do not hardcode 0.18, 0.60, or 1.06 in src/"
    - "do not query DuckDB or read weather"
    - "do not implement parquet I/O or SSOT yet (05-03)"
    - "do not persist the golden sha256 yet (05-04)"
    - "do not mutate config/consumer_profile.yaml"
---

<objective>
Implement SPEC-03 §2 steps 1–4 as a vectorized weight engine (`day_type` / `special_factor` / `hourly_weights`) plus ADR-012 (SG-04 maintenance week). `build_profile` / `monthly_volumes` may remain NotImplementedError until 05-02/05-03.

Purpose: T4.01 — without correct weights, normalization and goldens cannot pass.
Output: ADR-012, weight functions, rule tests.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-05-m4-consumer-profile/05-CONTEXT.md
@/workspace/.planning/phases/EPRA-05-m4-consumer-profile/05-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/EPRA-05-m4-consumer-profile/05-CONTEXT.md
@.planning/phases/EPRA-05-m4-consumer-profile/05-RESEARCH.md
@.planning/phases/EPRA-05-m4-consumer-profile/05-PATTERNS.md
@docs/SPEC-03_consumer_load_profile.md
@docs/EXECUTION_BLUEPRINT/03_MODULES.md
@docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md
@src/epra/ingest/calendar.py
@src/epra/common/config.py
@config/consumer_profile.yaml
@docs/ADR/ADR-006_validation-gate-scope-local-year.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-012 SG-04 maintenance week</name>
  <read_first>
    - docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md (SG-04 row)
    - docs/ADR/ADR-006_validation-gate-scope-local-year.md (heading template)
    - docs/SPEC-03_consumer_load_profile.md §3.3
    - .planning/phases/EPRA-05-m4-consumer-profile/05-CONTEXT.md D-02
  </read_first>
  <files>docs/ADR/ADR-012_maintenance-week-sg04.md, docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md</files>
  <action>
    Author ADR-012 with Context / Decision / Consequences / Spec deviations. Decision: first Monday m with m ≥ August 1; maintenance window = [m, m+6] inclusive; if 1 August is Monday the window is 1–7 August (always ends ≤ 13 Aug). Cite SPEC-03 §3.3, SG-04, D-02. Mark SG-04 status `adopted (ADR-012)` in 14_SPEC_GAPS.md.
  </action>
  <verify>
    <automated>test -f docs/ADR/ADR-012_maintenance-week-sg04.md && rg -n "adopted \\(ADR-012\\)" docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md</automated>
  </verify>
  <acceptance_criteria>
    - ADR-012 exists with the four standard sections and states the first-Monday-on-or-after-1-August rule including the 1 Aug Monday edge.
    - 14_SPEC_GAPS.md SG-04 status is adopted (ADR-012).
  </acceptance_criteria>
  <done>SG-04 is an accepted ADR, not a silent interpretation.</done>
</task>

<task type="auto">
  <name>Task 2: TDD weight engine + rule tests (LP §2 steps 1–4)</name>
  <read_first>
    - docs/SPEC-03_consumer_load_profile.md §2 and §3
    - docs/EXECUTION_BLUEPRINT/03_MODULES.md epra.consumer.profile
    - .planning/phases/EPRA-05-m4-consumer-profile/05-RESEARCH.md Pattern 1 and Pitfalls 1–4
    - tests/unit/test_calendar.py (module-scoped build_calendar fixture)
    - src/epra/consumer/profile.py (stub)
  </read_first>
  <files>tests/unit/test_profile.py, src/epra/consumer/profile.py</files>
  <action>
    Write tests first, then implement. Public names from 03_MODULES: `day_type`, `special_factor`, `hourly_weights`. Vectorize the hot path (boolean masks + 24-length numpy shape tables + 12-length seasonal array). `day_type` may accept a single row for unit tests; `hourly_weights` must not loop Python over hours.

    Rules to test against a `build_calendar(load_settings(), end=date(2027,12,31))` fixture (or a slimmer fixed-end covering 2022–2023):
    - 2022-08-01..07: special_factor == cfg.maintenance.factor; day_type is weekday or weekend by dow/holiday, never shutdown
    - 2022-08-08: special_factor == 1.0
    - A holiday Monday (e.g. 2023-05-01 if Monday, or another known Styrian holiday weekday): day_type weekend
    - Christmas: Dec 25 and Dec 26 2023 weights equal; day_type shutdown; special_factor 1.0
    - Jan 1 is shutdown and belongs to the new local year (year_local check on calendar)
    - Unknown profile_name → ValueError
    - Guide 5.4 intuition: weekday hour 14 in a non-special March day weight == weekday[14] * seasonal[3] (read from cfg, not literals in src)

    Christmas window from cfg.christmas_shutdown "MM-DD" strings. Maintenance from ADR-012 helper (e.g. `maintenance_dates_for_year(year, cfg)`).

    Leave `build_profile` / `monthly_volumes` raising NotImplementedError so stub tests still pass.

    Docstrings: `Implements: LP-001, LP-002, SPEC-03 §2 steps 1-4, ADR-012`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_profile.py -m "not live" --no-cov -q && uv run ruff check src/epra/consumer/profile.py tests/unit/test_profile.py && ! rg -n "0\\.18|0\\.60|1\\.06" src/</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_profile.py` green.
    - `rg -n '0\.18|0\.60|1\.06' src/` finds no matches.
    - `hourly_weights` has no `itertuples` / `iterrows` / `apply(` over the calendar (source assertion).
    - Stub tests for `build_profile` / `monthly_volumes` still raise NotImplementedError matching M4.
  </acceptance_criteria>
  <done>Weight engine matches SPEC-03 steps 1–4 and SG-04; YAML numerics stay in config.</done>
</task>

</tasks>
