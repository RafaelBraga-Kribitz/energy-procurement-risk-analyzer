---
phase: EPRA-05-m4-consumer-profile
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/epra/consumer/profile.py
  - tests/unit/test_profile.py
autonomous: true
requirements:
  - REQ-LP-01
  - LP-004
  - LP-034
must_haves:
  truths:
    - "normalize_by_local_year produces per-full-local-year sums of annual_consumption_mwh ± 0.01 MWh including a DST year (2020 or 2024)"
    - "LP-034: a Jan–Jun 2023 calendar slice yields monthly volumes matching the corresponding months of a full-2023 run"
    - "build_profile(calendar_df, cfg) returns ts_utc, load_mwh — every calendar hour once, no NaN, no negatives"
    - "incomplete years use hypothetical full-year Σw from build_calendar(end=date(Y,12,31)) filtered to year_local==Y (05-RESEARCH Pattern 2)"
  artifacts:
    - src/epra/consumer/profile.py
    - tests/unit/test_profile.py
  key_links:
    - "05-03 writes parquet from build_profile output"
  prohibitions:
    - "do not silently drop hours if calendar is gappy — ValueError (03_MODULES)"
    - "do not normalize by UTC year (T-1 / Pitfall 9)"
    - "do not add I/O yet"
---

<objective>
Implement LP-004 / LP-034 normalization and `build_profile` orchestration so full local years sum to 50 GWh ± 0.01 and partial windows keep monthly volumes consistent with a 50 GWh year shape.

Purpose: T4.02.
Output: `normalize_by_local_year`, working `build_profile` (still no parquet).
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-05-m4-consumer-profile/05-RESEARCH.md
</execution_context>

<context>
@.planning/phases/EPRA-05-m4-consumer-profile/05-CONTEXT.md
@.planning/phases/EPRA-05-m4-consumer-profile/05-RESEARCH.md
@docs/SPEC-03_consumer_load_profile.md
@docs/EXECUTION_BLUEPRINT/03_MODULES.md
@src/epra/ingest/calendar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: normalize_by_local_year + build_profile (TDD)</name>
  <read_first>
    - docs/SPEC-03_consumer_load_profile.md LP-004, LP-034, §2 step 5
    - docs/EXECUTION_BLUEPRINT/05_IMPLEMENTATION_GUIDES.md §5.4 partial-year paragraph
    - .planning/phases/EPRA-05-m4-consumer-profile/05-RESEARCH.md Pattern 2, Pitfall 5
    - src/epra/consumer/profile.py (05-01 weights)
  </read_first>
  <files>src/epra/consumer/profile.py, tests/unit/test_profile.py</files>
  <action>
    Add `normalize_by_local_year(weights, calendar_df, cfg) -> pd.Series` and `build_profile`. Public output columns exactly `ts_utc, load_mwh`. Align weights to calendar index/order.

    Completeness: a local year is complete iff calendar covers that year's first and last local hours (use calendar min/max `date_local`/`hour_local` or ts range after Vienna conversion already on the frame). Incomplete → Σw_full from `hourly_weights(build_calendar(load_settings(), end=date(year,12,31)).query("year_local == year"), cfg)`.

    Empty calendar or duplicate `ts_utc` → ValueError. Delete the `build_profile` row from `test_stubs_fail_loudly.py` in this commit (function no longer raises). Leave `monthly_volumes` stub until 05-03.

    Tests: 2019, 2020 (leap/DST), 2023 full years sum to 50000 ± 0.01; LP-034 2023-01-01..2023-06-30 slice vs full-year monthly sums for months 1–6 (compute monthly from merged year_local/month_local); LP-041-ish: no NaN/negatives; 2024-03-31 row count 23, 2024-10-27 row count 25 matching calendar.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_profile.py tests/unit/test_stubs_fail_loudly.py -m "not live" --no-cov -q && uv run ruff check src/epra/consumer/profile.py tests/unit/test_profile.py && uv run mypy src/epra/consumer/profile.py</automated>
  </verify>
  <acceptance_criteria>
    - Full local years 2019/2020/2023 sum to 50000 ± 0.01 MWh.
    - LP-034 6-month 2023 volumes match full-year months 1–6.
    - `build_profile` is absent from STUBS in test_stubs_fail_loudly.py; remaining M4 stub is monthly_volumes only.
    - DST days 2024-03-31 / 2024-10-27 have 23 / 25 profile rows.
  </acceptance_criteria>
  <done>Normalization matches LP-004/LP-034; build_profile is a real function.</done>
</task>

</tasks>
