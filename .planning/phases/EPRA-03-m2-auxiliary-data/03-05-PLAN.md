---
phase: EPRA-03-m2-auxiliary-data
plan: 05
type: tdd
wave: 4
depends_on:
  - 03-04
files_modified:
  - src/epra/ingest/oespi.py
  - tests/unit/test_oespi.py
  - tests/fixtures/oespi/synthetic_oespi_monthly.csv
  - src/epra/ingest/validate.py
  - tests/unit/test_ingest_gates.py
  - docs/ADR/ADR-008_oespi-series-methodology.md
  - Makefile
  - tests/unit/test_stubs_fail_loudly.py
  - LIMITATIONS.md
autonomous: true
requirements:
  - REQ-ING-01
  - ING-100
  - ING-102
  - ING-103
  - ING-104
must_haves:
  truths:
    - load_oespi loads data/manual/oespi_monthly.csv, validates the ING-100 schema (month, oespi_base, oespi_peak, source_url, retrieved_at), and returns a month-indexed frame; schema drift raises (no silent acceptance)
    - load_oespi asserts a single constant source_url across the series (never splice two methods/series, D-01) and sets peak_available=False with the ING-104 base-only fallback when oespi_peak is absent (D-02)
    - gate_ing_103 checks continuity (no month gaps), positivity, 2022 peak >= 3x the 2019 mean (crisis visibility), and month-over-month change within +/-60%, each with a failing synthetic case (D-05)
    - the committed synthetic_oespi_monthly.csv exercises every ING-103 fail case (month gap, negative value, 2022 peak <3x 2019 mean, >60% MoM jump)
    - ADR-008 pins ONE AEA series/source page as the sole transcription source for 2019->latest, pending human confirmation at transcription time (D-01/D-04)
  artifacts:
    - src/epra/ingest/oespi.py
    - tests/fixtures/oespi/synthetic_oespi_monthly.csv
    - src/epra/ingest/validate.py
    - docs/ADR/ADR-008_oespi-series-methodology.md
  key_links:
    - load_oespi + gate_ing_103 are built and unit-tested against the committed synthetic CSV; the real reconciled CSV is a human checkpoint (03-06), not a ship blocker (D-05)
    - gate_ing_103 extends the validate.py GateResult framework (pure, mirrors gate_ing_082)
  prohibitions:
    - never splice two ÖSPI methods/series — one consistent series only; assert source_url constant (D-01, RESEARCH Pitfall 2)
    - never auto-fix or silently correct a malformed ÖSPI row — raise and require re-reading the source (P-3, A-2)
    - do not treat the Dec-2023 "Indices 2.0" family as usable for 2019->present (RESEARCH Anti-Pattern)
    - do not block shipping on the real reconciled CSV — loader + gates ship against the synthetic CSV (D-05/D-06)
---

<objective>
Implement the ÖSPI loader (SPEC-01 §10, ING-100/102/104) and the pure `gate_ing_103` series gates, both built and unit-tested against a committed synthetic CSV that exercises every gate's fail case (D-05). Draft ADR-008 pinning ONE AEA series as the transcription source (D-01/D-04). The real reconciled `oespi_monthly.csv` is a human checkpoint (03-06), never a ship blocker.

Purpose: ROADMAP Phase 3 criteria 1 & 2 — ÖSPI loader/gates ship green; real double-entry data lands via the human checkpoint.
Output: `load_oespi`, `gate_ing_103`, the synthetic CSV fixture, gate + loader tests, ADR-008, a LIMITATIONS.md note (base-only fallback), and the Makefile `oespi` target.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/graphs/GRAPH_REPORT.md
@.planning/phases/EPRA-03-m2-auxiliary-data/03-CONTEXT.md
@.planning/phases/EPRA-03-m2-auxiliary-data/03-RESEARCH.md
@src/epra/ingest/oespi.py
@scripts/oespi_reconcile.py
@src/epra/ingest/validate.py
@tests/unit/test_ingest_gates.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: load_oespi loader + synthetic CSV (schema, single-series, base-only fallback)</name>
  <read_first>
    - src/epra/ingest/oespi.py (module docstring — ING-100 schema, ING-102 methodology-break warning, ING-104 base-only)
    - scripts/oespi_reconcile.py (EXPECTED_COLUMNS = month, oespi_base, oespi_peak, source_url, retrieved_at; VALUE_COLUMNS — the exact-column-match precedent)
    - docs/SPEC-01_data_ingestion.md §10 (ÖSPI ING-100..104 binding contract)
    - .planning/phases/EPRA-03-m2-auxiliary-data/03-RESEARCH.md Summary + Pitfall 2 (single "old-method" series covers 2019->present with Base+Peak; never splice Indices 2.0)
  </read_first>
  <files>src/epra/ingest/oespi.py, tests/unit/test_oespi.py, tests/fixtures/oespi/synthetic_oespi_monthly.csv</files>
  <behavior>
    - load_oespi(settings) reads settings.paths.data_manual / "oespi_monthly.csv", validates columns == [month, oespi_base, oespi_peak, source_url, retrieved_at] (ING-100); wrong columns raise ContractError/ValueError (schema drift, never silent).
    - Returns a month-indexed frame (month parsed YYYY-MM) with float oespi_base and oespi_peak (or NaN where peak absent).
    - Single-series invariant: source_url must be constant across all rows; a mid-series change raises (never splice, D-01).
    - Base-only fallback: when oespi_peak is entirely absent/blank, load_oespi sets a peak_available=False signal and returns base-only without failing (ING-104, D-02); when peak present throughout, peak_available=True.
    - Never mutates/fills a malformed value — a non-numeric or negative-where-impossible row raises (P-3).
    - A test loader path can accept an explicit csv path so tests use the committed synthetic fixture, not data/manual.
  </behavior>
  <action>
    Implement `load_oespi(settings, *, csv_path=None) -> pd.DataFrame` (default path `settings.paths.data_manual/"oespi_monthly.csv"`; `csv_path` seam for tests) with `Implements: ING-100, ING-102, ING-104` docstring. Read via `pandas.read_csv` with explicit dtypes; assert the exact ING-100 column list (reuse the `oespi_reconcile.EXPECTED_COLUMNS` ordering as the source of truth); parse `month` to a period/timestamp index; coerce `oespi_base`/`oespi_peak` to float raising on non-numeric (no coercion-to-NaN of a real bad value). Assert `source_url.nunique() == 1` and raise a methodology-splice error naming the differing months otherwise (D-01, Pitfall 2). Detect base-only (`oespi_peak` all-NaN/blank) → attach `peak_available=False` (a frame attribute or an accompanying return signal consistent with how SPEC-05/ING-104 will consume it) and skip peak-dependent handling; else `peak_available=True`. Author `tests/fixtures/oespi/synthetic_oespi_monthly.csv` with a clean Base+Peak core series 2019->2023 that a downstream gate would PASS, PLUS separate small malformed fixtures (or parametrized rows) for the failure tests. Write `tests/unit/test_oespi.py`: `test_load_oespi_schema_drift_raises`, `test_load_oespi_base_only_fallback` (peak column blank → peak_available False, no raise), a single-source_url-splice-raises test, and a malformed-value-raises test.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run pytest tests/unit/test_oespi.py -m "not live" -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_oespi.py::test_load_oespi_schema_drift_raises -x` and `::test_load_oespi_base_only_fallback -x` exit 0.
    - Source assertion: load_oespi asserts `source_url` is constant (grep shows a nunique/consistency check) and raises on non-numeric values (no silent `errors="coerce"` on real value columns).
    - `tests/fixtures/oespi/synthetic_oespi_monthly.csv` has exactly the ING-100 columns.
  </acceptance_criteria>
  <done>load_oespi enforces the ING-100 schema, single-series invariant, and ING-104 base-only fallback against the synthetic CSV; never auto-fixes a bad row.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: pure gate_ing_103 series gates with every fail case</name>
  <read_first>
    - src/epra/ingest/validate.py (GateResult/ValidationReport; gate_ing_082 pure-gate style; empty-input returns passed=False precedent)
    - tests/unit/test_ingest_gates.py (synthetic pass/fail case pattern)
    - src/epra/ingest/oespi.py (load_oespi frame shape from Task 1)
  </read_first>
  <files>src/epra/ingest/validate.py, tests/unit/test_ingest_gates.py, tests/fixtures/oespi/synthetic_oespi_monthly.csv</files>
  <behavior>
    - gate_ing_103(oespi) is pure (no I/O): GateResult("ING-103", passed, summary, evidence).
    - Continuity: months are consecutive with no gaps (a missing month fails).
    - Positivity: oespi_base (and oespi_peak when present) strictly positive (a negative/zero fails).
    - Crisis visibility: the 2022 peak (max monthly value in 2022) >= 3x the 2019 mean.
    - MoM change: month-over-month relative change within +/-60% (a larger jump fails).
    - Empty input returns passed=False (no vacuous pass, A-2).
    - Each of the four checks has >=1 passing and >=1 failing synthetic case.
  </behavior>
  <action>
    Add `gate_ing_103` to `validate.py` next to the ING-08x/094 gates, pure and mirroring `gate_ing_082`, producing a single GateResult aggregating the four sub-checks with an evidence frame listing which check failed and where. Reuse the clean core of the synthetic CSV as the passing case; add targeted failing fixtures/rows (a month gap, a negative base, a 2022 peak below 3x the 2019 mean, and a >60% MoM jump) for the failing cases. In `tests/unit/test_ingest_gates.py` add `test_gate_ing_103_passes_clean_series` and `test_gate_ing_103_*` failing tests (one per sub-check). Do NOT widen any threshold to make a case pass — a failing gate is a signal (A-2/EN-061).
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run pytest tests/unit/test_ingest_gates.py -k 103 -m "not live" -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_ingest_gates.py -k 103 -x` exits 0 with a passing case and one failing case for each of continuity, positivity, 2022-peak-3x, and MoM-60%.
    - Source assertion: `gate_ing_103` returns `GateResult("ING-103", ...)` and returns passed=False on empty input.
  </acceptance_criteria>
  <done>gate_ing_103 is a pure gate catching every ING-103 fail case on the committed synthetic CSV.</done>
</task>

<task type="auto">
  <name>Task 3: ADR-008 draft, oespi main/CLI, Makefile, LIMITATIONS note, stub removal</name>
  <read_first>
    - docs/ADR/ADR-005_latest-complete-month-sg02.md (M1 ADR format to mirror)
    - .planning/phases/EPRA-03-m2-auxiliary-data/03-RESEARCH.md Open Question 2 + Sources (AEA "old method" strompreisindex page URL; provisional pick pending T2.05)
    - LIMITATIONS.md (existing structure — add the base-only-fallback note if applicable)
    - src/epra/ingest/oespi.py main stub; Makefile M2 section; tests/unit/test_stubs_fail_loudly.py (oespi rows)
  </read_first>
  <files>docs/ADR/ADR-008_oespi-series-methodology.md, src/epra/ingest/oespi.py, Makefile, LIMITATIONS.md, tests/unit/test_stubs_fail_loudly.py</files>
  <action>
    Author `docs/ADR/ADR-008_oespi-series-methodology.md` (next free number, confirmed against docs/ADR/) mirroring the M1 ADR structure: context (ING-102 methodology-break risk; two AEA families — the continuously-published "old method" strompreisindex series vs the Dec-2023 "Indices 2.0" supplement), decision (pin ONE series/source URL — the RESEARCH-recommended continuously-published Base+Peak series at the AEA strompreisindex page — as the sole 2019->latest transcription source, explicitly flagged "pending human confirmation at T2.05 transcription per D-01/D-04"), and consequences (never splice; base-only fallback recorded here + LIMITATIONS.md if peak turns out discontinuous, D-02). Implement `main(argv)` (`python -m epra.ingest.oespi`, ING-103) that loads settings, calls `load_oespi`, runs `gate_ing_103`, prints/writes the result, and returns non-zero on gate failure. Add a Makefile `oespi` target (`$(UV) run python -m epra.ingest.oespi`) + `.PHONY`. Add a LIMITATIONS.md note stating the ÖSPI series pick is provisional pending T2.05 human confirmation and that base-only fallback (ING-104) applies if monthly Peak is not published for part of the window. Remove the `oespi.load_oespi` and `oespi.main` M2 rows from `STUBS` in `tests/unit/test_stubs_fail_loudly.py`.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run pytest tests/unit/test_stubs_fail_loudly.py -m "not live" && test -f docs/ADR/ADR-008_oespi-series-methodology.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ADR/ADR-008_oespi-series-methodology.md` exists, pins ONE series/source URL, and is flagged pending T2.05 human confirmation (D-01/D-04).
    - `test_stubs_fail_loudly.py` no longer references `oespi.load_oespi` or `oespi.main`; suite passes.
    - `make oespi` target present; LIMITATIONS.md notes the provisional pick + ING-104 base-only fallback.
  </acceptance_criteria>
  <done>ADR-008 drafted with next-free number and single-series decision, oespi CLI wired, LIMITATIONS updated, oespi stubs removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human-edited CSV → load_oespi | untrusted, hand-transcribed `data/manual/oespi_monthly.csv` crosses into the loader and gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | ÖSPI CSV rows | high | mitigate | `oespi_reconcile.py` exact-column check + `load_oespi` dtype/schema validation catch malformed rows; never auto-fix — raise and require re-reading the source (P-3, A-2) |
| T-03-09 | Tampering | methodology splice | medium | mitigate | `load_oespi` asserts `source_url` constant across the series; a mid-series change raises (D-01, RESEARCH Pitfall 2) — the ING-102 methodology-break guard |
| T-03-11 | Tampering | ING-103 threshold integrity | medium | mitigate | Crisis/MoM/positivity bands are fixed; a failing gate raises (EN-061), never widened without an ADR (A-2) |
| T-03-SC | Tampering | pip installs | low | accept | No new packages — stdlib `csv` + pinned `pandas` only (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `epra.ingest.oespi.load_oespi(settings, *, csv_path=None)` — ING-100/102/104 loader with peak_available signal (new).
- `epra.ingest.oespi.main(argv)` — CLI running load + gate_ing_103 (new).
- `epra.ingest.validate.gate_ing_103` — pure ÖSPI series gate (new).
- `tests/fixtures/oespi/synthetic_oespi_monthly.csv` (+ malformed variants) — every ING-103 fail case (new).
- `tests/unit/test_oespi.py` and `test_gate_ing_103_*` cases (new).
- `docs/ADR/ADR-008_oespi-series-methodology.md` (new, next-free ADR number).
- Makefile `oespi` target; LIMITATIONS.md base-only-fallback note; removal of `oespi.*` rows from `test_stubs_fail_loudly.py`.
</artifacts_produced>

<verification>
- `uv run pytest tests/unit/test_oespi.py tests/unit/test_ingest_gates.py tests/unit/test_stubs_fail_loudly.py -m "not live"` green.
- ADR-008 present with next-free number; LIMITATIONS.md updated.
- `uv run ruff check src/epra/ingest/oespi.py src/epra/ingest/validate.py tests/unit/test_oespi.py` and `uv run mypy` clean.
- The real reconciled `data/manual/oespi_monthly.csv` (double-entry) is the 03-06 human checkpoint (D-05/D-06) — NOT required for this plan to seal.
</verification>

<success_criteria>
load_oespi and gate_ing_103 ship green against the committed synthetic CSV covering every fail case, ADR-008 pins one series (pending human confirmation), and no code path splices series or auto-fixes a bad row.
</success_criteria>

<output>
Create `.planning/phases/EPRA-03-m2-auxiliary-data/03-05-SUMMARY.md` when done.
</output>