---
phase: EPRA-03-m2-auxiliary-data
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/epra/ingest/calendar.py
  - tests/unit/test_calendar.py
  - Makefile
  - tests/unit/test_stubs_fail_loudly.py
autonomous: true
requirements:
  - REQ-ING-01
  - ING-110
  - ING-111
must_haves:
  truths:
    - build_calendar returns one row per UTC hour from 2019-01-01 to the forward-window end with the ING-110 columns (ts_utc, date_local, hour_local, dow_local, is_weekend, is_holiday_at, is_peak_hour, year_local, month_local)
    - DST spring-forward local day has 23 rows and DST fall-back local day has 25 rows (counted via timeutil, never hand-derived)
    - is_peak_hour values come from epra.common.timeutil.is_peak_hour (holiday-aware), not re-implemented in calendar.py (D-10)
    - Styrian holidays come from holidays.Austria(subdiv='6', years=range(2019, end.year+1)); 2024 has the expected national-holiday count and Jan 1 / May 1 / Dec 25 are always is_holiday_at=True (ING-111)
    - default --end is latest_complete_month(settings) + 18 months (D-08/D-09); a fixed --end is accepted only for deterministic tests
    - calendar persists to data/raw/calendar/calendar.parquet as a single file (not monthly-partitioned; does not use _io.write_month)
  artifacts:
    - src/epra/ingest/calendar.py
    - tests/unit/test_calendar.py
    - data/raw/calendar/calendar.parquet
  key_links:
    - calendar imports latest_complete_month from epra.ingest.entsoe for the dynamic default end (D-09)
    - calendar imports is_peak_hour / to_local / VIENNA from epra.common.timeutil (T-1, D-10)
  prohibitions:
    - do not re-implement is_peak_hour or any tz/DST arithmetic in calendar.py (D-10, RESEARCH Anti-Pattern)
    - do not import M6 strategies.yaml / StrategyCfg / load_strategy_config into calendar.py — hardcode _FORWARD_HORIZON_MONTHS=18 with a SG-15/D-08 comment (RESEARCH Pitfall 3)
    - do not hardcode a fixed holiday year list — derive years=range(2019, end.year+1) from the computed end (RESEARCH Pitfall 4)
    - do not compute season / hdd_18 / cdd_22 or any weather join — deferred to M3 (CONTEXT Deferred Ideas)
---

<objective>
Build the hourly UTC calendar spine (SPEC-01 §11, ING-110) with Vienna-local attributes, Styrian public holidays, and peak-hour flags, persisted to `data/raw/calendar/calendar.parquet`, plus the ING-111 correctness tests.

Purpose: ROADMAP Phase 3 success criteria 1 & 2 — the calendar parquet must be present and ING-111 gate-clean. Self-contained: reuses `timeutil` (peak/DST) and `holidays` (subdiv='6'); no external network.
Output: `build_calendar()`, `main()` CLI (`python -m epra.ingest.calendar [--end YYYY-MM-DD]`), the committed parquet, a Makefile `calendar` target, and `test_calendar.py`.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/graphs/GRAPH_REPORT.md
@.planning/phases/EPRA-03-m2-auxiliary-data/03-CONTEXT.md
@.planning/phases/EPRA-03-m2-auxiliary-data/03-RESEARCH.md
@src/epra/ingest/calendar.py
@src/epra/common/timeutil.py
@config/settings.yaml
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: build_calendar hourly spine with DST + Styrian holidays + peak flags (RED→GREEN→REFACTOR)</name>
  <read_first>
    - src/epra/ingest/calendar.py (stub docstring — the ING-110 column list and contract)
    - src/epra/common/timeutil.py (is_peak_hour, to_local, VIENNA, local_hours_in_day — the ONLY sanctioned tz layer, T-1)
    - docs/SPEC-01_data_ingestion.md §11 (Calendar ING-110/111 binding contract) and §7 (calendar output contract)
    - src/epra/ingest/entsoe.py latest_complete_month signature (imported for the dynamic default end)
  </read_first>
  <files>src/epra/ingest/calendar.py, tests/unit/test_calendar.py</files>
  <behavior>
    - RED first: write tests/unit/test_calendar.py before implementing build_calendar.
    - Spine: build_calendar(settings, end=<date>) yields one row per UTC hour from 2019-01-01 00:00 UTC through the last UTC hour of `end`; ts_utc is tz-aware UTC.
    - Columns exactly: ts_utc, date_local, hour_local, dow_local (0=Mon), is_weekend (dow>=5), is_holiday_at (bool), is_peak_hour (bool), year_local, month_local.
    - DST: for a known spring-forward Vienna date (last Sunday of March) the local day has 23 distinct local hours; for a known fall-back date (last Sunday of October) it has 25 — asserted by grouping rows by date_local (uses timeutil.local_hours_in_day as the expected count).
    - ING-111 holiday count: holidays.Austria(subdiv='6', years=range(2019, end.year+1)); the count of distinct is_holiday_at=True local dates in calendar year 2024 equals holidays.Austria(subdiv='6', years=2024) length; Jan 1, May 1, Dec 25 of 2024 are is_holiday_at=True.
    - ING-111 peak: a known non-holiday Monday 10:00 local is is_peak_hour=True; a known Sunday is is_peak_hour=False; a holiday weekday 10:00 is is_peak_hour=False (holiday-aware via timeutil).
    - SG-10 subdiv assertion: the test asserts the working Styria subdivision code is '6' (holidays.Austria.subdivisions contains '6').
  </behavior>
  <action>
    Implement `build_calendar(settings: Settings, end: date | None = None) -> pd.DataFrame` with `Implements: ING-110` docstring. Define a module-local constant `_FORWARD_HORIZON_MONTHS = 18` with a comment citing SG-15 / D-08 and the "12-month forward sim + 6-month cushion" derivation — do NOT import M6 config. When `end is None`, compute the default as `latest_complete_month(settings)` (imported from `epra.ingest.entsoe`) advanced by `_FORWARD_HORIZON_MONTHS` months (use `pd.DateOffset(months=...)` / `dateutil`-free month arithmetic consistent with the codebase). Build the hourly index with `pd.date_range(start="2019-01-01", end=<end-of-day of `end`>, freq="h", tz="UTC")`. Derive local attributes by converting each ts_utc to Europe/Vienna via `timeutil.to_local` (or `.dt.tz_convert(VIENNA)`), then: `date_local` = local date, `hour_local` = local hour, `dow_local` = local weekday (Mon=0), `is_weekend` = dow_local >= 5. Compute holidays via `holidays.Austria(subdiv="6", years=range(2019, end.year + 1))`; `is_holiday_at` = local date in that set. Compute `is_peak_hour` by applying `timeutil.is_peak_hour(local_ts, is_holiday=is_holiday_at)` per row — NEVER re-derive the Mon–Fri 08–20 rule locally (D-10). `year_local`/`month_local` from the local timestamp. Return the frame; persistence is Task 2. Write `tests/unit/test_calendar.py` covering every bullet in `<behavior>`, using a fixed `end` (e.g. `date(2027, 12, 31)`) so the tests never touch `latest_complete_month`/network.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run pytest tests/unit/test_calendar.py -m "not live" -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_calendar.py::test_build_calendar_dst_rows -x` exits 0 (23/25-hour DST days correct).
    - `pytest tests/unit/test_calendar.py -k ing_111 -x` exits 0 (2024 holiday count, Jan1/May1/Dec25 holidays, peak Mon/Sun/holiday, subdiv='6').
    - Source assertion: `grep -n "is_peak_hour" src/epra/ingest/calendar.py` shows only an import/call of `epra.common.timeutil.is_peak_hour`, no local re-definition of the peak rule.
    - Source assertion: `grep -n "strategies\|StrategyCfg\|load_strategy_config" src/epra/ingest/calendar.py` returns nothing.
  </acceptance_criteria>
  <done>build_calendar returns the ING-110 spine with correct DST hour counts, Styrian holidays, and timeutil-sourced peak flags; ING-111 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: calendar CLI, parquet persistence, Makefile target, stub removal</name>
  <read_first>
    - src/epra/ingest/calendar.py (build_calendar from Task 1; main stub signature)
    - src/epra/ingest/entsoe.py (argparse `--start/--end` CLI + `_parse_cli_date` pattern to mirror)
    - Makefile (existing backfill / validate-ingest targets — placement + `$(UV) run python -m ...` style)
    - tests/unit/test_stubs_fail_loudly.py (STUBS list — the two `cal.build_calendar` / `cal.main` M2 rows to remove)
  </read_first>
  <files>src/epra/ingest/calendar.py, Makefile, tests/unit/test_stubs_fail_loudly.py, tests/unit/test_calendar.py</files>
  <action>
    Implement `main(argv)` with an `argparse` parser accepting an optional `--end YYYY-MM-DD` (default `None` → dynamic `latest_complete_month()+18mo` per D-09), reusing the `entsoe._parse_cli_date` YYYY-MM-DD validation style. `main` calls `build_calendar(settings, end)`, writes the frame to `settings.paths.data_raw / "calendar" / "calendar.parquet"` (resolve against REPO_ROOT like `_io._data_raw_root`), creating parent dirs, using `to_parquet(index=False, engine="pyarrow")` — a SINGLE file, NOT monthly-partitioned and NOT via `_io.write_month`. Log rows written and return 0. Add a Makefile `calendar` target under the M2 section: `$(UV) run python -m epra.ingest.calendar` (add `calendar` to `.PHONY`). Remove the two M2 rows for `cal.build_calendar` and `cal.main` from the `STUBS` list in `tests/unit/test_stubs_fail_loudly.py`. Add a `test_calendar.py` test that invokes `main(["--end", "2027-12-31"])`, asserts exit 0, and asserts the parquet exists with the ING-110 columns.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run pytest tests/unit/test_calendar.py tests/unit/test_stubs_fail_loudly.py -m "not live" && uv run python -m epra.ingest.calendar --end 2027-12-31 && test -f data/raw/calendar/calendar.parquet</automated>
  </verify>
  <acceptance_criteria>
    - `python -m epra.ingest.calendar --end 2027-12-31` exits 0 and writes `data/raw/calendar/calendar.parquet`.
    - `test_stubs_fail_loudly.py` no longer references `cal.build_calendar` or `cal.main` and the suite passes (the removed stubs no longer raise NotImplementedError).
    - `make calendar` target present in Makefile `.PHONY` and body.
  </acceptance_criteria>
  <done>Calendar CLI persists the single parquet, Makefile wired, calendar stubs removed, suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI `--end` → build_calendar | operator-supplied end date controls output size; no external/network input in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Denial of Service | build_calendar horizon | low | mitigate | Default end is bounded (`latest_complete_month()+18mo`); an unbounded far-future `--end` is an operator choice, and `_parse_cli_date` rejects malformed dates before any allocation |
| T-03-04 | Tampering | holidays year range | low | mitigate | `years=range(2019, end.year+1)` derived from the computed end (no stale hardcoded list, RESEARCH Pitfall 4); holiday data from the pinned `holidays` package, not a hand table |
| T-03-SC | Tampering | pip installs | low | accept | No new packages — `holidays>=0.50` (0.100 installed), `pandas`/`pyarrow` already pinned/vetted in M1 (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `epra.ingest.calendar.build_calendar(settings, end=None)` — ING-110 hourly spine (new).
- `epra.ingest.calendar.main(argv)` — CLI with dynamic default `--end` (new).
- Module constant `_FORWARD_HORIZON_MONTHS = 18` (SG-15/D-08).
- `data/raw/calendar/calendar.parquet` — single-file calendar output.
- `tests/unit/test_calendar.py` — DST + ING-111 + CLI tests (new file).
- Makefile `calendar` target.
- Removal of `cal.build_calendar` / `cal.main` rows from `tests/unit/test_stubs_fail_loudly.py`.
</artifacts_produced>

<verification>
- `uv run pytest tests/unit/test_calendar.py tests/unit/test_stubs_fail_loudly.py -m "not live"` green.
- `uv run python -m epra.ingest.calendar --end 2027-12-31` writes the parquet.
- `uv run ruff check src/epra/ingest/calendar.py tests/unit/test_calendar.py` and `uv run mypy` clean.
</verification>

<success_criteria>
The calendar hourly spine is present at `data/raw/calendar/calendar.parquet`, DST 23/25-hour days and Styrian holidays are correct, peak flags come from timeutil, and ING-111 tests pass network-free.
</success_criteria>

<output>
Create `.planning/phases/EPRA-03-m2-auxiliary-data/03-02-SUMMARY.md` when done.
</output>
