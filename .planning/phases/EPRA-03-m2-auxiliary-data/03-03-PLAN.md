---
phase: EPRA-03-m2-auxiliary-data
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - src/epra/ingest/geosphere.py
  - tests/unit/test_geosphere.py
  - tests/fixtures/geosphere/metadata.json
  - config/settings.yaml
  - docs/ADR/ADR-007_geosphere-station-selection.md
  - tests/unit/test_stubs_fail_loudly.py
autonomous: true
requirements:
  - REQ-ING-01
  - ING-090
  - ING-091
  - ING-092
must_haves:
  truths:
    - discover_station(settings) returns the Graz station with the longest record, preferring "Graz Universität" on ties, parsed from the GeoSphere station metadata GeoJSON (ING-091)
    - discovery is live-first (D-07); if outbound network is unavailable it falls back to the committed metadata.json fixture and the live pull is recorded as a human checkpoint
    - the committed metadata.json fixture contains "Graz Universität" plus at least one decoy Graz station so the tie-break is exercised deterministically in CI
    - ADR-007 records the chosen station id/name/lat/lon, the tie-break rationale, and the verified klima-v2-1d dataset id (or an ADR'd substitution if it differs)
  artifacts:
    - src/epra/ingest/geosphere.py
    - tests/unit/test_geosphere.py
    - tests/fixtures/geosphere/metadata.json
    - docs/ADR/ADR-007_geosphere-station-selection.md
  key_links:
    - discover_station reads settings.geosphere.base_url / dataset_id and hits /station/historical/<dataset_id>/metadata
    - the chosen station id/name is written into config/settings.yaml geosphere.station_id / station_name (or left null with a human-checkpoint note if live discovery was blocked)
  prohibitions:
    - do not hardcode a station id in code — discovery result lives in config/settings.yaml + ADR-007 (SPEC-01 §9 ING-091)
    - do not gate CI on live network — the metadata.json fixture makes the tie-break test deterministic (D-06/D-07)
    - do not deeply index the GeoJSON before validating its top-level FeatureCollection shape (RESEARCH Security Domain, Pitfall 5)
---

<objective>
Implement GeoSphere station discovery (SPEC-01 §9, ING-091): fetch the `klima-v2-1d` station metadata, pick the Graz station with the longest record (prefer "Graz Universität"), and record the choice in `config/settings.yaml` and ADR-007. Live-first with a committed-fixture fallback so CI is deterministic (D-07).

Purpose: the ingest step (03-04) needs a pinned station id; discovery is the one-time ING-091 step whose *result* is persisted, not re-run per ingest.
Output: `discover_station()` + `StationInfo`, a committed `metadata.json` tie-break fixture, `test_geosphere.py` discovery test, ADR-007, and the filled `geosphere:` config (or a documented human checkpoint if network was blocked).
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/graphs/GRAPH_REPORT.md
@.planning/phases/EPRA-03-m2-auxiliary-data/03-CONTEXT.md
@.planning/phases/EPRA-03-m2-auxiliary-data/03-RESEARCH.md
@src/epra/ingest/geosphere.py
@src/epra/ingest/_fetch.py
@config/settings.yaml
@docs/ADR/ADR-005_latest-complete-month-sg02.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: discover_station with live-first / fixture-fallback and the tie-break test</name>
  <read_first>
    - src/epra/ingest/geosphere.py (stub docstring — ING-091 contract, prefer "Graz Universität")
    - src/epra/ingest/_fetch.py (requests + timeout + retry politeness pattern to mirror; GeoSphere needs its own small no-auth GET, NOT fetch_entsoe)
    - config/settings.yaml geosphere block (base_url, dataset_id, station_id=null, parameter=tl_mittel)
    - src/epra/common/config.py GeosphereCfg model (fields available on settings.geosphere)
  </read_first>
  <files>src/epra/ingest/geosphere.py, tests/unit/test_geosphere.py, tests/fixtures/geosphere/metadata.json</files>
  <action>
    Define a frozen dataclass `StationInfo(id, name, lat, lon, record_start)` and implement `discover_station(settings) -> StationInfo` with `Implements: ING-091`. Build the metadata URL as `f"{settings.geosphere.base_url}/station/historical/{settings.geosphere.dataset_id}/metadata"`; GET it with `requests.get(..., timeout=30)` (no auth, ING-093) and `raise_for_status()`. Before deep indexing, validate the payload is a dict with a `stations` (or GeoJSON `features`) list — raise a clear `ContractError`/`ValueError` if the top-level shape is wrong (Security: malformed/oversized response, Pitfall 5). Filter stations whose name contains "Graz"; choose the one with the longest record (earliest `valid_from`/record-start), breaking ties by preferring the name containing "Graz Universität"; raise a `DiscoveryError`-style exception naming the available candidates if none match. Add a `_load_metadata(settings, *, transport=None)` seam (mirroring `_fetch`'s `transport` injection) so tests pass the committed fixture instead of the network. Create `tests/fixtures/geosphere/metadata.json` containing "Graz Universität" (longest record) plus at least one decoy Graz station (shorter record) and a non-Graz station, in the real GeoSphere metadata shape. Write `tests/unit/test_geosphere.py::test_discover_station_prefers_graz_universitaet` that feeds the fixture and asserts the returned `StationInfo.name` contains "Graz Universität" and the id matches the fixture's longest-record Graz station. Mark any test that would hit the real network with `@pytest.mark.live` (EN-070) — the deterministic test uses only the fixture.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run pytest tests/unit/test_geosphere.py::test_discover_station_prefers_graz_universitaet -m "not live" -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_geosphere.py::test_discover_station_prefers_graz_universitaet -m "not live" -x` exits 0 against the committed fixture.
    - Source assertion: `discover_station` calls `requests.get` with a `timeout=` argument and validates the top-level payload shape before indexing nested keys.
    - `tests/fixtures/geosphere/metadata.json` exists and contains a "Graz Universität" entry plus ≥1 decoy Graz station.
  </acceptance_criteria>
  <done>discover_station picks the correct Graz station from the fixture deterministically; live path is behind the transport seam / @pytest.mark.live.</done>
</task>

<task type="auto">
  <name>Task 2: ADR-007, settings.yaml station fill, and discover_station stub removal</name>
  <read_first>
    - docs/ADR/ADR-005_latest-complete-month-sg02.md (M1 ADR format to mirror — context/decision/consequences)
    - config/settings.yaml geosphere block (station_id / station_name currently null)
    - tests/unit/test_stubs_fail_loudly.py (STUBS — the `geosphere.discover_station` M2 row to remove)
    - .planning/phases/EPRA-03-m2-auxiliary-data/03-CONTEXT.md Claude's Discretion (ADR numbering: ADR-007 is next free)
  </read_first>
  <files>docs/ADR/ADR-007_geosphere-station-selection.md, config/settings.yaml, tests/unit/test_stubs_fail_loudly.py</files>
  <action>
    Attempt a real `discover_station` live call (D-07). If outbound network reaches GeoSphere: write the discovered `station_id`/`station_name` (and note lat/lon) into `config/settings.yaml` under `geosphere:` and record the concrete values in ADR-007. If the agent environment blocks outbound network: leave `station_id`/`station_name` as `null`, record in ADR-007 that the live discovery is a committed human/local checkpoint (operator runs `python -c "from epra.common.config import load_settings; from epra.ingest.geosphere import discover_station; print(discover_station(load_settings()))"` and fills config), and proceed on the fixture. Author `docs/ADR/ADR-007_geosphere-station-selection.md` (next free number, confirmed against docs/ADR/) using the M1 ADR structure: context (ING-091 Graz-station selection + klima-v2-1d dataset-id verification), decision (chosen station or the pending-human-confirmation state, tie-break = longest record then "Graz Universität"), and consequences (station pinned in config, not code; dataset-id substitution ADR'd if klima-v2-1d differs). Remove the `geosphere.discover_station` M2 row from `STUBS` in `tests/unit/test_stubs_fail_loudly.py` (leave the `geosphere.ingest` and `geosphere.main` rows — 03-04 removes those).
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run pytest tests/unit/test_stubs_fail_loudly.py -m "not live" && test -f docs/ADR/ADR-007_geosphere-station-selection.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ADR/ADR-007_geosphere-station-selection.md` exists and states the chosen station (or the pending-human-confirmation state) and the tie-break rationale.
    - `test_stubs_fail_loudly.py` no longer lists `geosphere.discover_station`; it still lists `geosphere.ingest` and `geosphere.main`; suite passes.
    - `config/settings.yaml` geosphere.station_id is either a real id (live succeeded) or `null` with an ADR-007-documented human checkpoint.
  </acceptance_criteria>
  <done>ADR-007 written with next-free number, config reflects discovery result or a documented human checkpoint, discover_station stub removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GeoSphere Data Hub → discover_station | untrusted (public, no-auth) HTTP GeoJSON response crosses into the parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Denial of Service | GeoSphere metadata response parsing | medium | mitigate | `requests.get(..., timeout=30)`; validate top-level FeatureCollection/stations shape before deep-indexing; raise on malformed payload rather than crash (Pitfall 5) |
| T-03-07 | Information Disclosure | GeoSphere transport | low | accept | GeoSphere requires no auth token (ING-093) — no secret exists to leak; if a key is ever introduced, apply `_fetch` env-only discipline |
| T-03-SC | Tampering | pip installs | low | accept | No new packages — reuses `requests` already pinned/vetted in M1 (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `epra.ingest.geosphere.StationInfo` dataclass and `discover_station(settings)` (new, ING-091).
- `_load_metadata` transport seam for live-vs-fixture discovery.
- `tests/fixtures/geosphere/metadata.json` — tie-break fixture (new).
- `tests/unit/test_geosphere.py` with the discovery tie-break test (new file, extended in 03-04).
- `docs/ADR/ADR-007_geosphere-station-selection.md` (new, next-free ADR number).
- `config/settings.yaml` geosphere.station_id/station_name filled (or documented human checkpoint).
- Removal of `geosphere.discover_station` from `tests/unit/test_stubs_fail_loudly.py`.
</artifacts_produced>

<verification>
- `uv run pytest tests/unit/test_geosphere.py tests/unit/test_stubs_fail_loudly.py -m "not live"` green.
- ADR-007 present with next-free number (confirmed against `docs/ADR/`).
- `uv run ruff check src/epra/ingest/geosphere.py tests/unit/test_geosphere.py` and `uv run mypy` clean.
</verification>

<success_criteria>
GeoSphere station discovery selects the correct Graz station deterministically from a committed fixture, records the decision in ADR-007 and config, and defers any live pull to a documented checkpoint — CI never depends on network.
</success_criteria>

<output>
Create `.planning/phases/EPRA-03-m2-auxiliary-data/03-03-SUMMARY.md` when done.
</output>
