---
phase: EPRA-03-m2-auxiliary-data
plan: 06
type: execute
wave: 5
depends_on:
  - 03-02
  - 03-04
  - 03-05
files_modified:
  - src/epra/ingest/validate.py
  - tests/unit/test_ingest_gates.py
autonomous: false
requirements:
  - REQ-ING-01
  - ING-094
  - ING-101
  - ING-103
  - ING-111
must_haves:
  truths:
    - run_gates loads the GeoSphere, ÖSPI, and calendar raw outputs and runs ING-094, ING-103, and ING-111 alongside the M1 ING-080..085 gates, listing every gate exactly once in one report
    - gate_ing_111 is a thin wrapper over build_calendar producing a GateResult so the calendar checks appear in the aggregate validation report
    - make validate-ingest produces reports/ingestion/validation_<date>.md covering all M1+M2 gates and exits non-zero if any fail (EN-061)
    - the real reconciled data/manual/oespi_monthly.csv (double-entry) and a live GeoSphere pull land as committed human/local checkpoints with a report under reports/ingestion/, NOT as CI blockers (D-06)
  artifacts:
    - src/epra/ingest/validate.py
    - reports/ingestion/validation_<date>.md
  key_links:
    - run_gates imports load_oespi, gate_ing_103, gate_ing_094 (via GeoSphere parquet load), build_calendar + gate_ing_111
    - ING-101 double-entry reconciliation runs via scripts/oespi_reconcile.py before load_oespi consumes the reconciled CSV
  prohibitions:
    - do not gate CI on live network or on the real ÖSPI transcription (D-06) — the aggregate report runs on whatever raw data is present, human checkpoint supplies the real data
    - do not silently skip a registered gate — ValidationReport must list every M1+M2 gate exactly once (T-02-13, EN-061)
    - do not auto-approve the human transcription/verification checkpoint (double-entry is a human operation, D-03)
---

<objective>
Assemble the full M2 ingestion validation suite: wire ING-094 (GeoSphere), ING-103 (ÖSPI), and ING-111 (calendar) into `run_gates()` so `make validate-ingest` renders one report over all M1+M2 gates (ROADMAP criterion 3), then run the human checkpoint that supplies the real reconciled ÖSPI CSV and live GeoSphere data (D-03/D-06).

Purpose: ROADMAP Phase 3 criterion 3 — full ingestion validation suite passes for 2019→latest; criterion 2 — `oespi_monthly.csv` double-entry reconciled.
Output: extended `run_gates`, `gate_ing_111` wrapper, the aggregate report on real data committed under `reports/ingestion/`.
</objective>

<execution_context>
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/graphs/GRAPH_REPORT.md
@.planning/phases/EPRA-03-m2-auxiliary-data/03-CONTEXT.md
@.planning/phases/EPRA-03-m2-auxiliary-data/03-RESEARCH.md
@src/epra/ingest/validate.py
@scripts/oespi_reconcile.py
@docs/ADR/ADR-006_validation-gate-scope-local-year.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gate_ing_111 wrapper + wire ING-094/103/111 into run_gates</name>
  <read_first>
    - src/epra/ingest/validate.py (run_gates, _load_hourly, _write_report, ValidationReport "every gate exactly once" invariant; gate_ing_094 and gate_ing_103 added in 03-04/03-05)
    - src/epra/ingest/calendar.py (build_calendar — source for the ING-111 wrapper)
    - src/epra/ingest/oespi.py (load_oespi — source for ING-103 input) and geosphere parquet layout data/raw/geosphere_graz_daily/*/*.parquet
    - tests/unit/test_ingest_gates.py (run_gates aggregate test pattern)
  </read_first>
  <files>src/epra/ingest/validate.py, tests/unit/test_ingest_gates.py</files>
  <behavior>
    - gate_ing_111(calendar_frame) returns GateResult("ING-111", passed, summary, evidence) asserting: 2024 Styrian holiday count matches holidays.Austria(subdiv='6', years=2024); Jan1/May1/Dec25 flagged; peak-hour Mon/Sun correct. Empty input → passed=False.
    - run_gates additionally: loads the geosphere_graz_daily monthly parquet and runs gate_ing_094; loads the reconciled ÖSPI CSV via load_oespi and runs gate_ing_103 (skips gracefully — reports a non-fatal informational result — if data/manual/oespi_monthly.csv is absent, per D-06 so CI without real data still renders); loads/builds the calendar frame and runs gate_ing_111.
    - The report lists every M1 gate (ING-080..085) AND every M2 gate (ING-094, ING-103, ING-111) exactly once.
    - run_gates raises GateFailure naming any failed gate (EN-061).
  </behavior>
  <action>
    Add `gate_ing_111(calendar_frame)` to `validate.py` as a thin pure wrapper over the calendar assertions (reuse the ING-111 logic validated in 03-02; this is the aggregate-report surface resolving RESEARCH Open Question 3 toward report-uniformity). Extend `run_gates`: add a GeoSphere loader (glob `data/raw/geosphere_graz_daily/*/*.parquet`, concat) feeding `gate_ing_094`; call `load_oespi(settings)` guarded so a missing real CSV yields an informational (non-crashing) result rather than an exception (D-06) and feeds `gate_ing_103` when present; build/load the calendar frame for `gate_ing_111`. Register each result via `report.add(...)` so the "every gate exactly once" invariant (T-02-13) holds across all M1+M2 gates. Keep gate functions pure — only `run_gates` does I/O. Update the module docstring `Implements:` line to include ING-094, ING-103, ING-111. Extend `tests/unit/test_ingest_gates.py`: a `gate_ing_111` pass/fail pair, and a `run_gates` aggregate test asserting all M1+M2 gate ids appear exactly once and that a seeded failing gate raises GateFailure.
  </action>
  <verify>
    <automated>cd "C:/Users/Benutzer1/Documents/_DOING/PROJECTS/PORTFOLIO_PROJECTS/IDEAS_YET_NOT_PLANNED/energy-procurement-risk-analyzer" && uv run pytest tests/unit/test_ingest_gates.py -m "not live" -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_ingest_gates.py -m "not live" -x` exits 0, including the gate_ing_111 pair and the run_gates aggregate test.
    - Source assertion: `run_gates` registers ING-080..085, ING-094, ING-103, ING-111 (grep shows each `report.add` / gate call once).
    - A missing `data/manual/oespi_monthly.csv` does NOT crash run_gates (renders an informational ÖSPI result, D-06).
  </acceptance_criteria>
  <done>run_gates renders one report over all M1+M2 gates, gate_ing_111 appears in the aggregate, and the suite is green network-free.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human checkpoint — real ÖSPI double-entry + live GeoSphere pull + validate-ingest on real data</name>
  <action>
    Human-only work (not agent-automatable): confirm the ADR-008 series pick against the live AEA publication, transcribe the ÖSPI series twice (double-entry, D-03), reconcile via scripts/oespi_reconcile.py (ING-101), optionally run the live GeoSphere pull (D-07), then run `make validate-ingest` on real data and commit the resulting report. Do NOT auto-approve — double-entry transcription is a human operation (D-03). See how-to-verify for exact steps.
  </action>
  <what-built>
    Loader/gates/calendar all ship green on synthetic + fixture data (CI network-free, D-06). This checkpoint supplies the REAL data that only a human can produce: the double-entry ÖSPI transcription (D-03) and a live GeoSphere pull (D-07), then runs the full aggregate suite on real 2019→latest data.
  </what-built>
  <how-to-verify>
    1. Confirm the ADR-008 ÖSPI series pick against the live AEA strompreisindex publication (D-01/D-04); correct ADR-008 if the actual publication differs.
    2. Transcribe the monthly ÖSPI series (Base + Peak) TWICE in separate sessions into `data/manual/oespi_monthly_entry1.csv` and `oespi_monthly_entry2.csv` (ING-100 schema), then run `python scripts/oespi_reconcile.py` (ING-101) until it exits 0 and writes the reconciled `data/manual/oespi_monthly.csv`; delete the entry files.
    3. If outbound network is available, fill `config/settings.yaml` geosphere.station_id via `discover_station` (03-03) and run `python -m epra.ingest.geosphere --start 2019-01-01 --end <latest complete month>` for the real daily pull.
    4. Run `make validate-ingest` and confirm it exits 0 (ALL GATES PASSED) with ING-094/103/111 green on real data; commit the produced `reports/ingestion/validation_<date>.md`.
    5. If monthly Peak is not published for part of the window, confirm the ING-104 base-only fallback engaged and LIMITATIONS.md reflects it (D-02).
  </how-to-verify>
  <resume-signal>Type "approved" once make validate-ingest exits 0 on real data and the report is committed, or describe which gate failed / which data is still pending.</resume-signal>
  <verify>
    <human-check>Operator confirms `make validate-ingest` exits 0 (ALL GATES PASSED) on real data and `reports/ingestion/validation_<date>.md` is committed.</human-check>
  </verify>
  <done>Reconciled data/manual/oespi_monthly.csv present, ING-094/103/111 green on real data, aggregate validation report committed under reports/ingestion/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw data (parquet/CSV) → run_gates | ingested + human-transcribed data crosses into the aggregate validation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Repudiation | gate aggregation | medium | mitigate | ValidationReport lists every registered gate exactly once (T-02-13); run_gates raises GateFailure on any failure (EN-061) — no silent pass-through |
| T-03-08 | Tampering | real ÖSPI CSV | high | mitigate | Double-entry reconcile (`oespi_reconcile.py`, ING-101) + `load_oespi` schema/gate validation before the value reaches the report; human checkpoint, never auto-approved (D-03, P-3) |
| T-03-SC | Tampering | pip installs | low | accept | No new packages introduced in this plan (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `epra.ingest.validate.gate_ing_111` — thin calendar gate wrapper for the aggregate report (new).
- Extended `run_gates` loading GeoSphere/ÖSPI/calendar and rendering ING-094/103/111 alongside ING-080..085.
- `run_gates` aggregate + `gate_ing_111` tests in `tests/unit/test_ingest_gates.py`.
- Human-checkpoint outputs (committed, not code): reconciled `data/manual/oespi_monthly.csv`, real GeoSphere parquet, `reports/ingestion/validation_<date>.md`.
</artifacts_produced>

<verification>
- `uv run pytest tests/unit/test_ingest_gates.py -m "not live"` green (aggregate wiring).
- `uv run pytest -m "not live"` full suite green network-free (D-06 CI close condition).
- Human checkpoint: `make validate-ingest` exits 0 on real data; report committed under `reports/ingestion/`.
- `uv run ruff check src/epra/ingest/validate.py` and `uv run mypy` clean.
</verification>

<success_criteria>
`make validate-ingest` runs one report over all M1+M2 gates (ING-080..085, 094, 103, 111), exits 0 network-free on fixtures, and — via the human checkpoint — exits 0 on the real reconciled ÖSPI CSV and live GeoSphere data, completing the ingestion layer (REQ-ING-01).
</success_criteria>

<output>
Create `.planning/phases/EPRA-03-m2-auxiliary-data/03-06-SUMMARY.md` when done.
</output>
