---
phase: EPRA-07-m6-strategy-simulator
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/epra/strategies/calibration.py
  - tests/unit/test_strategies_calibration.py
  - tests/unit/test_stubs_fail_loudly.py
autonomous: true
requirements:
  - ST-201
  - ST-202
  - ST-203
  - ST-204
must_haves:
  truths:
    - "Anchors frozen dataclass with p_ref_base, p_ref_peak, oespi_base_ref, oespi_peak_ref; all > 0; p_ref_peak >= p_ref_base asserted on compute (STOP if real data violates — do not delete assert)"
    - "p_ref_base = cost_S1(2019)/volume(2019) on aligned hours; synthetic 2019 matches hand calc"
    - "ST-202 function docstring contains the spec sentence about 2019 peak rescaled by realized-vs-base ratio"
    - "oespi refs = arithmetic mean of fct_price_monthly oespi_base/oespi_peak over calendar 2019; missing 2019 ÖSPI raises"
    - "compute_anchors returns a one-row frame plus writes CALIBRATED ssot_inputs_strategies rows for the four anchors (or returns Anchors for T6.05 to persist — pick one and test it); incomplete 2019 coverage raises a named error (callers skip in pytest)"
  artifacts:
    - src/epra/strategies/calibration.py
    - tests/unit/test_strategies_calibration.py
  key_links:
    - "T6.01 AlignedVolumes"
  prohibitions:
    - "do not multiply ÖSPI by volume (T-5)"
    - "do not extend ADR-010 fixture to 2019"
    - "do not assert p_ref_base in [30,60] on synthetic/fixture data"
    - "do not implement S2–S4 monthly engines"
---

<objective>
2019 calibration anchors ST-201..204.

Purpose: T6.02 — P_ref translation (T-5).
Output: Anchors + tests.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-07-m6-strategy-simulator/07-CONTEXT.md
</execution_context>

<context>
@docs/SPEC-05_strategy_simulator.md
@src/epra/strategies/align.py
@src/epra/strategies/calibration.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Anchors + synthetic 2019 tests</name>
  <read_first>
    - SPEC-05 §4
    - 07-RESEARCH.md ST-202 and T-5 identity
    - calibration.py existing docstring (keep ST-202 sentence in the implementing function)
  </read_first>
  <files>src/epra/strategies/calibration.py, tests/unit/test_strategies_calibration.py, tests/unit/test_stubs_fail_loudly.py</files>
  <action>
    Un-stub compute_anchors(settings, cfg, *, aligned=None, monthly_oespi=None). Reference year from cfg.reference_year. Peak mean uses is_peak_hour on aligned 2019 hours. Persist or return four CALIBRATED keys. Delete compute_anchors from STUBS. Tests inject frames; pytest.raises when 2019 missing. Implements: ST-201..204, T-5.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_strategies_calibration.py tests/unit/test_stubs_fail_loudly.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - Hand-computable synthetic anchors match.
    - ST-202 docstring grep-able.
  </acceptance_criteria>
  <done>S2/S3 can blend through P_ref.</done>
</task>
</tasks>
