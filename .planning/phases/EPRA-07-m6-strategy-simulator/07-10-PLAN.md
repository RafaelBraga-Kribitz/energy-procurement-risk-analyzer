---
phase: EPRA-07-m6-strategy-simulator
plan: 10
type: execute
wave: 10
depends_on:
  - 07-07
  - 07-08
  - 07-09
files_modified:
  - scripts/generate_golden_metrics.py
  - tests/golden/strategy_annual_summary.json
  - tests/test_golden_strategies.py
  - Makefile
  - docs/BUILD_LOG.md
  - tests/unit/test_stubs_fail_loudly.py
autonomous: true
requirements:
  - ST-601
  - ST-603
  - ST-604
  - ST-405
  - EN-050
must_haves:
  truths:
    - "generate_golden_metrics.py refuses dirty git tree; writes tests/golden/strategy_annual_summary.json from the same synthetic helper as tests (D-19)"
    - "tests/test_golden_strategies.py recomputes annual matrix and compares to JSON (ST-601)"
    - "determinism: two simulate-equivalent calls on injected frames → identical summary values (ST-405); Makefile simulate is retrospective then forward_risk; ssot is generate_ssot.py; neither invokes dbt"
    - "M6 stub rows gone; M7 charts stub remains"
    - "BUILD_LOG M6 entry: what shipped, pytest snippet, open questions (operator ST-602(a), human real golden, NUMERIC_SSOT commit) — no invented market EUR"
  artifacts:
    - scripts/generate_golden_metrics.py
    - tests/golden/strategy_annual_summary.json
    - tests/test_golden_strategies.py
    - Makefile
    - docs/BUILD_LOG.md
  key_links:
    - "verify-work checks ROADMAP SC and operator real warehouse"
  prohibitions:
    - "do not write fixture-warehouse euros into the golden and call them Austrian results"
    - "do not mix M7 README rewrite"
    - "do not un-stub render_executive_charts"
    - "do not claim GitHub required-check"
---

<objective>
Goldens, Makefile simulate/ssot, determinism, BUILD_LOG, M6 close-out.

Purpose: T6.10 — operator interface + ST-601 CI contract.
Output: golden JSON + make targets + BUILD_LOG.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-07-m6-strategy-simulator/07-CONTEXT.md
</execution_context>

<context>
@Makefile
@docs/BUILD_LOG.md
@docs/SPEC-05_strategy_simulator.md
@tests/unit/test_analytics_gates.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: golden script, ST-601 test, Makefile, BUILD_LOG</name>
  <read_first>
    - SPEC-05 §9
    - 07-CONTEXT D-03 D-19
    - Makefile analyze: analog
    - docs/BUILD_LOG.md latest entry style
  </read_first>
  <files>scripts/generate_golden_metrics.py, tests/golden/strategy_annual_summary.json, tests/test_golden_strategies.py, Makefile, docs/BUILD_LOG.md, tests/unit/test_strategies_gates.py, tests/unit/test_stubs_fail_loudly.py</files>
  <action>
    Implement dirty-tree guard (subprocess git status --porcelain). Golden from synthetic frames committed as engine contract. test_golden_strategies compares. Add tests/unit/test_strategies_gates.py: monkeypatch loaders, wipe reports/strategies known files, retrospective.main + forward_risk.main on tmp, optional assemble. Un-stub Makefile simulate and ssot. BUILD_LOG 2026-09-03 (or today) M6. Confirm STUBS only M7. make lint && pytest not live.
  </action>
  <verify>
    <automated>make lint && uv run pytest -m "not live" -q</automated>
  </verify>
  <acceptance_criteria>
    - make simulate / ssot recipes are the CLIs
    - ST-601 green on synthetic JSON
    - coverage ≥ 80%
    - BUILD_LOG does not present toy euros as market facts
  </acceptance_criteria>
  <done>M6 operator close-out ready for verify-work.</done>
</task>
</tasks>
