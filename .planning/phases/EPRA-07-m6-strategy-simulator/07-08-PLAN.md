---
phase: EPRA-07-m6-strategy-simulator
plan: 08
type: execute
wave: 8
depends_on:
  - 07-05
  - 07-07
files_modified:
  - src/epra/report/ssot.py
  - scripts/generate_ssot.py
  - docs/ADR/ADR-016_ssot-updated-at-rounding-sg09.md
  - tests/unit/test_ssot.py
autonomous: true
requirements:
  - GV-301
  - GV-302
  - ST-204
must_haves:
  truths:
    - "assemble reads ssot_inputs_*.parquet only (never recomputes costs); duplicate keys raise; sort by key"
    - "markdown columns key|value|unit|tag|produced_by|updated_at; updated_at = max input mtime ISO-8601 UTC (ADR-016)"
    - "two assemble calls with unchanged inputs → byte-identical markdown"
    - "GV-302 minimum keys present when producers supplied; oespi_peak_ref also present (ST-204); missing years omitted not zero-filled"
    - "data_last_month from marts (YYYY-MM), produced_by epra.report.ssot, tag VERIFIED — injectable in tests"
    - "E-2: renderer does not retype tags; scripts/generate_ssot.py is a thin shell"
  artifacts:
    - src/epra/report/ssot.py
    - scripts/generate_ssot.py
    - docs/ADR/ADR-016_ssot-updated-at-rounding-sg09.md
    - tests/unit/test_ssot.py
  key_links:
    - "profile + analytics producer files already exist"
  prohibitions:
    - "do not use datetime.now for updated_at"
    - "do not commit reports/NUMERIC_SSOT.md with synthetic euros (D-04)"
    - "do not call simulate from generate_ssot"
    - "do not read ENTSO-E raw for data_last_month"
---

<objective>
SSOT generator (GV-301/302) + ADR-016.

Purpose: T6.08 — single numeric table.
Output: `epra.report.ssot` + script + ADR.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-07-m6-strategy-simulator/07-CONTEXT.md
</execution_context>

<context>
@docs/SPEC-08_governance_quality.md
@docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md
@src/epra/analytics/_kit.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: assemble markdown + ADR-016 + CLI</name>
  <read_first>
    - SPEC-08 §3
    - 07-RESEARCH.md GV-302 enumeration and P5/P10
    - ADR-013 format
  </read_first>
  <files>src/epra/report/ssot.py, scripts/generate_ssot.py, docs/ADR/ADR-016_ssot-updated-at-rounding-sg09.md, tests/unit/test_ssot.py</files>
  <action>
    Write ADR-016 covering updated_at and the half-up rule used by T6.09 (can state checker will implement ROUND_HALF_UP). assemble(settings, *, frames=None, data_last_month=None, mtimes=None). Write reports/NUMERIC_SSOT.md under settings.paths.reports. CLI load_settings, missing producers raise listing files. Tests on tmp_settings only.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ssot.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - Byte-identical second assemble; no committed fake SSOT.
  </acceptance_criteria>
  <done>make ssot can be wired in T6.10.</done>
</task>
</tasks>
