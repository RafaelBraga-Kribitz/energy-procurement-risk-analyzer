---
phase: EPRA-07-m6-strategy-simulator
plan: 06
type: execute
wave: 6
depends_on:
  - 07-05
files_modified:
  - src/epra/strategies/retrospective.py
  - tests/unit/test_strategies_sensitivities.py
autonomous: true
requirements:
  - ST-303
must_haves:
  truths:
    - "exactly three sensitivity blocks: premium {0,5,10}, flat_baseload, lock_window 1..12 of Y-1"
    - "each is a config-delta rerun of the same cost engine (model_copy / cfg replace) — no forked formulas"
    - "flat_baseload rebuilds load via build_profile + re-align, not a hacked volume scalar"
    - "reports/strategies/sensitivity_matrix.md one compact table; ST-502 present; no fourth block"
  artifacts:
    - tests/unit/test_strategies_sensitivities.py
  key_links:
    - "consumer.profile build_profile profile_name=flat_baseload"
  prohibitions:
    - "do not add peak_available as a sensitivity table row (D-15 is a unit branch already)"
    - "do not invent extra what-ifs (A-3)"
    - "do not commit the md from fixture warehouse"
---

<objective>
ST-303 three sensitivities as config-delta reruns.

Purpose: T6.06 — scope-guarded what-ifs.
Output: sensitivity_matrix.md + tests.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-07-m6-strategy-simulator/07-CONTEXT.md
</execution_context>

<context>
@docs/SPEC-05_strategy_simulator.md
@src/epra/consumer/profile.py
@src/epra/common/config.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: sensitivity_matrix via same engine</name>
  <read_first>
    - SPEC-05 ST-303
    - 07-CONTEXT D-14
    - load_consumer_profile / build_profile
  </read_first>
  <files>src/epra/strategies/retrospective.py, tests/unit/test_strategies_sensitivities.py</files>
  <action>
    Add run_sensitivities(settings, ...) writing sensitivity_matrix.md. Premium loop uses cfg.model_copy(update={...}). Lock window full prior year. Flat profile: copy consumer cfg profile_name flat_baseload, build_profile on injected calendar, align with same prices. Tests assert three headings and that a fourth known phrase is absent. Call from retrospective.run after baseline (or explicit flag). Functions ~60 lines.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_strategies_sensitivities.py tests/unit/test_strategies_retrospective.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - Three blocks only; ST-502 in file.
  </acceptance_criteria>
  <done>ST-303 closed.</done>
</task>
</tasks>
