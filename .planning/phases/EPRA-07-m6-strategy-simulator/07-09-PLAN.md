---
phase: EPRA-07-m6-strategy-simulator
plan: 09
type: execute
wave: 9
depends_on:
  - 07-08
files_modified:
  - src/epra/report/ssot_check.py
  - scripts/check_ssot_consistency.py
  - scripts/ssot_whitelist.txt
  - .github/workflows/ci.yml
  - tests/unit/test_ssot_check.py
autonomous: true
requirements:
  - GV-303
  - EN-080
must_haves:
  truths:
    - "checker tokenizes README.md and reports/EXEC_SUMMARY.md for number+unit (EUR, EUR/MWh, %, hours, M); match iff |literal-round_half_up(ssot,d)|=0"
    - "scripts/ssot_whitelist.txt every line has # reason; year 2022 does not false-positive as a result euro"
    - "mutation test: change one README digit adjacent to a unit → fail naming a key or the literal"
    - "if reports/NUMERIC_SSOT.md missing: skip GV-302 completeness, still scan docs, exit 0 when no unmatched tokens (D-04/D-18)"
    - "ci.yml job ssot-check added (checkout, uv, run checker); do not claim GitHub required"
  artifacts:
    - src/epra/report/ssot_check.py
    - scripts/check_ssot_consistency.py
    - scripts/ssot_whitelist.txt
    - .github/workflows/ci.yml
  key_links:
    - "ADR-016 rounding"
  prohibitions:
    - "do not use Python round (banker)"
    - "do not write README euros in this plan (M7)"
    - "do not mark the GitHub check required via API"
---

<objective>
GV-303 checker + CI job 4.

Purpose: T6.09 — doc/number integrity.
Output: ssot_check + whitelist + ci job.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-07-m6-strategy-simulator/07-CONTEXT.md
</execution_context>

<context>
@docs/SPEC-08_governance_quality.md
@.github/workflows/ci.yml
@README.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: checker + whitelist + ssot-check job</name>
  <read_first>
    - SPEC-08 GV-303
    - 07-PATTERNS.md half-up snippet
    - current README for existing numerals (years, Python 3.12, etc.)
  </read_first>
  <files>src/epra/report/ssot_check.py, scripts/check_ssot_consistency.py, scripts/ssot_whitelist.txt, .github/workflows/ci.yml, tests/unit/test_ssot_check.py</files>
  <action>
    Implement parse + match. Whitelist years, section numbers, config echoes, coverage 80%, GWh 50, etc. with comments. CI job mirrors lint/test setup then `uv run python scripts/check_ssot_consistency.py`. Tests: mutation, whitelist 2022, missing SSOT skip path, half-up 1.25→1.3 at 1 decimal vs 1.2 banker trap.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ssot_check.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - Job present in ci.yml; checker green on current README with whitelist.
  </acceptance_criteria>
  <done>EN-080 job 4 exists as code.</done>
</task>
</tasks>
