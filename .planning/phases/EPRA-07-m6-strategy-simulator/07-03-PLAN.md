---
phase: EPRA-07-m6-strategy-simulator
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - src/epra/strategies/retrospective.py
  - tests/unit/test_strategies_retrospective.py
autonomous: true
requirements:
  - ST-101
  - ST-301
must_haves:
  truths:
    - "cost_s1(hourly aligned) returns monthly year_local, month_local, strategy_id=S1, volume_mwh, cost_eur, unit_cost_eur_mwh"
    - "synthetic month load×price sums match to the cent (float64)"
    - "cost_s1 does not drop NULLs itself (precondition: no NULL prices)"
    - "retrospective.run still may raise until T6.04/T6.05 wires full CLI — prefer exporting cost_s1 now and keep run() raising until T6.05 if that keeps stub test honest; if run() is partially wired, delete only the stub row when main() works"
  artifacts:
    - src/epra/strategies/retrospective.py
    - tests/unit/test_strategies_retrospective.py
  key_links:
    - "T6.04 reuses monthly S1 as the spot leg"
  prohibitions:
    - "do not implement p_s2/p_s3 yet unless tiny helpers are needed for types"
    - "do not write parquet or charts yet"
    - "do not invent 2022>>2019 real-data asserts in CI"
---

<objective>
Retrospective S1 hourly spot cost.

Purpose: T6.03 — FULL_SPOT monthly engine.
Output: `cost_s1` + tests.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-07-m6-strategy-simulator/07-CONTEXT.md
</execution_context>

<context>
@docs/SPEC-05_strategy_simulator.md
@src/epra/strategies/align.py
@src/epra/strategies/retrospective.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: cost_s1 + hand-computed month test</name>
  <read_first>
    - SPEC-05 ST-101
    - 03_MODULES cost_s1 contract
  </read_first>
  <files>src/epra/strategies/retrospective.py, tests/unit/test_strategies_retrospective.py</files>
  <action>
    Implement cost_s1(hourly: pd.DataFrame) -> monthly frame. strategy_id S1. unit_cost = cost/volume. Docstring Implements: ST-101. Keep run()/main as NotImplementedError until T6.05 unless cheap to leave a private helper only. Tests: two-hour month 1.0 MWh × 10 and 2.0 × 20 → cost 50, volume 3, unit 50/3. Do not delete retrospective.run stub yet.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_strategies_retrospective.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - Hand month matches to the cent.
    - run() still loud-fails if not fully wired (M6 stub row remains).
  </acceptance_criteria>
  <done>S4 can scale the S1 monthly leg.</done>
</task>
</tasks>
