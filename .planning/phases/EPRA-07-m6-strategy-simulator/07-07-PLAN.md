---
phase: EPRA-07-m6-strategy-simulator
plan: 07
type: execute
wave: 7
depends_on:
  - 07-05
files_modified:
  - src/epra/strategies/forward_risk.py
  - docs/ADR/ADR-014_st401-day-mapping-sg07.md
  - docs/ADR/ADR-015_quantile-cvar-sg08.md
  - tests/unit/test_strategies_forward.py
  - tests/unit/test_stubs_fail_loudly.py
autonomous: true
requirements:
  - ST-401
  - ST-402
  - ST-403
  - ST-404
  - ST-405
  - ST-406
  - ST-602
must_haves:
  truths:
    - "ADR-014 documents SG-07 mapping; mapper is deterministic; DST fill rules as D-10"
    - "ADR-015 pins numpy.quantile method=linear and CVaR=mean of ceil(0.05*N) highest"
    - "CostCells grain (horizon_month, pool_year, strategy_id); cell values equal a 2-month toy per-path recompute"
    - "simulate uses one default_rng(seed); path-major month-minor; prices+ÖSPI via one pool_year (T-6)"
    - "S3 lock uses observed ÖSPI when window past; drawn branch unit-tested with synthetic future lock"
    - "summarize mean/std/P5/P50/P95/CVaR95; P5<=P50<=P95; ST-602(c) on a crafted S1-fatter-tail sample"
    - "no-crisis pool uses regimes.fit_hmm + december_regime; crisis Decembers excluded; labels injectable"
    - "charts s5_forward_fan.png and s5_risk_return.png tag SIMULATED + ST-502; forward_risk run/main un-stubbed"
  artifacts:
    - src/epra/strategies/forward_risk.py
    - docs/ADR/ADR-014_st401-day-mapping-sg07.md
    - docs/ADR/ADR-015_quantile-cvar-sg08.md
    - tests/unit/test_strategies_forward.py
  key_links:
    - "T6.05 cost functions; M5 december_regime calm-wins"
  prohibitions:
    - "do not implement N=2000 hourly nested loops as the production path"
    - "do not reimplement GaussianHMM"
    - "do not draw prices and ÖSPI independently"
    - "do not clamp or invent missing ÖSPI"
    - "do not commit forward PNGs"
    - "do not follow guide 5.5 crisis-wins ties"
---

<objective>
Vectorized seasonal block bootstrap (ST-406) + ADRs 014/015.

Purpose: T6.07 — Q3 forward risk.
Output: cells, simulate, summarize, charts, ADRs.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-07-m6-strategy-simulator/07-CONTEXT.md
</execution_context>

<context>
@docs/SPEC-05_strategy_simulator.md
@docs/EXECUTION_BLUEPRINT/14_SPEC_GAPS.md
@src/epra/analytics/regimes.py
@src/epra/strategies/retrospective.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: ADR-014/015 + CostCells + simulate + summarize + CLI</name>
  <read_first>
    - SPEC-05 §6
    - 07-CONTEXT D-07..D-12
    - 07-RESEARCH.md cells and pitfalls P2 P13
    - existing ADR-013 for format
  </read_first>
  <files>src/epra/strategies/forward_risk.py, docs/ADR/ADR-014_st401-day-mapping-sg07.md, docs/ADR/ADR-015_quantile-cvar-sg08.md, tests/unit/test_strategies_forward.py, tests/unit/test_stubs_fail_loudly.py</files>
  <action>
    Write ADRs 014 and 015 (accepted). map_month(drawn_hourly, target_index, calendar flags). build_cost_cells(...)->CostCells. simulate(cells, rng_seed, n_paths, pool). summarize(paths). Unconditional + no-crisis tables. run() writes reports/strategies/ forward artifacts and ssot_inputs_strategies SIMULATED keys. Delete forward_risk stub rows. Tests: equivalence toy, determinism×2, quantile closed form, DST mapping fixture, drawn lock branch, crisis years excluded. n_paths in tests is small (e.g. 20); do not require 2000 in pytest.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_strategies_forward.py tests/unit/test_stubs_fail_loudly.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - ADRs committed; ST-405 identity on summarize values; no M6 forward stubs.
  </acceptance_criteria>
  <done>Q3 engine exists; Makefile still stub until T6.10 (or un-stub simulate in T6.10 — this plan may un-stub forward CLI only).</done>
</task>
</tasks>
