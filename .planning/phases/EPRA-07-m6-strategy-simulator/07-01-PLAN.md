---
phase: EPRA-07-m6-strategy-simulator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/epra/strategies/align.py
  - tests/unit/test_strategies_align.py
autonomous: true
requirements:
  - ST-001
  - ST-101
  - ST-501
must_haves:
  truths:
    - "align_hourly inner-joins load and prices on ts_utc and drops NULL price_at_eur_mwh; monthly volume is sum of remaining load_mwh"
    - "synthetic month with 3 NULL price hours yields identical monthly volume for every strategy_id (volume is strategy-agnostic)"
    - "dropped hour count is logged; AlignedVolumes is a frozen dataclass with hourly, monthly, dropped_hours"
    - "run() loaders query marts.* via connect(read_only=True) and raise with the SQL text when empty; unit tests inject frames and do not need DuckDB"
    - "w_peak loader reads ssot_inputs_profile.parquet key consumer_peak_share or raises naming the path"
  artifacts:
    - src/epra/strategies/align.py
    - tests/unit/test_strategies_align.py
  key_links:
    - "T6.02+ consume AlignedVolumes only"
  prohibitions:
    - "do not implement S1–S4 costs or anchors"
    - "do not call load_oespi"
    - "do not read data/raw"
    - "do not invent w_peak"
---

<objective>
Strategy data access + ST-101 volume alignment.

Purpose: T6.01 — fairness by construction.
Output: `align.py` + tests.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-07-m6-strategy-simulator/07-CONTEXT.md
</execution_context>

<context>
@docs/SPEC-05_strategy_simulator.md
@/workspace/.planning/phases/EPRA-07-m6-strategy-simulator/07-RESEARCH.md
@src/epra/analytics/_kit.py
@dbt/contracts/marts_contract.yml
</context>

<tasks>
<task type="auto">
  <name>Task 1: AlignedVolumes + mart loaders + w_peak</name>
  <read_first>
    - 07-CONTEXT.md D-01 D-02
    - 07-PATTERNS.md aligner snippet
    - src/epra/analytics/_kit.py _fetch
    - src/epra/consumer/profile.py SSOT schema
  </read_first>
  <files>src/epra/strategies/align.py, tests/unit/test_strategies_align.py</files>
  <action>
    Implement AlignedVolumes (frozen dataclass). align_hourly(load, prices) → dropna price_at_eur_mwh; monthly groupby year_local, month_local. Loaders: SQL constants for fct_price_hourly, fct_consumer_load_hourly, fct_price_monthly, fct_price_daily; empty → RuntimeError including SQL. load_w_peak(settings) from processed ssot_inputs_profile.parquet. Optional run(settings) that only validates loads is NOT required — keep align.py free of retrospective CLI. Tests: 3 NULLs; missing w_peak file raises; empty SQL raise via monkeypatch or tiny duckdb if easier. Docstrings Implements: ST-101, ST-501, D-01, D-02. ASCII hyphens only (RUF001).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_strategies_align.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - Align tests green; no strategy cost formulas yet.
    - Functions stay ~60 lines (W-3); compose helpers.
  </acceptance_criteria>
  <done>Later plans cost pre-aligned frames only.</done>
</task>
</tasks>
