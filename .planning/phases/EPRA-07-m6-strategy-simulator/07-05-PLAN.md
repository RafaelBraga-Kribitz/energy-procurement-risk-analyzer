---
phase: EPRA-07-m6-strategy-simulator
plan: 05
type: execute
wave: 5
depends_on:
  - 07-04
files_modified:
  - src/epra/strategies/retrospective.py
  - src/epra/warehouse/report.py
  - tests/unit/test_strategies_retrospective.py
  - tests/unit/test_stubs_fail_loudly.py
autonomous: true
requirements:
  - ST-301
  - ST-302
  - ST-304
  - ST-001
  - ST-602
must_haves:
  truths:
    - "annual summary has year, strategy_id, volume_mwh, cost_eur, unit_cost_eur_mwh, delta_vs_min_eur, rank"
    - "wrong_strategy_cost(Y)=max-min; SSOT keys wrong_strategy_cost_<year> and _total CALIBRATED when years present"
    - "ST-602(a): if 2022 S1 and S3 present, require cost_S1>cost_S3 else raise; if 2022 incomplete, skip (log) not pass"
    - "charts s5_annual_costs.png grouped bar and s5_cumulative.png use STRATEGY_COLORS; ST502 on S2/S3/S4; object-inspected; tag CALIBRATED; LP-050 on S1 volume charts"
    - "dual-write strategy_costs_monthly.parquet and procurement_cost_monthly/strategy_costs_monthly.parquet after wiping the glob dir; int64 year/month"
    - "python -m epra.strategies.retrospective run/main un-stubbed; missing warehouse exit 1"
  artifacts:
    - src/epra/strategies/retrospective.py
    - tests/unit/test_strategies_retrospective.py
  key_links:
    - "T6.07 cells can reuse monthly cost functions"
    - "warehouse stand-in flag: stop listing fct_procurement_cost_monthly as stand-in when real file is the contract — or keep flag for bootstrap-only; prefer clear stand-in iff glob still bootstrap-shaped is too heuristic — remove from _STAND_IN_MARTS in this plan and update warehouse report tests"
  prohibitions:
    - "do not commit reports/strategies PNGs"
    - "do not invoke dbt"
    - "do not rationalize ST-602(a) by widening"
    - "do not invent missing years as zero cost"
---

<objective>
Annual summary, headline, ST-304 charts, dual-write parquet, un-stub retrospective CLI.

Purpose: T6.05 — Q1 matrix + dbt drop-in.
Output: `run()` + parquet + charts.
</objective>

<execution_context>
@/workspace/.planning/phases/EPRA-07-m6-strategy-simulator/07-CONTEXT.md
</execution_context>

<context>
@docs/SPEC-05_strategy_simulator.md
@src/epra/report/style.py
@src/epra/analytics/_kit.py
@src/epra/warehouse/report.py
@dbt/models/sources.yml
</context>

<tasks>
<task type="auto">
  <name>Task 1: annual_summary + charts + dual-write + CLI</name>
  <read_first>
    - SPEC-05 §5
    - 07-CONTEXT D-03 D-05 D-06
    - warehouse _STAND_IN_MARTS
    - analytics __main__ missing-warehouse pattern
  </read_first>
  <files>src/epra/strategies/retrospective.py, src/epra/warehouse/report.py, tests/unit/test_strategies_retrospective.py, tests/unit/test_warehouse_report.py, tests/unit/test_stubs_fail_loudly.py</files>
  <action>
    Wire run(settings, *, aligned=None, oespi=None, anchors=None, w_peak=None). Config years from yaml. Write reports/strategies/ unit-cost md with ST-502 and AN-704-style ≥400 chars using computed numbers (RP-703) — no invented EUR. Wipe then dual-write parquet. Remove retrospective.run/main from STUBS. Remove fct_procurement_cost_monthly from _STAND_IN_MARTS and fix warehouse tests. ST-602(a) helper returns skip/fail/pass like AN-304.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_strategies_retrospective.py tests/unit/test_warehouse_report.py tests/unit/test_stubs_fail_loudly.py -m "not live" --no-cov -q</automated>
  </verify>
  <acceptance_criteria>
    - Dual-write files exist under tmp_settings; glob dir has no leftover stand-in names from the test wipe.
    - CLI missing warehouse returns 1.
  </acceptance_criteria>
  <done>Q1 artifacts exist on injected frames.</done>
</task>
</tasks>
